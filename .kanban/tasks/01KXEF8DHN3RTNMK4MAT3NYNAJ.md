---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kxg3nwnmajn6c348wbsqkt8c
  text: |-
    Implementation landed. New policy in `ConstraintSetup.reserves(forMaxTokens:)`:
    ```
    hardReserve = structuralReserve * 8   // unchanged
    normalZoneLength = min(structuralReserve * 3, maxTokens / 2)
    completionReserve = max(maxTokens - normalZoneLength, hardReserve)
    ```
    Replaces the old `max(structuralReserve * 3, maxTokens / 4)` floor, which let the `maxTokens/4` term dominate for virtually every realistic schema (soft zone never engaged until 75% through the budget regardless of schema size).

    Empirical tuning against the real previously-failing test (`intRoundTripGemma3270m`, xcodebuild against cached mlx-community/gemma-3-270m-it-4bit):
    - First candidate multiplier 10x: reduced the runaway from 3072 repeated "2" digits down to 20 -- still overflowed Int64 (JSONDecoder.dataCorrupted), because the soft-zone bias flips to EOS almost immediately once it engages (confirmed at both the original bug's token 3072 and this run's ~token 20) -- widening the runway doesn't help, starting the zone earlier does.
    - Reduced multiplier to 3x (matching the precedent already used elsewhere: GenerableRoundTripTests, HardReserveStressTests, GuidedGenerationTests all recompute `structuralReserve * 3` locally). Re-ran: model now produces "222222" (6 digits), decodes fine, test passes.

    Re-enabled `intRoundTripGemma3270m` (removed `.disabled`, rewrote its doc comment to describe the fix instead of the live bug).

    Full regression sweep (all real models, no mocks):
    - IntegrationTesting (xcodebuild, scheme IntegrationTesting): MultiModelGuidedGenerationTests 8/8, GenerableRoundTripTests 17/17, GuidedGenerationIntegrationTests 6/6, GuidedGenerationUsageTests 3/3, PrewarmGrammarTests 2/2, StopTokenRegressionIntegrationTests 4/4, HardReserveStressTests 6/6, GuidedGenerationTests 8/8 -- all green.
    - `swift test --filter MLXFoundationModelsTests`: 207/207 pass.
    - `swift test --filter MLXGuidedGenerationTests`: 63/63 pass.
    - `swift build --target MLXFoundationModels`: clean.

    Adversarial double-check agent dispatched to review the diff before handoff.
  timestamp: 2026-07-14T10:45:03.796349+00:00
- actor: claude-code
  id: 01kxg4cw0220jhz1tmzmdbd5gg
  text: |-
    Adversarial double-check (double-check agent) returned REVISE with one finding, PASS on everything else:
    - Math check: confirmed `completionReserve` is always non-negative and always `>= hardReserve` by construction (an actual correctness improvement over the old formula, which could invert zone order). Traced the `maxTokens: 8` / ContactForm edge case in `incompleteOutputYieldsMetadata` -- hard zone still dominates from token 0, same as before.
    - Call-site enumeration: confirmed exactly the two call sites I found (`runGuidedGenerationLoop`, `resolvePhase2MaxTokens`), no others.
    - Test-file re-enable: clean, no leftover disabled trait or stale references.
    - Finding: my doc comment on `normalZoneMultiplier` claimed `GenerableRoundTripTests`, `HardReserveStressTests`, and `GuidedGenerationTests` "recompute" the 3x multiplier as corroboration -- but those three files hardcode the OLD pre-fix `max(structuralReserve * 3, maxTokens / 4)` formula verbatim via their own local helpers that call `GuidedGenerationLoop.run` directly, bypassing `ConstraintSetup.reserves` entirely. Their passing doesn't validate this new capped-normal-zone policy.

    Fixed: reworded the doc comment to state plainly that 3x is reused only as a numerically-familiar starting point, and explicitly disclaims that those three test files corroborate anything about the new policy (they exercise an unrelated, unchanged direct-call path). Did NOT touch the three test files themselves (their own "mirrors production" comments predate this task and drifting is a disclosed, out-of-scope side effect of fixing the shared formula -- touching unrelated test files would be scope creep on a task explicitly scoped to `ConstraintSetup.reserves` + its own regression verification). Rebuilt clean after the doc fix; no logic changed, so the full regression sweep result already recorded stands.
  timestamp: 2026-07-14T10:57:36.770788+00:00
position_column: done
position_ordinal: b780
title: 'GuidedGeneration: completion-reserve policy lets trivial schemas run unbiased for most of the token budget, allowing runaway repetition'
---
## What\nRoot-caused while investigating kanban 68bc1rt (`MultiModelGuidedGenerationTests.intRoundTrip(modelID: gemma-3-270m-it-4bit)` DecodingError.dataCorrupted).\n\n`Executor.ConstraintSetup.reserves(forMaxTokens:)` in `Libraries/MLXFoundationModels/MLXLanguageModel.swift`:\n```swift\nfunc reserves(forMaxTokens maxTokens: Int) -> (completionReserve: Int, hardReserve: Int) {\n    (Swift.max(structuralReserve * 3, maxTokens / 4), structuralReserve * 8)\n}\n```\n`completionReserve` determines how many tokens before `maxTokens` the soft-zone closing bias (`GuidedGenerationLoop`'s `applyBiasAndSample`, +200 EOS / +100 closing-tier bias) starts nudging generation toward completion. The soft zone begins at token index `maxTokens - completionReserve`.\n\nFor a schema whose minimal JSON skeleton is tiny (e.g. bare `Int` -> `CompletionReserve.estimate` returns `\"0\"` tokenized, ~1-3 tokens), `structuralReserve * 3` is negligible, so `max(...)` always picks the `maxTokens / 4` floor. With the Executor's `defaultMaxTokens = 4096`, that floor is `1024`, so the soft zone doesn't begin until token `3072` -- 75% of the ENTIRE budget runs with zero completion bias, regardless of how trivially small the schema actually is.\n\nReproduced directly: `mlx-community/gemma-3-270m-it-4bit` asked \"What is 2+2? Reply with just the number.\" under an `Int` grammar constraint. The grammar makes `EOS` legal after every single digit (since `\"2\"`, `\"22\"`, `\"222\"`, ... are all valid, complete JSON integers), but the model's own raw preference (a known small-model repetition-degeneration failure mode) kept greedily selecting the digit `2` over `EOS` for the entire unbiased normal zone. At token 3072 the soft-zone `+200 EOS` bias FINALLY tipped the decision to `EOS` on the very first biased sample -- but by then the accumulated output was 3072 copies of `\"2\"`, a JSON-valid but astronomically large integer literal that overflows Swift's `Int` and fails `JSONDecoder` with `dataCorrupted`.\n\nThis is a genuine adapter-policy gap, not a grammar-correctness bug (the grammar mask worked exactly as designed) and not pure \"model incapability\" (the bias mechanism that's supposed to prevent exactly this only engages far too late for trivial schemas). Confirmed via a real, deterministic (greedy/temp=0) run -- not a flake.\n\n## Why not fixed directly under 68bc1rt\n`ConstraintSetup.reserves` is the single call site (`Executor.runGuidedGenerationLoop` and `reasoningAdjustedMaxTokens`) driving reserve sizing for EVERY guided-generation and tool-calling request that goes through `MLXLanguageModel.Executor.respond` -- i.e. most of `Tests/MLXFoundationModelsTests` and `IntegrationTesting/.../GuidedGeneration/*` (GenerableRoundTripTests, GuidedGenerationIntegrationTests, GuidedGenerationUsageTests, PrewarmGrammarTests, StopTokenRegressionIntegrationTests, MultiModelGuidedGenerationTests, tool-calling phase-2 budgeting, etc.). `HardReserveStressTests.swift` is NOT affected (it recomputes the same formula locally rather than calling `ConstraintSetup.reserves`), but the rest of the suite routes through this one function. Retuning it safely requires designing a new policy (e.g. anchoring the \"free normal-zone\" runway to schema complexity rather than a flat fraction of `maxTokens`, without regressing the deliberately-generous behavior `HardReserveStressTests`-style large/nested schemas rely on) AND rerunning the full real-model guided-generation suite to confirm no regressions -- a properly-scoped, separately-budgeted task, not a drive-by fix bundled into 68bc1rt's checkpoint-hygiene/4-test-triage scope.\n\n## Suggested direction\nMake the \"unbiased normal zone\" length scale with `structuralReserve` (the schema's actual minimal-content need) with a modest cap, instead of an unconditional `maxTokens / 4` floor that dominates for trivial schemas regardless of `maxTokens` size. Needs empirical tuning + full regression pass across the guided-generation integration suite (real models, real generations) before landing.\n\n## Resolution\nLanded policy in `ConstraintSetup.reserves(forMaxTokens:)`:\n```swift\nfunc reserves(forMaxTokens maxTokens: Int) -> (completionReserve: Int, hardReserve: Int) {\n    let hardReserve = structuralReserve * 8\n    let normalZoneLength = Swift.min(structuralReserve * 3, maxTokens / 2)\n    let completionReserve = Swift.max(maxTokens - normalZoneLength, hardReserve)\n    return (completionReserve, hardReserve)\n}\n```\nSee comments on `normalZoneMultiplier` and `reserves(forMaxTokens:)` for full rationale. Empirically tuned against real `mlx-community/gemma-3-270m-it-4bit` runs (10x multiplier reduced the runaway from 3072 to 20 repeated digits, still overflowed Int64; 3x converges to 6 digits, decodes fine). Full real-model regression sweep green: MultiModelGuidedGenerationTests 8/8, GenerableRoundTripTests 17/17, GuidedGenerationIntegrationTests 6/6, GuidedGenerationUsageTests 3/3, PrewarmGrammarTests 2/2, StopTokenRegressionIntegrationTests 4/4, HardReserveStressTests 6/6, GuidedGenerationTests 8/8 (all via xcodebuild against real cached models), plus `swift test --filter MLXFoundationModelsTests` 207/207 and `swift test --filter MLXGuidedGenerationTests` 63/63. See task comments for full detail and the adversarial double-check finding (doc-comment overclaim, fixed).\n\n## Acceptance Criteria\n- [x] New reserve-sizing policy designed and documented (why it doesn't regress large/nested-schema behavior)\n- [x] `MultiModelGuidedGenerationTests.intRoundTripGemma3270m` (currently `.disabled`, added under 68bc1rt; renamed from `intRoundTripGemma3_270m` during 68bc1rt's review pass to match this file's lowerCamelCase convention) re-enabled and passing\n- [x] Full `GuidedGeneration`-suite real-model regression run attached/referenced, zero unexplained failures\n