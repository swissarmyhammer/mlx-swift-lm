---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m3cpq89zxp84z1hxkfrrxw2f
  text: |-
    Research:
    - `ExecutorPromptCacheSlot.init(_ checkout:key:)` has no `report:` parameter, thus a test cannot read the plan line through it. The test checks out from the store, gets the handle with `#require(checkout.spilledHandle)` (this proves `.spilled`), and makes the slot with `init(spilled:key:report:)`. `init(_ checkout:key:)` calls the same init for `.spilled`.
    - `QwenCommittedTurnRule` is internal in MLXLMCommon. The test file needs `@testable import MLXLMCommon` (SamplingModeMapperTests in the same bundle does this).
    - The restored caches are `KVCacheSimple` (trimmable), thus the control (no render on record) gives `rewind`, not `rebuild`. The control expects a rule other than `splice` and a reuse count smaller than the ledger.
    - Test rules: no `guard` in a test (use `#require`), no unnamed numeric literals at call sites or comparisons.
  timestamp: 2026-09-25T16:35:04.639005+00:00
- actor: claude-code
  id: 01m3cpscrbgqk1pmqww2s68gxz
  text: |-
    ### implement — changed
    - evidence: 2 files — Tests/MLXFoundationModelsTests/ExecutorPromptCacheRestoreTests.swift (2 new tests, fixture `CommittedTurn`, `@testable import MLXLMCommon`), Tests/MLXFoundationModelsTests/PromptCacheSpoolTestSupport.swift (`entry(tokens:renderTokens:)` with defaults). MLXFoundationModelsTests: 303 tests passed (was 301).
    - The splice test passed at the first run: the library has no bug. The control shows that the file carries the render: with no render on record the plan is not `splice`.
    - next: /test
  timestamp: 2026-09-25T16:36:14.731616+00:00
- actor: claude-code
  id: 01m3cpyd70xr4zfazqxjkv7ye1
  text: |-
    ### test — green
    - evidence: `swift build --build-tests` — only the known warning `missing creator for mutated node`. `xcrun xctest` on all five bundles: MLXLMTests 695 XCTest + 1208 Swift Testing, MLXGuidedGenerationTests 70, MLXFoundationModelsTests 303, CXGrammarTests 7, MLXHuggingFaceMacrosTests 5; 0 failures, 0 skipped. MLXFoundationModelsTests ran 3 times: 303 passed each time.
    - next: /commit
  timestamp: 2026-09-25T16:38:59.040218+00:00
position_column: doing
position_ordinal: '80'
title: Test that the Qwen committed-turn rule splices after a restore from disk
---
## What

Path gap. The parts are covered one at a time:
- `renderTokens` survives the file: `Tests/MLXFoundationModelsTests/ExecutorPromptCacheFileTests.swift:248-249`.
- A protocol rule splices from an in-memory entry: `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:738-778`.
- `QwenCommittedTurnRule` alone: `Tests/MLXLMTests/QwenCommittedTurnRuleTests.swift:60-127`. It declines when no render is on record (168-174).
- `ExecutorPromptCacheSlot.restoreIfPending(model:parameters:)` (`Libraries/MLXFoundationModels/ExecutorPromptCache.swift:1653-1666`): 100%, but only the test at `ExecutorPromptCacheRestoreTests.swift:121-143` plans after a restore, and it plans an `extend`, not a `splice`.

No unit test proves the whole chain: an entry with a committed Qwen turn goes to disk, comes back through the slot, and the next plan is `.splice` with the reuse count of the committed turn. When `renderTokens` is lost in the file, a hybrid model cannot rewind and the turn rebuilds cold, thus this is the Qwen agent loop case.

What to test:
- Make an entry whose `tokens` are a render plus a generated turn that ends with the Qwen end-of-turn token, and whose `renderTokens` is that render. Use the fixture style of `QwenCommittedTurnRuleTests` and `QwenCommittedTurnRule(endOfTurnToken:)`.
- Check the entry into a store with memory budget 0, and wait for the spill. Check out: expect `.spilled`.
- Make an `ExecutorPromptCacheSlot(checkout, key:)`, call `restoreIfPending`, and call `plan(input:model:parameters:protocolRules: [rule], decodeTokens:)` with a next render that rewrites the model turn and adds a tool response after the commit.
- Expect `plan.decision == .splice`, `plan.reusedTokenCount` equal to the committed prefix, `plan.representedTokens` equal to the model tokens plus the fed tail, and a plan line that names `source=disk` and `rule=splice`.
- Control: the same chain with an entry whose `renderTokens` is empty gives a rule other than `splice`.

## Acceptance Criteria

- [x] A test proves `.splice` after a restore from disk, with the counts above.
- [x] The control proves that the file carries the render that the rule needs.
- [x] All five unit bundles pass.

## Tests

Test file: `Tests/MLXFoundationModelsTests/ExecutorPromptCacheRestoreTests.swift`.

Run:

```sh
swift build --build-tests
xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest
```

## Workflow

- Use `/tdd` #coverage-gap #prompt-cache