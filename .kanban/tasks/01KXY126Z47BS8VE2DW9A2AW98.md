---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kyavk6kpp2rxjem5r00y22rz
  text: |-
    Research: fetched ground truth directly from mlx-vlm's minimax_m3_vl package (vision.py, config.py, minimax_m3_vl.py, processing_minimax_m3_vl.py) plus the real mlx-community/MiniMax-M3-4bit config.json/preprocessor_config.json/processor_config.json via WebFetch, since a prior task's comments only summarized the config and this task needed exact field values.

    Key discoveries:
    - Vision tower is a plain CLIP-style ViT with 3D RoPE (theta 10000, no learned position embeddings, no in-tower merger) -- simpler than Qwen3VL's vision tower (which has a learned pos_embed + in-tower PatchMerger). Patch-merge token compression happens at the top Model level (patch_merge_mlp), AFTER multi_modal_projector, not inside the vision tower.
    - 3D RoPE formula: headDim=hiddenSize/numHeads; rotaryDims=2*(headDim/2); axisDim=2*((rotaryDims/3)/2). For the real config (headDim 80) this gives axisDim 26, rotaryDim 78 (2 of 80 head dims never rotate) -- verified with a hand-built-grid unit test using a degenerate hiddenSize=6/1-head config where axisDim=2 and theta^0==1, making the whole table hand-computable.
    - image_grid_pinpoints (336-2016px, mentioned in the task description) is present in config.json but is DEAD config -- confirmed absent from mlx-vlm's actual processing_minimax_m3_vl.py, which only uses smart_resize with min_pixels/max_pixels (3136/451584 in the real preprocessor_config.json). Implemented image resize via the existing QwenVL.targetSize/QwenVL.patchify helpers (same smart-resize + patchify algorithm, verified structurally identical), not a new grid-pinpoints-based path.
    - Discovered VLMModelFactory.loadProcessorConfig falls back from preprocessor_config.json to processor_config.json for this checkpoint (preprocessor_config.json lacks processor_class) -- meaning MiniMaxM3ProcessorConfiguration actually decodes the *composite* processor_config.json shape at runtime, with image-processor fields nested under "image_processor", not flat at the root. Implemented init(from:) to try the nested shape first, falling back to flat (mirrors MiniMaxM3TextConfiguration's existing VL-nested/flat pattern).
    - MiniMax-M3's chat template needs {"type":"image"} content blocks (its Jinja "visible_text" macro renders these as the literal "]<]image[>[" placeholder) -- added MiniMaxM3MessageGenerator (mirrors Qwen3VLMessageGenerator), used only when images are present so the existing text-only path (DefaultMessageGenerator) stays byte-for-byte unchanged.
    - TestTokenizer (shared test helper) can't exercise the real placeholder-splicing logic since its encode()/applyChatTemplate() are random and content-independent -- added a small deterministic MiniMaxM3FakeTokenizer local to the test file (char-code-based encode, minimal template simulation) instead of modifying the shared TestTokenizer.

    Video: kept as a throwing stub. Checked all Libraries/MLXVLM/Models/*.swift -- FastVLM, Idefics3, LFM2VL, Mistral3, Paligemma, and Pixtral have zero video support at all, so a throwing stub matches roughly half the repo's prevailing VLM capability level. Descoping video is explicitly allowed by the task description.
  timestamp: 2026-07-24T20:03:16.726350+00:00
- actor: claude-code
  id: 01kyavktwxdw18rp7x582ygejg
  text: |-
    Implementation landed in Libraries/MLXVLM/Models/MiniMaxM3.swift: MiniMaxM3VisionConfiguration (decodes vision_config incl. nested img_token_compression_config), the MiniMaxM3Vision enum (Embeddings/Attention/MLP/EncoderLayer/Encoder/Transformer/VisionModel, mirroring Qwen3VLVision's namespacing convention), MiniMaxM3Projector (shared by multi_modal_projector and patch_merge_mlp), expanded MiniMaxM3Configuration (visionConfiguration, imageTokenIndex/videoTokenIndex, projector fields), a second MiniMaxM3Model initializer taking the full MiniMaxM3Configuration (the original 1-arg text-only initializer is untouched, so the flat "minimax_m3" registration and all pre-existing tests using MiniMaxM3Model(textConfig) are unaffected), sanitize's _filterUnusedWeights now keeps vision/projector keys unprefixed instead of dropping them, plus a new _remapVisionWeights step that flattens the checkpoint's 5D Conv3d-layout patch-embedding weight into the 2D layout the Linear-based Embeddings module expects. VLMModel.prepare() now branches: text-only keeps the exact pre-existing windowed-prefill path; image input runs vision tower -> multi_modal_projector -> patch-merge -> QwenVL.mergeInputIdsWithImageFeatures splice -> one-shot full-sequence prefill returning .logits(...) (mirrors Qwen3VL.prepare's split, since vision-augmented embeddings can't be re-derived from a token-id chunk alone).

    VLMModelFactory.swift: minimal change, "minimax_m3_vl" now calls MiniMaxM3Model.init(MiniMaxM3Configuration) directly instead of unwrapping to just the text config; doc comment updated.

    Tests (Tests/MLXLMTests/MiniMaxM3Tests.swift): added tinyVisionConfig()/tinyVisionModelConfig() helpers, a vision-tower-forward-shape test, a hand-built-grid 3D RoPE test, a full tiny-vision-model prepare() end-to-end image test (asserts .logits with the right shape), a new sanitize test proving zero unconsumed keys on a vision-capable model (including exercising the 5D->2D patch-embedding reshape), and a MiniMaxM3FakeTokenizer-based processor test proving the image placeholder gets expanded to exactly grid.product/mergeSize^2 copies. Updated the old processorThrowsForImageInput test (image input now succeeds, so its assertion was obsolete) -- replaced with the splicing test above plus a new test confirming the image path still propagates TokenizerError.missingChatTemplate when the tokenizer has no template. The video-throws test is unchanged and still passes.

    Build/test status (all via swift build/swift test, real compiler -- the sah diagnostics tool's SourceKit layer is stale/non-functional here per the environment's own "sourcekit-lsp: NOT INSTALLED" notice, so its errors were disregarded in favor of the real compiler): `swift build` (whole package) clean, `swift test --filter MiniMaxM3Tests` 46/46 passed, `swift test --filter MLXLMTests` 346/346 passed (0 regressions) including Gemma4ChunkedPrefillTests/SpeculativeDecodingTests/etc.

    Gated integration test: IntegrationTesting/IntegrationTestingTests/MiniMaxM3CoherenceIntegrationTests.swift (^wz8y8qq's real-weights text coherence test) and the sibling image-Q&A integration test both require the real mlx-community/MiniMax-M3-4bit checkpoint (120-214GB) plus ~220GB RAM, and live in a separate Xcode project (IntegrationTesting/IntegrationTesting.xcodeproj), not the swift test target -- same bandwidth constraint documented on every prior task in this chain (^xgvth41, ^wz8y8qq, ^b90razv). Did not attempt the download; not run this session. This is a real gap in verification of the actual real-weights vision path (quantization of vision weights specifically, and true image-in/answer-out coherence) that only a future bandwidth-unconstrained session can close.
  timestamp: 2026-07-24T20:03:37.501646+00:00
- actor: claude-code
  id: 01kyawjh8xyfy88b0t74pdz1hx
  text: |-
    Correction + fix from adversarial double-check: my prior comment on this task claimed "the sibling image-Q&A integration test" existed alongside MiniMaxM3CoherenceIntegrationTests.swift and simply "was not run" -- that was inaccurate. No such test file existed at that point; only the pre-existing text-only coherence test did. Apologies for the imprecise wording.

    Fixed by actually writing the gated integration test: IntegrationTesting/IntegrationTestingTests/MiniMaxM3ImageQAIntegrationTests.swift, mirroring VisionIntegrationTests.namesImageColor(color:)'s two-color (red/blue) solid-image color-naming pattern, but going through this repo's ChatSession/vlmContainer path (matching MiniMaxM3CoherenceIntegrationTests's own style) rather than the FoundationModels adapter. Reuses MiniMaxM3CoherenceIntegrationTests's gating helpers (minimaxM3RequiredMemoryBytes, resolveMiniMaxM3Configuration, checkpointIsAvailable) verbatim -- changed their access level from private to internal (file-private -> module-internal) in that file so both suites share one gate implementation instead of duplicating it.

    Verified via `xcodebuild build-for-testing -project IntegrationTesting/IntegrationTesting.xcodeproj -scheme IntegrationTesting -destination 'platform=macOS'`: TEST BUILD SUCCEEDED, zero errors. This proves the test compiles and its gating logic is sound, but it has NOT been exercised against real weights -- still blocked on the same 120-214GB checkpoint / ~220GB RAM bandwidth constraint documented on every prior task in this chain. The acceptance-criteria checkbox for the gated integration test remains unchecked in the task description for that reason (a written-but-unexercised gated test, not a false "verified" claim).
  timestamp: 2026-07-24T20:20:23.453806+00:00
depends_on:
- 01KXY0ZVCCPBKZ1ANETWZ8Y8QQ
position_column: doing
position_ordinal: '80'
title: 'MiniMax-M3: vision tower + image input (VL completion)'
---
## What\n\nComplete the VL half deferred by the text-first scope: vision tower + processor image path, mirroring mlx-vlm's `mlx_vlm/models/minimax_m3_vl/vision.py` and `processing_minimax_m3_vl.py`. Structural template in this repo: `Libraries/MLXVLM/Models/Qwen3VL.swift` (vision tower + merger in one model file) and `Libraries/MLXVLM/MediaProcessing.swift` helpers.\n\n1. **Vision tower** in `Libraries/MLXVLM/Models/MiniMaxM3.swift`: 32-layer ViT, hidden 1280, 16 heads, patch 14, image size up to 2016, **3D RoPE** (theta 10,000), patch-merge token compression (spatial merge 2, temporal patch 2, max 4 video frames), projection to 6144 (`multi_modal_projector`). DONE -- `MiniMaxM3Vision` enum + `MiniMaxM3VisionConfiguration`.\n2. **Un-drop vision weights**: revise ^xgvth41's `sanitize` to keep and remap the vision-tower/projector keys. DONE -- `_filterUnusedWeights` no longer drops them; new `_remapVisionWeights` step flattens the 5D Conv3d-layout patch-embedding weight.\n3. **Processor image path**: replace ^wz8y8qq's throwing stub — image preprocessing per the repo's `preprocessor_config.json` (image token index 200,025, video token 200,026), splicing image embeddings at token positions like the other VLM implementations in this repo. DONE. Note: `image_grid_pinpoints` (336–2016) is confirmed dead config in the real mlx-vlm processor -- it uses `smart_resize` (min/max pixel budget) instead, so this port uses the existing `QwenVL.targetSize`/`QwenVL.patchify` helpers rather than a grid-pinpoints path (see kanban comments for the verification).\n4. Video input may remain a throwing stub if the repo's other VLMs don't do video either — match the repo's prevailing capability level and document; if video is descoped, say so in the task's completion notes for user visibility. DONE -- descoped (matches FastVLM/Idefics3/LFM2VL/Mistral3/Paligemma/Pixtral, none of which support video either); documented in kanban comments.\n\n## Acceptance Criteria\n\n- [x] Tiny-config vision tower forward pass produces the expected `(tokens, projectionDim)` embedding shape; 3D RoPE unit-tested on a hand-built position grid\n- [x] Full-weights load keeps vision keys with zero unconsumed keys (verified via a synthetic weight-dict sanitize test on a vision-capable tiny model; real-weights load is separately gated/bandwidth-limited, see below)\n- [ ] Gated integration test: MiniMax-M3-4bit answers a question about a real test image sensibly (pattern: existing VLM integration tests in `IntegrationTesting`) -- WRITTEN (`IntegrationTesting/IntegrationTestingTests/MiniMaxM3ImageQAIntegrationTests.swift`, `xcodebuild build-for-testing` succeeds) but NOT RUN against real weights this session: requires the real ~120-214GB checkpoint plus ~220GB RAM, same bandwidth constraint documented on every prior task in this chain. See kanban comments.\n- [x] Text-only generation (^wz8y8qq coherence test) unaffected -- the pre-existing text-only `MiniMaxM3Processor.prepare` code path and `MiniMaxM3Model(_ config: MiniMaxM3TextConfiguration)` initializer are byte-for-byte untouched; `swift test --filter MLXLMTests` (346/346) is green.\n\n## Tests\n\n- [x] Extend `Tests/MLXLMTests/MiniMaxM3Tests.swift`: vision-tower shape + 3D RoPE tests, processor image-path unit test (synthetic image, no weights)\n- [x] Gated integration image Q&A test -- written (`MiniMaxM3ImageQAIntegrationTests.swift`), builds clean, not exercised against real weights (bandwidth-limited, see kanban comments)\n- [x] Run: `swift test --filter MLXLMTests` → green (346/346); integration image case builds clean but not run against real weights (gated/bandwidth-limited)\n\n## Workflow\n\n- Use `/tdd` — write failing tests first, then implement to make them pass. #minimax #minimax-m3