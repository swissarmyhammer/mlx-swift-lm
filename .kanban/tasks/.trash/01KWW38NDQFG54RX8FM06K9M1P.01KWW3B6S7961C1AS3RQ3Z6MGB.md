---
assignees:
- claude-code
position_column: review
position_ordinal: '80'
title: Review of working changes (MLXLanguageModel/TranscriptConverter)
---
Scope: working tree (uncommitted changes vs HEAD)

## Review Findings (2026-07-06 10:56)

- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:476` — The `respond()` function combines model loading, vision capability gating, reasoning configuration resolution, and three separate generation paths (tool-calling, guided, plain text) in a single 700+ line function with nesting up to 4 levels and complex multi-condition guards, making it difficult to understand and modify. Extract the three generation paths into separate methods (e.g., `runToolCallingPath()`, `runGuidedGenerationPath()`, `runPlainTextPath()`), and move reasoning configuration resolution and capability gating into helper functions to reduce the primary function to a clear dispatcher.
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:729` — XGTokenizer and constraint creation block is duplicated in guided generation path (line ~880) — identical except for `kind` and `source` parameters, should be extracted to a shared function. Extract a helper that accepts the kind and source as parameters, eliminating the duplicated xgTokenizer creation and constraint setup logic between tool-calling and guided generation paths.
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:748` — Bias and reserve computation block is duplicated verbatim in guided generation path (line ~920) — differs only in the schemaJSON parameter name passed to CompletionReserve.estimate, indicating this is one function with an argument. Extract a shared helper function that computes bias and reserve values, accepting the schemaJSON/source as a parameter. Both paths should call this helper once, eliminating the 15-line duplication.
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:888` — The string literal "incompleteOutput" is repeated across three generation paths (tool-calling, guided, reasoning) as a metadata key with constant value true; extract as a named constant to avoid duplication. Extract `private static let incompleteOutputKey = "incompleteOutput"` in the Executor struct, then replace all three occurrences with this constant.
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:965` — The string literal "incompleteOutput" is repeated across three generation paths (tool-calling, guided, reasoning) as a metadata key with constant value true; extract as a named constant to avoid duplication. Extract `private static let incompleteOutputKey = "incompleteOutput"` in the Executor struct, then replace all three occurrences with this constant.
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:1074` — The string literal "incompleteOutput" is repeated across three generation paths (tool-calling, guided, reasoning) as a metadata key with constant value true; extract as a named constant to avoid duplication. Extract `private static let incompleteOutputKey = "incompleteOutput"` in the Executor struct, then replace all three occurrences with this constant.
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:1083` — The string literal "arguments" is repeated 3 times within emitToolCallingEvent to access the same JSON key; extract as a named constant. Extract `private static let argumentsKey = "arguments"` and replace all three occurrences with obj[argumentsKey].
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:1087` — The string literal "arguments" is repeated 3 times within emitToolCallingEvent to access the same JSON key; extract as a named constant. Extract `private static let argumentsKey = "arguments"` and replace all three occurrences with obj[argumentsKey].
- [ ] `Libraries/MLXFoundationModels/MLXLanguageModel.swift:1096` — The string literal "arguments" is repeated 3 times within emitToolCallingEvent to access the same JSON key; extract as a named constant. Extract `private static let argumentsKey = "arguments"` and replace all three occurrences with obj[argumentsKey].
- [ ] `Libraries/MLXFoundationModels/TranscriptConverter.swift:150` — extractToolOutputText replicates extractText instead of extending it. Both functions iterate segments via compactMap, extract text content, join results with newlines, and skip non-matching segments. The only differences—handling .structure segments and returning String vs String?—should have been parameterized into extractText rather than duplicating the core logic. Extend extractText with an optional parameter (e.g., includeStructured: Bool = false, includeNilWhenEmpty: Bool = true) to handle both cases. Replace extractToolOutputText with a call to the generalized extractText, or inline it if it remains a single-use wrapper.
#review