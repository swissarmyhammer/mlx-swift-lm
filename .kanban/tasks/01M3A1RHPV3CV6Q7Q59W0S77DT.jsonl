{"id":"01M3A1RHPVBTPGCZR56WSMA383","timestamp":"2026-09-24T15:50:18.075476Z","op":"create","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,58 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M3A1QAFD56F5TEPPADDJHENH\n+- 01M3A1QND68R1PN6K74Z6AV3EP\n+position_column: todo\n+position_ordinal: '8380'\n+title: 'Add a disk tier to ExecutorPromptCacheStore: spill on eviction, find on a memory miss, limit by bytes and age'\n+---\n+#prompt-cache\n+\n+## What\n+\n+In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` an optional disk tier. With no disk configuration, the store works as the byte-limited memory tier of task ^ddjhenh, and writes no file.\n+\n+Configuration (internal type in this task; the API task makes it public):\n+```swift\n+struct ExecutorPromptCacheDiskConfiguration: Sendable {\n+    enum Location: Sendable {\n+        /// All files in one directory. The store scans it at configure time.\n+        case directory(URL)\n+        /// The host gives one directory for each session. The store knows only\n+        /// the files it wrote or read in this process.\n+        case perSession(@Sendable (_ modelID: String, _ sessionID: String) -> URL)\n+    }\n+    var location: Location\n+    var byteLimit: Int          // total bytes of files the store knows\n+    var maximumAge: Duration?   // files older than this are deleted\n+}\n+```\n+\n+Behavior:\n+- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `directory(for: key)/fileName(for: key)`, when a disk tier is set. The write must not block the actor for its whole length: move the entry to `pendingWrites[key]`, run the write in a detached task, and on completion move the key to the disk index `diskIndex[key] = (url, byteCount, lastUse)`. A write that throws deletes the partial file and drops the entry, and logs one line.\n+- `checkOut` returns `enum ExecutorPromptCacheCheckout { case memory(ExecutorPromptCacheEntry), disk(URL), none }`. Order: memory entry → pending write (return the entry in memory as `.memory`, and cancel the effect of the write: its completion must not add the key to the disk index) → disk index → for `.perSession`, a file that exists at the resolved path (after a restart the index is empty) → `.none`. A `.disk` check-out removes the key from the disk index. The caller reads the file and deletes it after a successful read (task: executor wiring).\n+- `checkIn` of a key deletes any disk file of that key, because the entry in memory is newer.\n+- Disk limit: after each index change, delete least-recently-used files until the known total is at or below `byteLimit`, and delete files older than `maximumAge`.\n+- `evict(modelID:)` and a new `evict(sessionID:)` (all models) delete the memory entries, pending writes and files of the matching keys. For `.perSession`, `evict(sessionID:)` also deletes the file at the resolved path of each known model id.\n+- Add `func persistAll() async` — writes every memory entry to disk and waits until all writes end, for a host that shuts down and wants to restart warm. The entries stay in memory.\n+- The arrays of an entry must be evaluated before the write starts, thus the write copies data and runs no graph. Call `eval` on the arrays in `spill` before the detached write (they are normally already evaluated by generation).\n+\n+## Acceptance Criteria\n+- [ ] With a disk tier, an entry evicted by bytes is on disk, and a later `checkOut` of that key returns `.disk(url)` and not `.none`.\n+- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry, and after the write ends, no disk index entry and no file remain for that key.\n+- [ ] `checkIn` of a key removes that key's older file.\n+- [ ] The disk byte limit and the maximum age delete files, least recently used first.\n+- [ ] `.directory` scans existing files at configure time, and a new store instance (a simulated restart) returns `.disk` for a file an earlier instance wrote.\n+- [ ] `.perSession` finds a file at the resolved path after a simulated restart.\n+- [ ] `evict(sessionID:)` removes memory, pending and disk state of that session, for all models; `evict(modelID:)` for one model.\n+- [ ] `persistAll()` returns after every file exists.\n+- [ ] With no disk tier, no file is written (behavior equals ^ddjhenh).\n+\n+## Tests\n+- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheDiskTierTests.swift` with one test for each criterion above. Create a new `ExecutorPromptCacheStore()` instance for each test (not `.shared`) and a temporary directory. For the in-flight write case, inject a slow writer (an internal writer closure the tests can replace).\n+- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,58 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M3A1QAFD56F5TEPPADDJHENH\n-- 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: todo\n-position_ordinal: '8380'\n-title: 'Add a disk tier to ExecutorPromptCacheStore: spill on eviction, find on a memory miss, limit by bytes and age'\n----\n-#prompt-cache\n-\n-## What\n-\n-In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` an optional disk tier. With no disk configuration, the store works as the byte-limited memory tier of task ^ddjhenh, and writes no file.\n-\n-Configuration (internal type in this task; the API task makes it public):\n-```swift\n-struct ExecutorPromptCacheDiskConfiguration: Sendable {\n-    enum Location: Sendable {\n-        /// All files in one directory. The store scans it at configure time.\n-        case directory(URL)\n-        /// The host gives one directory for each session. The store knows only\n-        /// the files it wrote or read in this process.\n-        case perSession(@Sendable (_ modelID: String, _ sessionID: String) -> URL)\n-    }\n-    var location: Location\n-    var byteLimit: Int          // total bytes of files the store knows\n-    var maximumAge: Duration?   // files older than this are deleted\n-}\n-```\n-\n-Behavior:\n-- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `directory(for: key)/fileName(for: key)`, when a disk tier is set. The write must not block the actor for its whole length: move the entry to `pendingWrites[key]`, run the write in a detached task, and on completion move the key to the disk index `diskIndex[key] = (url, byteCount, lastUse)`. A write that throws deletes the partial file and drops the entry, and logs one line.\n-- `checkOut` returns `enum ExecutorPromptCacheCheckout { case memory(ExecutorPromptCacheEntry), disk(URL), none }`. Order: memory entry → pending write (return the entry in memory as `.memory`, and cancel the effect of the write: its completion must not add the key to the disk index) → disk index → for `.perSession`, a file that exists at the resolved path (after a restart the index is empty) → `.none`. A `.disk` check-out removes the key from the disk index. The caller reads the file and deletes it after a successful read (task: executor wiring).\n-- `checkIn` of a key deletes any disk file of that key, because the entry in memory is newer.\n-- Disk limit: after each index change, delete least-recently-used files until the known total is at or below `byteLimit`, and delete files older than `maximumAge`.\n-- `evict(modelID:)` and a new `evict(sessionID:)` (all models) delete the memory entries, pending writes and files of the matching keys. For `.perSession`, `evict(sessionID:)` also deletes the file at the resolved path of each known model id.\n-- Add `func persistAll() async` — writes every memory entry to disk and waits until all writes end, for a host that shuts down and wants to restart warm. The entries stay in memory.\n-- The arrays of an entry must be evaluated before the write starts, thus the write copies data and runs no graph. Call `eval` on the arrays in `spill` before the detached write (they are normally already evaluated by generation).\n-\n-## Acceptance Criteria\n-- [ ] With a disk tier, an entry evicted by bytes is on disk, and a later `checkOut` of that key returns `.disk(url)` and not `.none`.\n-- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry, and after the write ends, no disk index entry and no file remain for that key.\n-- [ ] `checkIn` of a key removes that key's older file.\n-- [ ] The disk byte limit and the maximum age delete files, least recently used first.\n-- [ ] `.directory` scans existing files at configure time, and a new store instance (a simulated restart) returns `.disk` for a file an earlier instance wrote.\n-- [ ] `.perSession` finds a file at the resolved path after a simulated restart.\n-- [ ] `evict(sessionID:)` removes memory, pending and disk state of that session, for all models; `evict(modelID:)` for one model.\n-- [ ] `persistAll()` returns after every file exists.\n-- [ ] With no disk tier, no file is written (behavior equals ^ddjhenh).\n-\n-## Tests\n-- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheDiskTierTests.swift` with one test for each criterion above. Create a new `ExecutorPromptCacheStore()` instance for each test (not `.shared`) and a temporary directory. For the in-flight write case, inject a slow writer (an internal writer closure the tests can replace).\n-- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3A1VFGQX2VV3RQ26G0JXAMW","timestamp":"2026-09-24T15:51:54.135845Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -6,52 +6,43 @@\n - 01M3A1QND68R1PN6K74Z6AV3EP\n position_column: todo\n position_ordinal: '8380'\n-title: 'Add a disk tier to ExecutorPromptCacheStore: spill on eviction, find on a memory miss, limit by bytes and age'\n+title: 'Add a disk spool to ExecutorPromptCacheStore: spill on eviction, hand out a spilled handle on check-out, limit the disk by bytes'\n ---\n #prompt-cache\n\n+(Plan item F3, the store side. The executor side is a separate task.)\n+\n ## What\n\n-In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` an optional disk tier. With no disk configuration, the store works as the byte-limited memory tier of task ^ddjhenh, and writes no file.\n-\n-Configuration (internal type in this task; the API task makes it public):\n-```swift\n-struct ExecutorPromptCacheDiskConfiguration: Sendable {\n-    enum Location: Sendable {\n-        /// All files in one directory. The store scans it at configure time.\n-        case directory(URL)\n-        /// The host gives one directory for each session. The store knows only\n-        /// the files it wrote or read in this process.\n-        case perSession(@Sendable (_ modelID: String, _ sessionID: String) -> URL)\n-    }\n-    var location: Location\n-    var byteLimit: Int          // total bytes of files the store knows\n-    var maximumAge: Duration?   // files older than this are deleted\n-}\n-```\n+In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` a disk spool. An entry has three states: in memory, spilling, and on disk.\n\n-Behavior:\n-- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `directory(for: key)/fileName(for: key)`, when a disk tier is set. The write must not block the actor for its whole length: move the entry to `pendingWrites[key]`, run the write in a detached task, and on completion move the key to the disk index `diskIndex[key] = (url, byteCount, lastUse)`. A write that throws deletes the partial file and drops the entry, and logs one line.\n-- `checkOut` returns `enum ExecutorPromptCacheCheckout { case memory(ExecutorPromptCacheEntry), disk(URL), none }`. Order: memory entry → pending write (return the entry in memory as `.memory`, and cancel the effect of the write: its completion must not add the key to the disk index) → disk index → for `.perSession`, a file that exists at the resolved path (after a restart the index is empty) → `.none`. A `.disk` check-out removes the key from the disk index. The caller reads the file and deletes it after a successful read (task: executor wiring).\n-- `checkIn` of a key deletes any disk file of that key, because the entry in memory is newer.\n-- Disk limit: after each index change, delete least-recently-used files until the known total is at or below `byteLimit`, and delete files older than `maximumAge`.\n-- `evict(modelID:)` and a new `evict(sessionID:)` (all models) delete the memory entries, pending writes and files of the matching keys. For `.perSession`, `evict(sessionID:)` also deletes the file at the resolved path of each known model id.\n-- Add `func persistAll() async` — writes every memory entry to disk and waits until all writes end, for a host that shuts down and wants to restart warm. The entries stay in memory.\n-- The arrays of an entry must be evaluated before the write starts, thus the write copies data and runs no graph. Call `eval` on the arrays in `spill` before the detached write (they are normally already evaluated by generation).\n+- Folder: one folder for each process, `FileManager.default.temporaryDirectory/mlx-prompt-cache/<pid>-<process start UUID>/`. At the first use, the store deletes the folders of processes that no longer run (the pid in the folder name is not alive, checked with `kill(pid, 0)`). An internal `init(directory:)` lets tests use their own folder.\n+- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `folder/fileName(for: key)`. First evaluate the entry's arrays (normally they are already evaluated by generation), then move the entry to `spilling[key]`, and write in a detached task, thus the actor stays free for other check-outs. On completion, the key moves to `onDisk[key] = (url, byteCount, lastUse, ledger length)`. A write that throws deletes the partial file, drops the entry, and logs one line.\n+- `checkOut` returns\n+  ```swift\n+  enum ExecutorPromptCacheCheckout {\n+      case memory(ExecutorPromptCacheEntry)\n+      case spilled(ExecutorPromptCacheSpilledHandle)   // url + key\n+      case none\n+  }\n+  ```\n+  Order: memory → spilling (take the memory copy back as `.memory`; the write's completion must then add nothing to `onDisk` and delete its file) → on disk (`.spilled`, and remove the key from `onDisk`; the executor reads and deletes the file) → `.none`.\n+- `checkIn` of a key deletes any file of that key: the entry in memory is newer.\n+- Disk budget: `diskBudgetBytes` (internal setter here; public in the API task). After each change of `onDisk`, delete least-recently-used files until the total is at or below the budget. Default: 25% of the free space of the volume of the folder at the first use.\n+- `evict(modelID:)` and a new `evict(sessionID:)` (for one session, all models) remove the matching memory entries, spilling entries, and files. `evict(modelID: nil)` removes all.\n+- `save(arrays:)` holds MLX's process-wide `evalLock` for the whole write (`.build/checkouts/mlx-swift/Source/MLX/IO.swift:61-77`). Every other pass that evaluates waits. Record the write duration in the log line of each spill, thus the benchmark task can read it.\n\n ## Acceptance Criteria\n-- [ ] With a disk tier, an entry evicted by bytes is on disk, and a later `checkOut` of that key returns `.disk(url)` and not `.none`.\n-- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry, and after the write ends, no disk index entry and no file remain for that key.\n+- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` of that key returns `.spilled` and not `.none`.\n+- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n+- [ ] Other check-outs and check-ins do not wait for a running write (the actor is free).\n - [ ] `checkIn` of a key removes that key's older file.\n-- [ ] The disk byte limit and the maximum age delete files, least recently used first.\n-- [ ] `.directory` scans existing files at configure time, and a new store instance (a simulated restart) returns `.disk` for a file an earlier instance wrote.\n-- [ ] `.perSession` finds a file at the resolved path after a simulated restart.\n-- [ ] `evict(sessionID:)` removes memory, pending and disk state of that session, for all models; `evict(modelID:)` for one model.\n-- [ ] `persistAll()` returns after every file exists.\n-- [ ] With no disk tier, no file is written (behavior equals ^ddjhenh).\n+- [ ] The disk budget deletes files, least recently used first.\n+- [ ] At first use, the folder of a process that no longer runs is deleted, and the folder of a live process is kept.\n+- [ ] `evict(sessionID:)` removes memory, spilling and disk state of that session for all models; `evict(modelID:)` for one model.\n\n ## Tests\n-- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheDiskTierTests.swift` with one test for each criterion above. Create a new `ExecutorPromptCacheStore()` instance for each test (not `.shared`) and a temporary directory. For the in-flight write case, inject a slow writer (an internal writer closure the tests can replace).\n+- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion. A new `ExecutorPromptCacheStore(directory:)` for each test, in a temporary folder. For the in-flight write cases, inject a slow writer (an internal writer closure that tests can replace).\n - [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,43 +6,52 @@\n - 01M3A1QND68R1PN6K74Z6AV3EP\n position_column: todo\n position_ordinal: '8380'\n-title: 'Add a disk spool to ExecutorPromptCacheStore: spill on eviction, hand out a spilled handle on check-out, limit the disk by bytes'\n+title: 'Add a disk tier to ExecutorPromptCacheStore: spill on eviction, find on a memory miss, limit by bytes and age'\n ---\n #prompt-cache\n\n-(Plan item F3, the store side. The executor side is a separate task.)\n-\n ## What\n\n-In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` a disk spool. An entry has three states: in memory, spilling, and on disk.\n+In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` an optional disk tier. With no disk configuration, the store works as the byte-limited memory tier of task ^ddjhenh, and writes no file.\n\n-- Folder: one folder for each process, `FileManager.default.temporaryDirectory/mlx-prompt-cache/<pid>-<process start UUID>/`. At the first use, the store deletes the folders of processes that no longer run (the pid in the folder name is not alive, checked with `kill(pid, 0)`). An internal `init(directory:)` lets tests use their own folder.\n-- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `folder/fileName(for: key)`. First evaluate the entry's arrays (normally they are already evaluated by generation), then move the entry to `spilling[key]`, and write in a detached task, thus the actor stays free for other check-outs. On completion, the key moves to `onDisk[key] = (url, byteCount, lastUse, ledger length)`. A write that throws deletes the partial file, drops the entry, and logs one line.\n-- `checkOut` returns\n-  ```swift\n-  enum ExecutorPromptCacheCheckout {\n-      case memory(ExecutorPromptCacheEntry)\n-      case spilled(ExecutorPromptCacheSpilledHandle)   // url + key\n-      case none\n-  }\n-  ```\n-  Order: memory → spilling (take the memory copy back as `.memory`; the write's completion must then add nothing to `onDisk` and delete its file) → on disk (`.spilled`, and remove the key from `onDisk`; the executor reads and deletes the file) → `.none`.\n-- `checkIn` of a key deletes any file of that key: the entry in memory is newer.\n-- Disk budget: `diskBudgetBytes` (internal setter here; public in the API task). After each change of `onDisk`, delete least-recently-used files until the total is at or below the budget. Default: 25% of the free space of the volume of the folder at the first use.\n-- `evict(modelID:)` and a new `evict(sessionID:)` (for one session, all models) remove the matching memory entries, spilling entries, and files. `evict(modelID: nil)` removes all.\n-- `save(arrays:)` holds MLX's process-wide `evalLock` for the whole write (`.build/checkouts/mlx-swift/Source/MLX/IO.swift:61-77`). Every other pass that evaluates waits. Record the write duration in the log line of each spill, thus the benchmark task can read it.\n+Configuration (internal type in this task; the API task makes it public):\n+```swift\n+struct ExecutorPromptCacheDiskConfiguration: Sendable {\n+    enum Location: Sendable {\n+        /// All files in one directory. The store scans it at configure time.\n+        case directory(URL)\n+        /// The host gives one directory for each session. The store knows only\n+        /// the files it wrote or read in this process.\n+        case perSession(@Sendable (_ modelID: String, _ sessionID: String) -> URL)\n+    }\n+    var location: Location\n+    var byteLimit: Int          // total bytes of files the store knows\n+    var maximumAge: Duration?   // files older than this are deleted\n+}\n+```\n\n+Behavior:\n+- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `directory(for: key)/fileName(for: key)`, when a disk tier is set. The write must not block the actor for its whole length: move the entry to `pendingWrites[key]`, run the write in a detached task, and on completion move the key to the disk index `diskIndex[key] = (url, byteCount, lastUse)`. A write that throws deletes the partial file and drops the entry, and logs one line.\n+- `checkOut` returns `enum ExecutorPromptCacheCheckout { case memory(ExecutorPromptCacheEntry), disk(URL), none }`. Order: memory entry → pending write (return the entry in memory as `.memory`, and cancel the effect of the write: its completion must not add the key to the disk index) → disk index → for `.perSession`, a file that exists at the resolved path (after a restart the index is empty) → `.none`. A `.disk` check-out removes the key from the disk index. The caller reads the file and deletes it after a successful read (task: executor wiring).\n+- `checkIn` of a key deletes any disk file of that key, because the entry in memory is newer.\n+- Disk limit: after each index change, delete least-recently-used files until the known total is at or below `byteLimit`, and delete files older than `maximumAge`.\n+- `evict(modelID:)` and a new `evict(sessionID:)` (all models) delete the memory entries, pending writes and files of the matching keys. For `.perSession`, `evict(sessionID:)` also deletes the file at the resolved path of each known model id.\n+- Add `func persistAll() async` — writes every memory entry to disk and waits until all writes end, for a host that shuts down and wants to restart warm. The entries stay in memory.\n+- The arrays of an entry must be evaluated before the write starts, thus the write copies data and runs no graph. Call `eval` on the arrays in `spill` before the detached write (they are normally already evaluated by generation).\n+\n ## Acceptance Criteria\n-- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` of that key returns `.spilled` and not `.none`.\n-- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n-- [ ] Other check-outs and check-ins do not wait for a running write (the actor is free).\n+- [ ] With a disk tier, an entry evicted by bytes is on disk, and a later `checkOut` of that key returns `.disk(url)` and not `.none`.\n+- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry, and after the write ends, no disk index entry and no file remain for that key.\n - [ ] `checkIn` of a key removes that key's older file.\n-- [ ] The disk budget deletes files, least recently used first.\n-- [ ] At first use, the folder of a process that no longer runs is deleted, and the folder of a live process is kept.\n-- [ ] `evict(sessionID:)` removes memory, spilling and disk state of that session for all models; `evict(modelID:)` for one model.\n+- [ ] The disk byte limit and the maximum age delete files, least recently used first.\n+- [ ] `.directory` scans existing files at configure time, and a new store instance (a simulated restart) returns `.disk` for a file an earlier instance wrote.\n+- [ ] `.perSession` finds a file at the resolved path after a simulated restart.\n+- [ ] `evict(sessionID:)` removes memory, pending and disk state of that session, for all models; `evict(modelID:)` for one model.\n+- [ ] `persistAll()` returns after every file exists.\n+- [ ] With no disk tier, no file is written (behavior equals ^ddjhenh).\n\n ## Tests\n-- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion. A new `ExecutorPromptCacheStore(directory:)` for each test, in a temporary folder. For the in-flight write cases, inject a slow writer (an internal writer closure that tests can replace).\n+- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheDiskTierTests.swift` with one test for each criterion above. Create a new `ExecutorPromptCacheStore()` instance for each test (not `.shared`) and a temporary directory. For the in-flight write case, inject a slow writer (an internal writer closure the tests can replace).\n - [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n","transaction_id":null}
{"id":"01M3A2AYY3ZTRP7V8R2906A1Y4","timestamp":"2026-09-24T16:00:21.443790Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -6,18 +6,21 @@\n - 01M3A1QND68R1PN6K74Z6AV3EP\n position_column: todo\n position_ordinal: '8380'\n-title: 'Add a disk spool to ExecutorPromptCacheStore: spill on eviction, hand out a spilled handle on check-out, limit the disk by bytes'\n+title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n #prompt-cache\n\n-(Plan item F3, the store side. The executor side is a separate task.)\n+(Plan item F3, store side, part 1. Part 2 — disk budget, folder clean-up, per-session removal — is a separate task.)\n\n ## What\n\n-In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` a disk spool. An entry has three states: in memory, spilling, and on disk.\n+In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` a spool. An entry has three states: in memory, spilling, on disk.\n\n-- Folder: one folder for each process, `FileManager.default.temporaryDirectory/mlx-prompt-cache/<pid>-<process start UUID>/`. At the first use, the store deletes the folders of processes that no longer run (the pid in the folder name is not alive, checked with `kill(pid, 0)`). An internal `init(directory:)` lets tests use their own folder.\n-- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `folder/fileName(for: key)`. First evaluate the entry's arrays (normally they are already evaluated by generation), then move the entry to `spilling[key]`, and write in a detached task, thus the actor stays free for other check-outs. On completion, the key moves to `onDisk[key] = (url, byteCount, lastUse, ledger length)`. A write that throws deletes the partial file, drops the entry, and logs one line.\n+- Folder: `FileManager.default.temporaryDirectory/mlx-prompt-cache/<pid>-<process start UUID>/`. An internal `init(directory:)` lets tests use their own folder.\n+- `spill(_:_:)` (the one eviction path from ^ddjhenh), ON THE ACTOR and synchronously: call `ExecutorPromptCacheFile.prepare(entry, key:)` (task ^z6av3ep). This copies the array handles and metadata, thus the writer holds no reference to the caches. The race it prevents: a check-out during a spill gives the same entry back to a turn, and `KVCacheSimple.update` changes its arrays in place (`self.keys?[...] = keys`), while a writer that reads `cache.state` would race on those Swift properties.\n+- Give each spill a new generation number (`UInt64`, from a counter on the actor) and write to `fileName(for: key, generation:)`. A completion may change `onDisk` or delete a file ONLY if its generation is still the current generation of that key. This prevents the case spill A → check-out → check-in → spill B, where A's completion would delete B's file.\n+- ONE serial writer (for example an `AsyncStream` of jobs consumed by one task): at most one write runs at a time, because `save(arrays:)` holds MLX's process-wide `evalLock` for the whole write (`.build/checkouts/mlx-swift/Source/MLX/IO.swift:61-77`). The writer, not the actor, evaluates the arrays (`eval` waits on `evalLock`). Log the write duration in each spill's log line.\n+- `spilling[key]` keeps the entry, its prepared input, its generation, and its bytes. Spilling bytes count in a separate total `spillingByteCount` (they are still resident until the write ends).\n - `checkOut` returns\n   ```swift\n   enum ExecutorPromptCacheCheckout {\n@@ -26,23 +29,22 @@\n       case none\n   }\n   ```\n-  Order: memory → spilling (take the memory copy back as `.memory`; the write's completion must then add nothing to `onDisk` and delete its file) → on disk (`.spilled`, and remove the key from `onDisk`; the executor reads and deletes the file) → `.none`.\n+  Order: memory → spilling (take the entry back as `.memory`; the write's completion then adds nothing to `onDisk` and deletes its own file) → on disk (`.spilled`, and remove the key from `onDisk`) → `.none`.\n - `checkIn` of a key deletes any file of that key: the entry in memory is newer.\n-- Disk budget: `diskBudgetBytes` (internal setter here; public in the API task). After each change of `onDisk`, delete least-recently-used files until the total is at or below the budget. Default: 25% of the free space of the volume of the folder at the first use.\n-- `evict(modelID:)` and a new `evict(sessionID:)` (for one session, all models) remove the matching memory entries, spilling entries, and files. `evict(modelID: nil)` removes all.\n-- `save(arrays:)` holds MLX's process-wide `evalLock` for the whole write (`.build/checkouts/mlx-swift/Source/MLX/IO.swift:61-77`). Every other pass that evaluates waits. Record the write duration in the log line of each spill, thus the benchmark task can read it.\n+- Update every caller of `checkOut`: the executor in `Libraries/MLXFoundationModels/MLXLanguageModel.swift` (`carriedPromptCache = await ...checkOut(...)`, about `:976`) and the call sites in `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift`. Until task ^jar6qq9 lands, the executor treats `.spilled` as a cold start and deletes the file.\n\n ## Acceptance Criteria\n-- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` of that key returns `.spilled` and not `.none`.\n-- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n-- [ ] Other check-outs and check-ins do not wait for a running write (the actor is free).\n+- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` returns `.spilled`.\n+- [ ] A check-out during a spill of the same key returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n+- [ ] A check-out during a spill, followed by a cache `update` on the returned entry, does not change the bytes of the file that the writer writes.\n+- [ ] Spill A (slow writer), check-out, check-in, spill B: after both writes end, B's file exists and is the only file for the key.\n+- [ ] At most one write runs at a time (assert with an injected writer that counts concurrent calls).\n+- [ ] Other check-outs and check-ins complete while a write runs (the actor is free).\n - [ ] `checkIn` of a key removes that key's older file.\n-- [ ] The disk budget deletes files, least recently used first.\n-- [ ] At first use, the folder of a process that no longer runs is deleted, and the folder of a live process is kept.\n-- [ ] `evict(sessionID:)` removes memory, spilling and disk state of that session for all models; `evict(modelID:)` for one model.\n+- [ ] `swift build --build-tests` compiles with no new warnings.\n\n ## Tests\n-- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion. A new `ExecutorPromptCacheStore(directory:)` for each test, in a temporary folder. For the in-flight write cases, inject a slow writer (an internal writer closure that tests can replace).\n+- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion, each with its own `ExecutorPromptCacheStore(directory:)` in a temporary folder. For in-flight cases, inject a slow writer (an internal writer closure that tests replace).\n - [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -6,21 +6,18 @@\n - 01M3A1QND68R1PN6K74Z6AV3EP\n position_column: todo\n position_ordinal: '8380'\n-title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n+title: 'Add a disk spool to ExecutorPromptCacheStore: spill on eviction, hand out a spilled handle on check-out, limit the disk by bytes'\n ---\n #prompt-cache\n\n-(Plan item F3, store side, part 1. Part 2 — disk budget, folder clean-up, per-session removal — is a separate task.)\n+(Plan item F3, the store side. The executor side is a separate task.)\n\n ## What\n\n-In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` a spool. An entry has three states: in memory, spilling, on disk.\n+In `Libraries/MLXFoundationModels/ExecutorPromptCache.swift`, give `ExecutorPromptCacheStore` a disk spool. An entry has three states: in memory, spilling, and on disk.\n\n-- Folder: `FileManager.default.temporaryDirectory/mlx-prompt-cache/<pid>-<process start UUID>/`. An internal `init(directory:)` lets tests use their own folder.\n-- `spill(_:_:)` (the one eviction path from ^ddjhenh), ON THE ACTOR and synchronously: call `ExecutorPromptCacheFile.prepare(entry, key:)` (task ^z6av3ep). This copies the array handles and metadata, thus the writer holds no reference to the caches. The race it prevents: a check-out during a spill gives the same entry back to a turn, and `KVCacheSimple.update` changes its arrays in place (`self.keys?[...] = keys`), while a writer that reads `cache.state` would race on those Swift properties.\n-- Give each spill a new generation number (`UInt64`, from a counter on the actor) and write to `fileName(for: key, generation:)`. A completion may change `onDisk` or delete a file ONLY if its generation is still the current generation of that key. This prevents the case spill A → check-out → check-in → spill B, where A's completion would delete B's file.\n-- ONE serial writer (for example an `AsyncStream` of jobs consumed by one task): at most one write runs at a time, because `save(arrays:)` holds MLX's process-wide `evalLock` for the whole write (`.build/checkouts/mlx-swift/Source/MLX/IO.swift:61-77`). The writer, not the actor, evaluates the arrays (`eval` waits on `evalLock`). Log the write duration in each spill's log line.\n-- `spilling[key]` keeps the entry, its prepared input, its generation, and its bytes. Spilling bytes count in a separate total `spillingByteCount` (they are still resident until the write ends).\n+- Folder: one folder for each process, `FileManager.default.temporaryDirectory/mlx-prompt-cache/<pid>-<process start UUID>/`. At the first use, the store deletes the folders of processes that no longer run (the pid in the folder name is not alive, checked with `kill(pid, 0)`). An internal `init(directory:)` lets tests use their own folder.\n+- `spill(_:_:)` (the one eviction path from ^ddjhenh) writes the entry with `ExecutorPromptCacheFile.write` (task ^z6av3ep) to `folder/fileName(for: key)`. First evaluate the entry's arrays (normally they are already evaluated by generation), then move the entry to `spilling[key]`, and write in a detached task, thus the actor stays free for other check-outs. On completion, the key moves to `onDisk[key] = (url, byteCount, lastUse, ledger length)`. A write that throws deletes the partial file, drops the entry, and logs one line.\n - `checkOut` returns\n   ```swift\n   enum ExecutorPromptCacheCheckout {\n@@ -29,22 +26,23 @@\n       case none\n   }\n   ```\n-  Order: memory → spilling (take the entry back as `.memory`; the write's completion then adds nothing to `onDisk` and deletes its own file) → on disk (`.spilled`, and remove the key from `onDisk`) → `.none`.\n+  Order: memory → spilling (take the memory copy back as `.memory`; the write's completion must then add nothing to `onDisk` and delete its file) → on disk (`.spilled`, and remove the key from `onDisk`; the executor reads and deletes the file) → `.none`.\n - `checkIn` of a key deletes any file of that key: the entry in memory is newer.\n-- Update every caller of `checkOut`: the executor in `Libraries/MLXFoundationModels/MLXLanguageModel.swift` (`carriedPromptCache = await ...checkOut(...)`, about `:976`) and the call sites in `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift`. Until task ^jar6qq9 lands, the executor treats `.spilled` as a cold start and deletes the file.\n+- Disk budget: `diskBudgetBytes` (internal setter here; public in the API task). After each change of `onDisk`, delete least-recently-used files until the total is at or below the budget. Default: 25% of the free space of the volume of the folder at the first use.\n+- `evict(modelID:)` and a new `evict(sessionID:)` (for one session, all models) remove the matching memory entries, spilling entries, and files. `evict(modelID: nil)` removes all.\n+- `save(arrays:)` holds MLX's process-wide `evalLock` for the whole write (`.build/checkouts/mlx-swift/Source/MLX/IO.swift:61-77`). Every other pass that evaluates waits. Record the write duration in the log line of each spill, thus the benchmark task can read it.\n\n ## Acceptance Criteria\n-- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` returns `.spilled`.\n-- [ ] A check-out during a spill of the same key returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n-- [ ] A check-out during a spill, followed by a cache `update` on the returned entry, does not change the bytes of the file that the writer writes.\n-- [ ] Spill A (slow writer), check-out, check-in, spill B: after both writes end, B's file exists and is the only file for the key.\n-- [ ] At most one write runs at a time (assert with an injected writer that counts concurrent calls).\n-- [ ] Other check-outs and check-ins complete while a write runs (the actor is free).\n+- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` of that key returns `.spilled` and not `.none`.\n+- [ ] A `checkOut` while the write of the same key still runs returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n+- [ ] Other check-outs and check-ins do not wait for a running write (the actor is free).\n - [ ] `checkIn` of a key removes that key's older file.\n-- [ ] `swift build --build-tests` compiles with no new warnings.\n+- [ ] The disk budget deletes files, least recently used first.\n+- [ ] At first use, the folder of a process that no longer runs is deleted, and the folder of a live process is kept.\n+- [ ] `evict(sessionID:)` removes memory, spilling and disk state of that session for all models; `evict(modelID:)` for one model.\n\n ## Tests\n-- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion, each with its own `ExecutorPromptCacheStore(directory:)` in a temporary folder. For in-flight cases, inject a slow writer (an internal writer closure that tests replace).\n+- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion. A new `ExecutorPromptCacheStore(directory:)` for each test, in a temporary folder. For the in-flight write cases, inject a slow writer (an internal writer closure that tests can replace).\n - [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n","transaction_id":null}
{"id":"01M3ABXJQ9SP6Y4PY1ZS171CDQ","timestamp":"2026-09-24T18:47:48.713022Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: todo\n-position_ordinal: '8380'\n+position_column: doing\n+position_ordinal: '80'\n title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n #prompt-cache\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8380'\n title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n #prompt-cache\n","transaction_id":null}
{"id":"01M3AC1NN2CDVNVMDJKWDY19ES","timestamp":"2026-09-24T18:50:02.786920Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,19 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m3ac1nn2hgxahs5jkw5acpmw\n+  text: |-\n+    Research before the change:\n+    - `ExecutorPromptCacheStore.spill(_:_:)` only logs `evictionLine` now. `checkIn` calls it for an entry larger than the budget, and `evictToBudget` calls it for each LRU entry. `evict(modelID:)` does not use it.\n+    - `ExecutorPromptCacheFile.prepare` makes new array handles (`$0[.ellipsis]`) and does not evaluate. `ExecutorPromptCacheFile.write` calls `writePromptCache` -> `save(arrays:)`, which evaluates the arrays under `evalLock`. Thus the writer task evaluates, not the actor.\n+    - `write` does not make the folder. The spool must make the folder before the first write.\n+    - Package is Swift 6 language mode (tools 6.2).\n+    - Existing store tests use `ExecutorPromptCacheStore()`. With a spool, all of those stores share one process folder, and file names are `<digest>-<generation>`. Two stores with a per-store generation counter can write the same file name for the same key. Thus the existing test file must give each store its own folder through `init(directory:)`.\n+    - The test \"the least recently used session loses its cache past the byte budget\" expects `checkOut == nil` for the evicted key. After this change the evicted key is spilling or on disk, thus that test must read `peek` (memory only).\n+    - A stale write completion (spill A, then check-out, check-in, spill B) must delete ITS OWN file (A's generation name), else B is not the only file. It never changes `onDisk` and never deletes B's file.\n+  timestamp: 2026-09-24T18:50:02.786385+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","reverse_patch":"--- original\n+++ modified\n@@ -1,19 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m3ac1nn2hgxahs5jkw5acpmw\n-  text: |-\n-    Research before the change:\n-    - `ExecutorPromptCacheStore.spill(_:_:)` only logs `evictionLine` now. `checkIn` calls it for an entry larger than the budget, and `evictToBudget` calls it for each LRU entry. `evict(modelID:)` does not use it.\n-    - `ExecutorPromptCacheFile.prepare` makes new array handles (`$0[.ellipsis]`) and does not evaluate. `ExecutorPromptCacheFile.write` calls `writePromptCache` -> `save(arrays:)`, which evaluates the arrays under `evalLock`. Thus the writer task evaluates, not the actor.\n-    - `write` does not make the folder. The spool must make the folder before the first write.\n-    - Package is Swift 6 language mode (tools 6.2).\n-    - Existing store tests use `ExecutorPromptCacheStore()`. With a spool, all of those stores share one process folder, and file names are `<digest>-<generation>`. Two stores with a per-store generation counter can write the same file name for the same key. Thus the existing test file must give each store its own folder through `init(directory:)`.\n-    - The test \"the least recently used session loses its cache past the byte budget\" expects `checkOut == nil` for the evicted key. After this change the evicted key is spilling or on disk, thus that test must read `peek` (memory only).\n-    - A stale write completion (spill A, then check-out, check-in, spill B) must delete ITS OWN file (A's generation name), else B is not the only file. It never changes `onDisk` and never deletes B's file.\n-  timestamp: 2026-09-24T18:50:02.786385+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","transaction_id":null}
{"id":"01M3ACGP3FAKSARNWFRK2HBSA6","timestamp":"2026-09-24T18:58:14.767758Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -14,6 +14,15 @@\n     - The test \"the least recently used session loses its cache past the byte budget\" expects `checkOut == nil` for the evicted key. After this change the evicted key is spilling or on disk, thus that test must read `peek` (memory only).\n     - A stale write completion (spill A, then check-out, check-in, spill B) must delete ITS OWN file (A's generation name), else B is not the only file. It never changes `onDisk` and never deletes B's file.\n   timestamp: 2026-09-24T18:50:02.786385+00:00\n+- actor: claude-code\n+  id: 01m3acgp3epa1gk3erexpw8wr8\n+  text: |-\n+    ### implement — changed\n+    - evidence: 6 files — Libraries/MLXFoundationModels/ExecutorPromptCache.swift (spool: `ExecutorPromptCacheCheckout`, `ExecutorPromptCacheSpilledHandle`, `ExecutorPromptCacheSpoolWriter` with one AsyncStream consumer task, `spilling`/`onDisk`/`spillingByteCount`, generation numbers, `init(directory:writer:)`, `waitForSpills()`, `spillLine`), Libraries/MLXFoundationModels/ExecutorPromptCacheFile.swift (`removePartialFile` becomes internal `removeFile(at:)`), Libraries/MLXFoundationModels/MLXLanguageModel.swift (`checkOutPromptCache`: `.spilled` starts cold and deletes the file), Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift (call sites; each store has its own folder and a writer that writes nothing; executor test for `.spilled`), Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift (new, one test for each criterion).\n+    - Notes: the folder is made by the default writer `ExecutorPromptCacheStore.writeSpillFile`, not by the consumer, thus a test writer that writes nothing makes no folder. A stale write (its generation is not the current generation of its key) deletes only its own file and changes no record.\n+    - `swift build --build-tests`: only the known `missing creator for mutated node` warning. MLXFoundationModelsTests: 267 Swift Testing tests in 31 suites pass, 6 runs in a row.\n+    - next: /test\n+  timestamp: 2026-09-24T18:58:14.766609+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","reverse_patch":"--- original\n+++ modified\n@@ -14,15 +14,6 @@\n     - The test \"the least recently used session loses its cache past the byte budget\" expects `checkOut == nil` for the evicted key. After this change the evicted key is spilling or on disk, thus that test must read `peek` (memory only).\n     - A stale write completion (spill A, then check-out, check-in, spill B) must delete ITS OWN file (A's generation name), else B is not the only file. It never changes `onDisk` and never deletes B's file.\n   timestamp: 2026-09-24T18:50:02.786385+00:00\n-- actor: claude-code\n-  id: 01m3acgp3epa1gk3erexpw8wr8\n-  text: |-\n-    ### implement — changed\n-    - evidence: 6 files — Libraries/MLXFoundationModels/ExecutorPromptCache.swift (spool: `ExecutorPromptCacheCheckout`, `ExecutorPromptCacheSpilledHandle`, `ExecutorPromptCacheSpoolWriter` with one AsyncStream consumer task, `spilling`/`onDisk`/`spillingByteCount`, generation numbers, `init(directory:writer:)`, `waitForSpills()`, `spillLine`), Libraries/MLXFoundationModels/ExecutorPromptCacheFile.swift (`removePartialFile` becomes internal `removeFile(at:)`), Libraries/MLXFoundationModels/MLXLanguageModel.swift (`checkOutPromptCache`: `.spilled` starts cold and deletes the file), Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift (call sites; each store has its own folder and a writer that writes nothing; executor test for `.spilled`), Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift (new, one test for each criterion).\n-    - Notes: the folder is made by the default writer `ExecutorPromptCacheStore.writeSpillFile`, not by the consumer, thus a test writer that writes nothing makes no folder. A stale write (its generation is not the current generation of its key) deletes only its own file and changes no record.\n-    - `swift build --build-tests`: only the known `missing creator for mutated node` warning. MLXFoundationModelsTests: 267 Swift Testing tests in 31 suites pass, 6 runs in a row.\n-    - next: /test\n-  timestamp: 2026-09-24T18:58:14.766609+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","transaction_id":null}
{"id":"01M3AD8XX3RV0DK1YRM8E9MT85","timestamp":"2026-09-24T19:11:29.187155Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -23,6 +23,13 @@\n     - `swift build --build-tests`: only the known `missing creator for mutated node` warning. MLXFoundationModelsTests: 267 Swift Testing tests in 31 suites pass, 6 runs in a row.\n     - next: /test\n   timestamp: 2026-09-24T18:58:14.766609+00:00\n+- actor: claude-code\n+  id: 01m3ad8xx1bfar2zg86gjwz8yz\n+  text: |-\n+    ### test — green (baseline only)\n+    - evidence: `swift build --build-tests` — only the known `missing creator for mutated node` warning. `xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — 267 tests in 31 suites pass (7 runs, no flaky test). `xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — 694 XCTest with 36 failures (GlmOcr/Qwen25VL/Qwen35/Qwen3VL ContinuationTests + NanbeigeTests) and 1207 Swift Testing tests with 101 issues: equal to the 2026-09-24 baseline. The change touches only MLXFoundationModels, thus the other three bundles do not change.\n+    - next: /commit\n+  timestamp: 2026-09-24T19:11:29.185268+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","reverse_patch":"--- original\n+++ modified\n@@ -23,13 +23,6 @@\n     - `swift build --build-tests`: only the known `missing creator for mutated node` warning. MLXFoundationModelsTests: 267 Swift Testing tests in 31 suites pass, 6 runs in a row.\n     - next: /test\n   timestamp: 2026-09-24T18:58:14.766609+00:00\n-- actor: claude-code\n-  id: 01m3ad8xx1bfar2zg86gjwz8yz\n-  text: |-\n-    ### test — green (baseline only)\n-    - evidence: `swift build --build-tests` — only the known `missing creator for mutated node` warning. `xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — 267 tests in 31 suites pass (7 runs, no flaky test). `xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — 694 XCTest with 36 failures (GlmOcr/Qwen25VL/Qwen35/Qwen3VL ContinuationTests + NanbeigeTests) and 1207 Swift Testing tests with 101 issues: equal to the 2026-09-24 baseline. The change touches only MLXFoundationModels, thus the other three bundles do not change.\n-    - next: /commit\n-  timestamp: 2026-09-24T19:11:29.185268+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","transaction_id":null}
{"id":"01M3ADAGEZ3F8BZ39PQ06967HW","timestamp":"2026-09-24T19:12:20.959047Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -63,18 +63,18 @@\n - Update every caller of `checkOut`: the executor in `Libraries/MLXFoundationModels/MLXLanguageModel.swift` (`carriedPromptCache = await ...checkOut(...)`, about `:976`) and the call sites in `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift`. Until task ^jar6qq9 lands, the executor treats `.spilled` as a cold start and deletes the file.\n\n ## Acceptance Criteria\n-- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` returns `.spilled`.\n-- [ ] A check-out during a spill of the same key returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n-- [ ] A check-out during a spill, followed by a cache `update` on the returned entry, does not change the bytes of the file that the writer writes.\n-- [ ] Spill A (slow writer), check-out, check-in, spill B: after both writes end, B's file exists and is the only file for the key.\n-- [ ] At most one write runs at a time (assert with an injected writer that counts concurrent calls).\n-- [ ] Other check-outs and check-ins complete while a write runs (the actor is free).\n-- [ ] `checkIn` of a key removes that key's older file.\n-- [ ] `swift build --build-tests` compiles with no new warnings.\n+- [x] An entry evicted by the byte budget is on disk, and a later `checkOut` returns `.spilled`.\n+- [x] A check-out during a spill of the same key returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n+- [x] A check-out during a spill, followed by a cache `update` on the returned entry, does not change the bytes of the file that the writer writes.\n+- [x] Spill A (slow writer), check-out, check-in, spill B: after both writes end, B's file exists and is the only file for the key.\n+- [x] At most one write runs at a time (assert with an injected writer that counts concurrent calls).\n+- [x] Other check-outs and check-ins complete while a write runs (the actor is free).\n+- [x] `checkIn` of a key removes that key's older file.\n+- [x] `swift build --build-tests` compiles with no new warnings.\n\n ## Tests\n-- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion, each with its own `ExecutorPromptCacheStore(directory:)` in a temporary folder. For in-flight cases, inject a slow writer (an internal writer closure that tests replace).\n-- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n+- [x] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion, each with its own `ExecutorPromptCacheStore(directory:)` in a temporary folder. For in-flight cases, inject a slow writer (an internal writer closure that tests replace).\n+- [x] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -63,18 +63,18 @@\n - Update every caller of `checkOut`: the executor in `Libraries/MLXFoundationModels/MLXLanguageModel.swift` (`carriedPromptCache = await ...checkOut(...)`, about `:976`) and the call sites in `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift`. Until task ^jar6qq9 lands, the executor treats `.spilled` as a cold start and deletes the file.\n\n ## Acceptance Criteria\n-- [x] An entry evicted by the byte budget is on disk, and a later `checkOut` returns `.spilled`.\n-- [x] A check-out during a spill of the same key returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n-- [x] A check-out during a spill, followed by a cache `update` on the returned entry, does not change the bytes of the file that the writer writes.\n-- [x] Spill A (slow writer), check-out, check-in, spill B: after both writes end, B's file exists and is the only file for the key.\n-- [x] At most one write runs at a time (assert with an injected writer that counts concurrent calls).\n-- [x] Other check-outs and check-ins complete while a write runs (the actor is free).\n-- [x] `checkIn` of a key removes that key's older file.\n-- [x] `swift build --build-tests` compiles with no new warnings.\n+- [ ] An entry evicted by the byte budget is on disk, and a later `checkOut` returns `.spilled`.\n+- [ ] A check-out during a spill of the same key returns `.memory` with the same entry; after the write ends, no `onDisk` record and no file remain for that key.\n+- [ ] A check-out during a spill, followed by a cache `update` on the returned entry, does not change the bytes of the file that the writer writes.\n+- [ ] Spill A (slow writer), check-out, check-in, spill B: after both writes end, B's file exists and is the only file for the key.\n+- [ ] At most one write runs at a time (assert with an injected writer that counts concurrent calls).\n+- [ ] Other check-outs and check-ins complete while a write runs (the actor is free).\n+- [ ] `checkIn` of a key removes that key's older file.\n+- [ ] `swift build --build-tests` compiles with no new warnings.\n\n ## Tests\n-- [x] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion, each with its own `ExecutorPromptCacheStore(directory:)` in a temporary folder. For in-flight cases, inject a slow writer (an internal writer closure that tests replace).\n-- [x] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n+- [ ] New `Tests/MLXFoundationModelsTests/ExecutorPromptCacheSpoolTests.swift`, one test for each criterion, each with its own `ExecutorPromptCacheStore(directory:)` in a temporary folder. For in-flight cases, inject a slow writer (an internal writer closure that tests replace).\n+- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3ADC70Q6YAADZSVF4W4V060","timestamp":"2026-09-24T19:13:16.823996Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -33,7 +33,7 @@\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -33,7 +33,7 @@\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n","transaction_id":null}
{"id":"01M3ADY7XKKZDJXKXWM6QNRMX4","timestamp":"2026-09-24T19:23:07.571189Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -77,4 +77,15 @@\n - [x] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+## Review Findings (2026-09-24 14:13)\n+\n+> Scope: `review sha HEAD~1..HEAD` — reviewed the diffs only — lines this change added or modified. 5 file(s) reviewed, 2 not reviewed.\n+\n+> 2 file(s) not reviewed — no validator matched:\n+> - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.jsonl` — no validator matches this file\n+> - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.md` — no validator matches this file\n+\n+- [ ] `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` `swift/access-control` — Internal storage detail should be explicitly declared private, not implicitly internal. The `directory` property is only used within the actor and is not intended as part of the API surface; all other similar properties on this actor explicitly declare their access level. Change line 299 to `private let directory: URL`.\n+- [ ] `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` `test-integrity/test-partitioning` — The test `anExecutorPassThatFindsItsCacheOnDiskStartsColdAndDeletesTheFile` uses real file I/O to disk (a real external system). According to test-partitioning rules, integration tests that use real external systems belong in a separate integration test target, not the unit test target. The documented convention for this file (lines 37-41) explicitly states: 'These tests read the memory tier alone, and `ExecutorPromptCacheSpoolTests` reads the files.'. Either move this test to `ExecutorPromptCacheSpoolTests.swift` (if it fits the spool testing scope), create a separate integration test target for it, or refactor to test the executor behavior without direct file I/O assertions. The test name suggests it belongs with executor behavior, not file I/O tests — consider if the assertion can be rephrased to test executor behavior rather than file system side effects.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -77,15 +77,4 @@\n - [x] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — all pass.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-## Review Findings (2026-09-24 14:13)\n-\n-> Scope: `review sha HEAD~1..HEAD` — reviewed the diffs only — lines this change added or modified. 5 file(s) reviewed, 2 not reviewed.\n-\n-> 2 file(s) not reviewed — no validator matched:\n-> - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.jsonl` — no validator matches this file\n-> - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.md` — no validator matches this file\n-\n-- [ ] `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` `swift/access-control` — Internal storage detail should be explicitly declared private, not implicitly internal. The `directory` property is only used within the actor and is not intended as part of the API surface; all other similar properties on this actor explicitly declare their access level. Change line 299 to `private let directory: URL`.\n-- [ ] `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` `test-integrity/test-partitioning` — The test `anExecutorPassThatFindsItsCacheOnDiskStartsColdAndDeletesTheFile` uses real file I/O to disk (a real external system). According to test-partitioning rules, integration tests that use real external systems belong in a separate integration test target, not the unit test target. The documented convention for this file (lines 37-41) explicitly states: 'These tests read the memory tier alone, and `ExecutorPromptCacheSpoolTests` reads the files.'. Either move this test to `ExecutorPromptCacheSpoolTests.swift` (if it fits the spool testing scope), create a separate integration test target for it, or refactor to test the executor behavior without direct file I/O assertions. The test name suggests it belongs with executor behavior, not file I/O tests — consider if the assertion can be rephrased to test executor behavior rather than file system side effects.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3ADYPSZT57K7ZRYG8ENEVSE","timestamp":"2026-09-24T19:23:22.815043Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -30,6 +30,15 @@\n     - evidence: `swift build --build-tests` — only the known `missing creator for mutated node` warning. `xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — 267 tests in 31 suites pass (7 runs, no flaky test). `xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — 694 XCTest with 36 failures (GlmOcr/Qwen25VL/Qwen35/Qwen3VL ContinuationTests + NanbeigeTests) and 1207 Swift Testing tests with 101 issues: equal to the 2026-09-24 baseline. The change touches only MLXFoundationModels, thus the other three bundles do not change.\n     - next: /commit\n   timestamp: 2026-09-24T19:11:29.185268+00:00\n+- actor: claude-code\n+  id: 01m3adyprvsp231wg089s7mdz3\n+  text: |-\n+    ### finish iteration 1 — findings\n+    - implement: changed (6 files: ExecutorPromptCache.swift, ExecutorPromptCacheFile.swift, MLXLanguageModel.swift, ExecutorPromptCacheTests.swift, new ExecutorPromptCacheSpoolTests.swift, task files)\n+    - test: green against the baseline (MLXFoundationModelsTests 267/267 pass; MLXLMTests 36 XCTest failures + 101 Swift Testing issues, equal to the baseline; only the known build warning)\n+    - commit: 7c1aac5 feat(prompt-cache): spill evicted entries to disk with one serial writer\n+    - review: findings — 2: `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` swift/access-control (`directory` must be `private let`); `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` test-integrity/test-partitioning (the executor test with file I/O must move to `ExecutorPromptCacheSpoolTests.swift`)\n+  timestamp: 2026-09-24T19:23:22.779441+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","reverse_patch":"--- original\n+++ modified\n@@ -30,15 +30,6 @@\n     - evidence: `swift build --build-tests` — only the known `missing creator for mutated node` warning. `xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest` — 267 tests in 31 suites pass (7 runs, no flaky test). `xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — 694 XCTest with 36 failures (GlmOcr/Qwen25VL/Qwen35/Qwen3VL ContinuationTests + NanbeigeTests) and 1207 Swift Testing tests with 101 issues: equal to the 2026-09-24 baseline. The change touches only MLXFoundationModels, thus the other three bundles do not change.\n     - next: /commit\n   timestamp: 2026-09-24T19:11:29.185268+00:00\n-- actor: claude-code\n-  id: 01m3adyprvsp231wg089s7mdz3\n-  text: |-\n-    ### finish iteration 1 — findings\n-    - implement: changed (6 files: ExecutorPromptCache.swift, ExecutorPromptCacheFile.swift, MLXLanguageModel.swift, ExecutorPromptCacheTests.swift, new ExecutorPromptCacheSpoolTests.swift, task files)\n-    - test: green against the baseline (MLXFoundationModelsTests 267/267 pass; MLXLMTests 36 XCTest failures + 101 Swift Testing issues, equal to the baseline; only the known build warning)\n-    - commit: 7c1aac5 feat(prompt-cache): spill evicted entries to disk with one serial writer\n-    - review: findings — 2: `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` swift/access-control (`directory` must be `private let`); `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` test-integrity/test-partitioning (the executor test with file I/O must move to `ExecutorPromptCacheSpoolTests.swift`)\n-  timestamp: 2026-09-24T19:23:22.779441+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","transaction_id":null}
{"id":"01M3AEPS03HF8T6S9JWHX8801M","timestamp":"2026-09-24T19:36:31.491004Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -96,5 +96,5 @@\n > - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.jsonl` — no validator matches this file\n > - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.md` — no validator matches this file\n\n-- [ ] `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` `swift/access-control` — Internal storage detail should be explicitly declared private, not implicitly internal. The `directory` property is only used within the actor and is not intended as part of the API surface; all other similar properties on this actor explicitly declare their access level. Change line 299 to `private let directory: URL`.\n-- [ ] `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` `test-integrity/test-partitioning` — The test `anExecutorPassThatFindsItsCacheOnDiskStartsColdAndDeletesTheFile` uses real file I/O to disk (a real external system). According to test-partitioning rules, integration tests that use real external systems belong in a separate integration test target, not the unit test target. The documented convention for this file (lines 37-41) explicitly states: 'These tests read the memory tier alone, and `ExecutorPromptCacheSpoolTests` reads the files.'. Either move this test to `ExecutorPromptCacheSpoolTests.swift` (if it fits the spool testing scope), create a separate integration test target for it, or refactor to test the executor behavior without direct file I/O assertions. The test name suggests it belongs with executor behavior, not file I/O tests — consider if the assertion can be rephrased to test executor behavior rather than file system side effects.\n\\ No newline at end of file\n+- [x] `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` `swift/access-control` — Internal storage detail should be explicitly declared private, not implicitly internal. The `directory` property is only used within the actor and is not intended as part of the API surface; all other similar properties on this actor explicitly declare their access level. Change line 299 to `private let directory: URL`.\n+- [x] `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` `test-integrity/test-partitioning` — The test `anExecutorPassThatFindsItsCacheOnDiskStartsColdAndDeletesTheFile` uses real file I/O to disk (a real external system). According to test-partitioning rules, integration tests that use real external systems belong in a separate integration test target, not the unit test target. The documented convention for this file (lines 37-41) explicitly states: 'These tests read the memory tier alone, and `ExecutorPromptCacheSpoolTests` reads the files.'. Either move this test to `ExecutorPromptCacheSpoolTests.swift` (if it fits the spool testing scope), create a separate integration test target for it, or refactor to test the executor behavior without direct file I/O assertions. The test name suggests it belongs with executor behavior, not file I/O tests — consider if the assertion can be rephrased to test executor behavior rather than file system side effects.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -96,5 +96,5 @@\n > - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.jsonl` — no validator matches this file\n > - `.kanban/tasks/01M3A1RHPV3CV6Q7Q59W0S77DT.md` — no validator matches this file\n\n-- [x] `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` `swift/access-control` — Internal storage detail should be explicitly declared private, not implicitly internal. The `directory` property is only used within the actor and is not intended as part of the API surface; all other similar properties on this actor explicitly declare their access level. Change line 299 to `private let directory: URL`.\n-- [x] `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` `test-integrity/test-partitioning` — The test `anExecutorPassThatFindsItsCacheOnDiskStartsColdAndDeletesTheFile` uses real file I/O to disk (a real external system). According to test-partitioning rules, integration tests that use real external systems belong in a separate integration test target, not the unit test target. The documented convention for this file (lines 37-41) explicitly states: 'These tests read the memory tier alone, and `ExecutorPromptCacheSpoolTests` reads the files.'. Either move this test to `ExecutorPromptCacheSpoolTests.swift` (if it fits the spool testing scope), create a separate integration test target for it, or refactor to test the executor behavior without direct file I/O assertions. The test name suggests it belongs with executor behavior, not file I/O tests — consider if the assertion can be rephrased to test executor behavior rather than file system side effects.\n\\ No newline at end of file\n+- [ ] `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` `swift/access-control` — Internal storage detail should be explicitly declared private, not implicitly internal. The `directory` property is only used within the actor and is not intended as part of the API surface; all other similar properties on this actor explicitly declare their access level. Change line 299 to `private let directory: URL`.\n+- [ ] `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` `test-integrity/test-partitioning` — The test `anExecutorPassThatFindsItsCacheOnDiskStartsColdAndDeletesTheFile` uses real file I/O to disk (a real external system). According to test-partitioning rules, integration tests that use real external systems belong in a separate integration test target, not the unit test target. The documented convention for this file (lines 37-41) explicitly states: 'These tests read the memory tier alone, and `ExecutorPromptCacheSpoolTests` reads the files.'. Either move this test to `ExecutorPromptCacheSpoolTests.swift` (if it fits the spool testing scope), create a separate integration test target for it, or refactor to test the executor behavior without direct file I/O assertions. The test name suggests it belongs with executor behavior, not file I/O tests — consider if the assertion can be rephrased to test executor behavior rather than file system side effects.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3AEW2NH6QQ2HWNZ53NEVSCY","timestamp":"2026-09-24T19:39:25.233241Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -42,8 +42,8 @@\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ff9980\n title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n #prompt-cache\n","reverse_patch":"--- original\n+++ modified\n@@ -42,8 +42,8 @@\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n-position_column: done\n-position_ordinal: ff9980\n+position_column: review\n+position_ordinal: '80'\n title: 'Disk spool, part 1: spill evicted entries with one serial writer, and hand out a spilled handle on check-out'\n ---\n #prompt-cache\n","transaction_id":null}
{"id":"01M3AEW5B0P140BEAG3ER2G53T","timestamp":"2026-09-24T19:39:27.968994Z","op":"update","item_id":"01M3A1RHPV3CV6Q7Q59W0S77DT","forward_patch":"--- original\n+++ modified\n@@ -39,6 +39,15 @@\n     - commit: 7c1aac5 feat(prompt-cache): spill evicted entries to disk with one serial writer\n     - review: findings — 2: `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` swift/access-control (`directory` must be `private let`); `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` test-integrity/test-partitioning (the executor test with file I/O must move to `ExecutorPromptCacheSpoolTests.swift`)\n   timestamp: 2026-09-24T19:23:22.779441+00:00\n+- actor: claude-code\n+  id: 01m3aew5b0sfaev0jd62bpdxvy\n+  text: |-\n+    ### finish iteration 2 — clean\n+    - implement: changed — `directory` is now `private let` in ExecutorPromptCache.swift; the executor test with spill-file I/O moved from ExecutorPromptCacheTests.swift to ExecutorPromptCacheSpoolTests.swift; one shared `respondOnce(inside:modelID:sessionID:)` helper in ScriptedModelTestSupport.swift serves both suites.\n+    - test: green against the baseline (MLXFoundationModelsTests 267/267 pass, 4 runs; MLXLMTests 36 XCTest failures + 101 Swift Testing issues, equal to the baseline; only the known build warning)\n+    - commit: 30c27ee refactor(prompt-cache): keep the spool folder private and move the spilled executor test to the spool suite\n+    - review: clean — `review sha HEAD~1..HEAD`, 0 findings; both findings of iteration 1 are ticked. Task moved to done.\n+  timestamp: 2026-09-24T19:39:27.968462+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","reverse_patch":"--- original\n+++ modified\n@@ -39,15 +39,6 @@\n     - commit: 7c1aac5 feat(prompt-cache): spill evicted entries to disk with one serial writer\n     - review: findings — 2: `Libraries/MLXFoundationModels/ExecutorPromptCache.swift:299` swift/access-control (`directory` must be `private let`); `Tests/MLXFoundationModelsTests/ExecutorPromptCacheTests.swift:483` test-integrity/test-partitioning (the executor test with file I/O must move to `ExecutorPromptCacheSpoolTests.swift`)\n   timestamp: 2026-09-24T19:23:22.779441+00:00\n-- actor: claude-code\n-  id: 01m3aew5b0sfaev0jd62bpdxvy\n-  text: |-\n-    ### finish iteration 2 — clean\n-    - implement: changed — `directory` is now `private let` in ExecutorPromptCache.swift; the executor test with spill-file I/O moved from ExecutorPromptCacheTests.swift to ExecutorPromptCacheSpoolTests.swift; one shared `respondOnce(inside:modelID:sessionID:)` helper in ScriptedModelTestSupport.swift serves both suites.\n-    - test: green against the baseline (MLXFoundationModelsTests 267/267 pass, 4 runs; MLXLMTests 36 XCTest failures + 101 Swift Testing issues, equal to the baseline; only the known build warning)\n-    - commit: 30c27ee refactor(prompt-cache): keep the spool folder private and move the spilled executor test to the spool suite\n-    - review: clean — `review sha HEAD~1..HEAD`, 0 findings; both findings of iteration 1 are ticked. Task moved to done.\n-  timestamp: 2026-09-24T19:39:27.968462+00:00\n depends_on:\n - 01M3A1QAFD56F5TEPPADDJHENH\n - 01M3A1QND68R1PN6K74Z6AV3EP\n","transaction_id":null}
