{"id":"01M3A1PX657W3BRC3BVG7RY8Q8","timestamp":"2026-09-24T15:49:24.293634Z","op":"create","item_id":"01M3A1PX65M12926Y4BJVAG56K","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,32 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '80'\n+title: Restore a saved prompt cache into the model's own fresh caches, and prove a round trip for every cache type\n+---\n+## What\n+\n+The disk tier of the executor prompt cache (see the dependent tasks) must write a session's caches to a file and read them back, also in a new process after a restart. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has two gaps:\n+\n+1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) thus saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n+2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers itself lazily, from its own `init()` (`MiniMaxM3.swift:1522`). In a new process, a load before the first `MiniMaxM3KVCache()` fails with \"Unknown cache class\".\n+\n+The fix: add a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n+\n+- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`), thus call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n+- Keep the old `loadPromptCacheSnapshot(url:)` unchanged.\n+- A throw must never leave a half-written template in use: the caller discards the templates on a throw. Say this in the doc comment.\n+\n+## Acceptance Criteria\n+- [ ] A cache of each type below survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays (`allClose`, exact for integer arrays) and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache`, `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n+- [ ] After the restore, one more `update(keys:values:)` on the restored cache gives the same output as the same update on the original cache.\n+- [ ] `LMOutput.State` saved with the caches comes back equal.\n+- [ ] A class mismatch or a layer-count mismatch throws `KVCacheError`, and does not crash.\n+\n+## Tests\n+- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing), one parameterized test over the cache types above, plus the mismatch tests.\n+- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. Note: on 2026-09-24, 36 XCTest failures and 101 Swift Testing issues (DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal, SSM chunked) already occur on `stable` before this work. Do not add to that list.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #prompt-cache\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,32 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '80'\n-title: Restore a saved prompt cache into the model's own fresh caches, and prove a round trip for every cache type\n----\n-## What\n-\n-The disk tier of the executor prompt cache (see the dependent tasks) must write a session's caches to a file and read them back, also in a new process after a restart. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has two gaps:\n-\n-1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) thus saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n-2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers itself lazily, from its own `init()` (`MiniMaxM3.swift:1522`). In a new process, a load before the first `MiniMaxM3KVCache()` fails with \"Unknown cache class\".\n-\n-The fix: add a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n-\n-- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`), thus call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n-- Keep the old `loadPromptCacheSnapshot(url:)` unchanged.\n-- A throw must never leave a half-written template in use: the caller discards the templates on a throw. Say this in the doc comment.\n-\n-## Acceptance Criteria\n-- [ ] A cache of each type below survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays (`allClose`, exact for integer arrays) and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache`, `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n-- [ ] After the restore, one more `update(keys:values:)` on the restored cache gives the same output as the same update on the original cache.\n-- [ ] `LMOutput.State` saved with the caches comes back equal.\n-- [ ] A class mismatch or a layer-count mismatch throws `KVCacheError`, and does not crash.\n-\n-## Tests\n-- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing), one parameterized test over the cache types above, plus the mismatch tests.\n-- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. Note: on 2026-09-24, 36 XCTest failures and 101 Swift Testing issues (DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal, SSM chunked) already occur on `stable` before this work. Do not add to that list.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #prompt-cache\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3A1V081J015VGMZBZ5NV1F3","timestamp":"2026-09-24T15:51:38.497925Z","op":"update","item_id":"01M3A1PX65M12926Y4BJVAG56K","forward_patch":"--- original\n+++ modified\n@@ -5,28 +5,37 @@\n position_ordinal: '80'\n title: Restore a saved prompt cache into the model's own fresh caches, and prove a round trip for every cache type\n ---\n+#prompt-cache\n+\n+(Also covers plan item F4: the offset of recurrent caches through the disk.)\n+\n ## What\n\n-The disk tier of the executor prompt cache (see the dependent tasks) must write a session's caches to a file and read them back, also in a new process after a restart. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has two gaps:\n+The disk tier of the executor prompt cache must write a session's caches to a file and read them back. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has three gaps:\n\n-1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) thus saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n+1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n 2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers itself lazily, from its own `init()` (`MiniMaxM3.swift:1522`). In a new process, a load before the first `MiniMaxM3KVCache()` fails with \"Unknown cache class\".\n+3. `ArraysCache`/`MambaCache` lose their `offset`: their `metaState` (`KVCache.swift:1531-1554`) has no offset. The executor then refuses the cache, because every cache offset must equal the ledger length (`Libraries/MLXLMCommon/PromptCacheReusePolicy.swift:448`). Qwen3.5 / Qwen3-Next would thus always come back cold. Also check the `ChunkedKVCache` offset after a front trim.\n\n-The fix: add a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n+The fix: a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n\n-- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`), thus call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n-- Keep the old `loadPromptCacheSnapshot(url:)` unchanged.\n-- A throw must never leave a half-written template in use: the caller discards the templates on a throw. Say this in the doc comment.\n+- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`): call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n+- `savePromptCache` also records the `offset` of each cache in the file (in a reserved metadata key, NOT in `metaState`: `testCacheSerialization` compares `metaState`, and its format must not change). The new load sets the offset of each restored cache from that record. An older file without the record loads as before.\n+- The safetensors load is lazy (`Libraries/MLXLMCommon/Load.swift:9-15`): the file must stay until the restored arrays are evaluated. State this in the doc comment of the new function, and evaluate the restored arrays before it returns.\n+- Keep `loadPromptCacheSnapshot(url:)` unchanged.\n+- A throw must never leave a half-written template in use: say in the doc comment that the caller discards the templates on a throw.\n\n ## Acceptance Criteria\n-- [ ] A cache of each type below survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays (`allClose`, exact for integer arrays) and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache`, `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n-- [ ] After the restore, one more `update(keys:values:)` on the restored cache gives the same output as the same update on the original cache.\n+- [ ] A cache of each type survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache` (also after a front trim), `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n+- [ ] After the restore, one more `update(keys:values:)` (or the recurrent equivalent) gives the same output as the same update on the original cache.\n - [ ] `LMOutput.State` saved with the caches comes back equal.\n - [ ] A class mismatch or a layer-count mismatch throws `KVCacheError`, and does not crash.\n+- [ ] The file can be deleted after the function returns, and the restored caches still work.\n+- [ ] The existing `testCacheSerialization` still passes unchanged.\n\n ## Tests\n-- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing), one parameterized test over the cache types above, plus the mismatch tests.\n-- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. Note: on 2026-09-24, 36 XCTest failures and 101 Swift Testing issues (DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal, SSM chunked) already occur on `stable` before this work. Do not add to that list.\n+- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing): one parameterized test over the cache types above, the offset checks, the delete-after-load check, and the mismatch tests.\n+- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. (On 2026-09-24, `stable` already has 36 XCTest failures and 101 Swift Testing issues in DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal and the chunked SSM test. Do not add to that list.)\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #prompt-cache\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -5,37 +5,28 @@\n position_ordinal: '80'\n title: Restore a saved prompt cache into the model's own fresh caches, and prove a round trip for every cache type\n ---\n-#prompt-cache\n-\n-(Also covers plan item F4: the offset of recurrent caches through the disk.)\n-\n ## What\n\n-The disk tier of the executor prompt cache must write a session's caches to a file and read them back. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has three gaps:\n+The disk tier of the executor prompt cache (see the dependent tasks) must write a session's caches to a file and read them back, also in a new process after a restart. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has two gaps:\n\n-1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n+1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) thus saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n 2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers itself lazily, from its own `init()` (`MiniMaxM3.swift:1522`). In a new process, a load before the first `MiniMaxM3KVCache()` fails with \"Unknown cache class\".\n-3. `ArraysCache`/`MambaCache` lose their `offset`: their `metaState` (`KVCache.swift:1531-1554`) has no offset. The executor then refuses the cache, because every cache offset must equal the ledger length (`Libraries/MLXLMCommon/PromptCacheReusePolicy.swift:448`). Qwen3.5 / Qwen3-Next would thus always come back cold. Also check the `ChunkedKVCache` offset after a front trim.\n\n-The fix: a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n+The fix: add a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n\n-- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`): call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n-- `savePromptCache` also records the `offset` of each cache in the file (in a reserved metadata key, NOT in `metaState`: `testCacheSerialization` compares `metaState`, and its format must not change). The new load sets the offset of each restored cache from that record. An older file without the record loads as before.\n-- The safetensors load is lazy (`Libraries/MLXLMCommon/Load.swift:9-15`): the file must stay until the restored arrays are evaluated. State this in the doc comment of the new function, and evaluate the restored arrays before it returns.\n-- Keep `loadPromptCacheSnapshot(url:)` unchanged.\n-- A throw must never leave a half-written template in use: say in the doc comment that the caller discards the templates on a throw.\n+- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`), thus call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n+- Keep the old `loadPromptCacheSnapshot(url:)` unchanged.\n+- A throw must never leave a half-written template in use: the caller discards the templates on a throw. Say this in the doc comment.\n\n ## Acceptance Criteria\n-- [ ] A cache of each type survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache` (also after a front trim), `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n-- [ ] After the restore, one more `update(keys:values:)` (or the recurrent equivalent) gives the same output as the same update on the original cache.\n+- [ ] A cache of each type below survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays (`allClose`, exact for integer arrays) and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache`, `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n+- [ ] After the restore, one more `update(keys:values:)` on the restored cache gives the same output as the same update on the original cache.\n - [ ] `LMOutput.State` saved with the caches comes back equal.\n - [ ] A class mismatch or a layer-count mismatch throws `KVCacheError`, and does not crash.\n-- [ ] The file can be deleted after the function returns, and the restored caches still work.\n-- [ ] The existing `testCacheSerialization` still passes unchanged.\n\n ## Tests\n-- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing): one parameterized test over the cache types above, the offset checks, the delete-after-load check, and the mismatch tests.\n-- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. (On 2026-09-24, `stable` already has 36 XCTest failures and 101 Swift Testing issues in DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal and the chunked SSM test. Do not add to that list.)\n+- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing), one parameterized test over the cache types above, plus the mismatch tests.\n+- [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. Note: on 2026-09-24, 36 XCTest failures and 101 Swift Testing issues (DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal, SSM chunked) already occur on `stable` before this work. Do not add to that list.\n\n ## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n\\ No newline at end of file\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #prompt-cache\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3A29G13EHKTRHSHT0N1RER3","timestamp":"2026-09-24T15:59:33.411701Z","op":"update","item_id":"01M3A1PX65M12926Y4BJVAG56K","forward_patch":"--- original\n+++ modified\n@@ -7,34 +7,37 @@\n ---\n #prompt-cache\n\n-(Also covers plan item F4: the offset of recurrent caches through the disk.)\n+(The offset part of F4 is now its own task, which depends on this one.)\n\n ## What\n\n-The disk tier of the executor prompt cache must write a session's caches to a file and read them back. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has three gaps:\n+The disk spool must read a session's caches back, also in a new process. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). Two gaps:\n\n-1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n-2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers itself lazily, from its own `init()` (`MiniMaxM3.swift:1522`). In a new process, a load before the first `MiniMaxM3KVCache()` fails with \"Unknown cache class\".\n-3. `ArraysCache`/`MambaCache` lose their `offset`: their `metaState` (`KVCache.swift:1531-1554`) has no offset. The executor then refuses the cache, because every cache offset must equal the ledger length (`Libraries/MLXLMCommon/PromptCacheReusePolicy.swift:448`). Qwen3.5 / Qwen3-Next would thus always come back cold. Also check the `ChunkedKVCache` offset after a front trim.\n+1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. It is saved as `\"KVCache\"`, and the load throws. Its `init` needs a `DeepSeekV4Configuration`, thus a `(state, metaState)` factory cannot build it.\n+2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers lazily from its own `init()` (`:1522`). In a new process, a load before the first instance fails with \"Unknown cache class\".\n\n-The fix: a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n+The fix: restore the saved state INTO caches that the model made (`model.newCache(parameters:)`).\n\n-- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`): call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n-- `savePromptCache` also records the `offset` of each cache in the file (in a reserved metadata key, NOT in `metaState`: `testCacheSerialization` compares `metaState`, and its format must not change). The new load sets the offset of each restored cache from that record. An older file without the record loads as before.\n-- The safetensors load is lazy (`Libraries/MLXLMCommon/Load.swift:9-15`): the file must stay until the restored arrays are evaluated. State this in the doc comment of the new function, and evaluate the restored arrays before it returns.\n+- In `KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`.\n+- For each layer `i`, match the saved class name against `cacheClassName(templates[i])`. Throw `KVCacheError` on a layer-count mismatch.\n+- Converted layers: generation can change a `KVCacheSimple` layer into `QuantizedKVCache` (`maybeQuantizeKVCache`) or `TurboQuantKVCache`, and `newCache(parameters:)` never gives those classes. For a `KVCacheSimple` template, accept a saved `QuantizedKVCache` or `TurboQuantKVCache` and build that layer with the existing restore path (`restoreCacheFromMetaState`). Any other mismatch throws.\n+- VALIDATE BEFORE ANY SETTER RUNS. The setters stop the process on bad input: `KVCacheSimple.state` has `fatalError` for a count other than 2, `MiniMaxM3KVCache.state` has `fatalError` in its `default:` case, `ArraysCache.metaState` has `assertionFailure`. For each class, check the state-array count, the array rank, and the metaState count first, as `validatePromptCache` does for the existing path (`KVCache.swift:2385`). Include `DeepSeekV4Cache` with and without an indexer (window state 0 or 2 arrays + the branch slots) and `MiniMaxM3KVCache` with 2 or 3 arrays.\n+- Then restore: `ArraysCache`/`MambaCache` through `restoreFromMetaState(state:savedMetaState:)`; `CacheList` into the template's own children; other types through `state` and `metaState`.\n+- The safetensors load is lazy (`Libraries/MLXLMCommon/Load.swift:9-15`): evaluate the restored arrays before the function returns, thus the caller can delete the file. Say so in the doc comment.\n+- A throw leaves the templates in an undefined state: say in the doc comment that the caller discards them.\n - Keep `loadPromptCacheSnapshot(url:)` unchanged.\n-- A throw must never leave a half-written template in use: say in the doc comment that the caller discards the templates on a throw.\n\n ## Acceptance Criteria\n-- [ ] A cache of each type survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache` (also after a front trim), `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n-- [ ] After the restore, one more `update(keys:values:)` (or the recurrent equivalent) gives the same output as the same update on the original cache.\n-- [ ] `LMOutput.State` saved with the caches comes back equal.\n-- [ ] A class mismatch or a layer-count mismatch throws `KVCacheError`, and does not crash.\n+- [ ] Round trip `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` gives equal `state` arrays and equal `metaState` for: `KVCacheSimple`, `RotatingKVCache` (before and after wrap), `QuantizedKVCache`, `ChunkedKVCache`, `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n+- [ ] A `QuantizedKVCache` layer and a `TurboQuantKVCache` layer restore into a `KVCacheSimple` template.\n+- [ ] After the restore, one more `update(keys:values:)` (or the recurrent equivalent) gives the same output as on the original cache.\n+- [ ] `LMOutput.State` comes back equal.\n+- [ ] A class mismatch, a layer-count mismatch, a wrong state-array count for a class, and a wrong metaState count each throw `KVCacheError`, and none stops the process.\n - [ ] The file can be deleted after the function returns, and the restored caches still work.\n-- [ ] The existing `testCacheSerialization` still passes unchanged.\n+- [ ] All existing tests in `Tests/MLXLMTests/KVCacheTests.swift` pass without changes.\n\n ## Tests\n-- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing): one parameterized test over the cache types above, the offset checks, the delete-after-load check, and the mismatch tests.\n+- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing): one parameterized round trip over the types above, the converted-layer cases, the delete-after-load case, and each error case.\n - [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. (On 2026-09-24, `stable` already has 36 XCTest failures and 101 Swift Testing issues in DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal and the chunked SSM test. Do not add to that list.)\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -7,37 +7,34 @@\n ---\n #prompt-cache\n\n-(The offset part of F4 is now its own task, which depends on this one.)\n+(Also covers plan item F4: the offset of recurrent caches through the disk.)\n\n ## What\n\n-The disk spool must read a session's caches back, also in a new process. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). Two gaps:\n+The disk tier of the executor prompt cache must write a session's caches to a file and read them back. `loadPromptCacheSnapshot(url:)` (`Libraries/MLXLMCommon/KVCache.swift:2033`) rebuilds each cache from its saved class name through `restoreCacheFromMetaState` (`KVCache.swift:2191`). That path has three gaps:\n\n-1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. It is saved as `\"KVCache\"`, and the load throws. Its `init` needs a `DeepSeekV4Configuration`, thus a `(state, metaState)` factory cannot build it.\n-2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers lazily from its own `init()` (`:1522`). In a new process, a load before the first instance fails with \"Unknown cache class\".\n+1. `DeepSeekV4Cache` (`Libraries/MLXLLM/Models/DeepSeekV4Cache.swift:363`) is not registered in `KVCacheSerializationRegistry`. `cacheClassName` (`KVCache.swift:1898`) saves it as `\"KVCache\"`, and the load throws \"Corrupt prompt cache\" because the state has more than 2 arrays. Its `init` needs a `DeepSeekV4Configuration`, thus a restore factory with only `(state, metaState)` cannot build it.\n+2. `MiniMaxM3KVCache` (`Libraries/MLXVLM/Models/MiniMaxM3.swift:1485`) registers itself lazily, from its own `init()` (`MiniMaxM3.swift:1522`). In a new process, a load before the first `MiniMaxM3KVCache()` fails with \"Unknown cache class\".\n+3. `ArraysCache`/`MambaCache` lose their `offset`: their `metaState` (`KVCache.swift:1531-1554`) has no offset. The executor then refuses the cache, because every cache offset must equal the ledger length (`Libraries/MLXLMCommon/PromptCacheReusePolicy.swift:448`). Qwen3.5 / Qwen3-Next would thus always come back cold. Also check the `ChunkedKVCache` offset after a front trim.\n\n-The fix: restore the saved state INTO caches that the model made (`model.newCache(parameters:)`).\n+The fix: a restore that writes the saved state INTO caches the model made. The model knows its own cache shape (`model.newCache(parameters:)`), thus no registry and no configuration is necessary.\n\n-- In `KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`.\n-- For each layer `i`, match the saved class name against `cacheClassName(templates[i])`. Throw `KVCacheError` on a layer-count mismatch.\n-- Converted layers: generation can change a `KVCacheSimple` layer into `QuantizedKVCache` (`maybeQuantizeKVCache`) or `TurboQuantKVCache`, and `newCache(parameters:)` never gives those classes. For a `KVCacheSimple` template, accept a saved `QuantizedKVCache` or `TurboQuantKVCache` and build that layer with the existing restore path (`restoreCacheFromMetaState`). Any other mismatch throws.\n-- VALIDATE BEFORE ANY SETTER RUNS. The setters stop the process on bad input: `KVCacheSimple.state` has `fatalError` for a count other than 2, `MiniMaxM3KVCache.state` has `fatalError` in its `default:` case, `ArraysCache.metaState` has `assertionFailure`. For each class, check the state-array count, the array rank, and the metaState count first, as `validatePromptCache` does for the existing path (`KVCache.swift:2385`). Include `DeepSeekV4Cache` with and without an indexer (window state 0 or 2 arrays + the branch slots) and `MiniMaxM3KVCache` with 2 or 3 arrays.\n-- Then restore: `ArraysCache`/`MambaCache` through `restoreFromMetaState(state:savedMetaState:)`; `CacheList` into the template's own children; other types through `state` and `metaState`.\n-- The safetensors load is lazy (`Libraries/MLXLMCommon/Load.swift:9-15`): evaluate the restored arrays before the function returns, thus the caller can delete the file. Say so in the doc comment.\n-- A throw leaves the templates in an undefined state: say in the doc comment that the caller discards them.\n+- In `Libraries/MLXLMCommon/KVCache.swift`, add `public func loadPromptCacheSnapshot(url: URL, into templates: [KVCache]) throws -> PromptCacheSnapshot`. For each layer `i`: require that the saved class name equals `cacheClassName(templates[i])` (throw `KVCacheError` if not, and if the counts differ); then restore the saved `state` and `metaState` into `templates[i]`. `ArraysCache`, `MambaCache` and `CacheList` refuse a direct `metaState` set (`KVCache.swift:1551`, `:1791`): call `restoreFromMetaState(state:savedMetaState:)` for `ArraysCache`/`MambaCache`, and restore each child of a `CacheList` into the template's own children.\n+- `savePromptCache` also records the `offset` of each cache in the file (in a reserved metadata key, NOT in `metaState`: `testCacheSerialization` compares `metaState`, and its format must not change). The new load sets the offset of each restored cache from that record. An older file without the record loads as before.\n+- The safetensors load is lazy (`Libraries/MLXLMCommon/Load.swift:9-15`): the file must stay until the restored arrays are evaluated. State this in the doc comment of the new function, and evaluate the restored arrays before it returns.\n - Keep `loadPromptCacheSnapshot(url:)` unchanged.\n+- A throw must never leave a half-written template in use: say in the doc comment that the caller discards the templates on a throw.\n\n ## Acceptance Criteria\n-- [ ] Round trip `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` gives equal `state` arrays and equal `metaState` for: `KVCacheSimple`, `RotatingKVCache` (before and after wrap), `QuantizedKVCache`, `ChunkedKVCache`, `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n-- [ ] A `QuantizedKVCache` layer and a `TurboQuantKVCache` layer restore into a `KVCacheSimple` template.\n-- [ ] After the restore, one more `update(keys:values:)` (or the recurrent equivalent) gives the same output as on the original cache.\n-- [ ] `LMOutput.State` comes back equal.\n-- [ ] A class mismatch, a layer-count mismatch, a wrong state-array count for a class, and a wrong metaState count each throw `KVCacheError`, and none stops the process.\n+- [ ] A cache of each type survives `savePromptCache` → `loadPromptCacheSnapshot(url:into:)` with equal `offset`, equal `state` arrays and equal `metaState`: `KVCacheSimple`, `RotatingKVCache` (before and after it wraps), `QuantizedKVCache`, `ChunkedKVCache` (also after a front trim), `MambaCache`, `ArraysCache`, `TurboQuantKVCache`, `VarianceNormalizedKVCache`, `CacheList`, `DeepSeekV4Cache` (with and without an indexer), `MiniMaxM3KVCache`.\n+- [ ] After the restore, one more `update(keys:values:)` (or the recurrent equivalent) gives the same output as the same update on the original cache.\n+- [ ] `LMOutput.State` saved with the caches comes back equal.\n+- [ ] A class mismatch or a layer-count mismatch throws `KVCacheError`, and does not crash.\n - [ ] The file can be deleted after the function returns, and the restored caches still work.\n-- [ ] All existing tests in `Tests/MLXLMTests/KVCacheTests.swift` pass without changes.\n+- [ ] The existing `testCacheSerialization` still passes unchanged.\n\n ## Tests\n-- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing): one parameterized round trip over the types above, the converted-layer cases, the delete-after-load case, and each error case.\n+- [ ] New `Tests/MLXLMTests/PromptCacheTemplateRestoreTests.swift` (Swift Testing): one parameterized test over the cache types above, the offset checks, the delete-after-load check, and the mismatch tests.\n - [ ] `swift build --build-tests && xcrun xctest .build/out/Products/Debug/MLXLMTests.xctest` — the new tests pass. (On 2026-09-24, `stable` already has 36 XCTest failures and 101 Swift Testing issues in DeepSeekV4*, MiniMaxM3, Qwen35MTPMetal and the chunked SSM test. Do not add to that list.)\n\n ## Workflow\n","transaction_id":null}
