---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m3dhgdr4pge00wqehnvkdw6z
  text: |-
    Research:
    - The line numbers of the task are still correct in `ExecutorPromptCacheFile.swift` (write 110-119, rename 193-199, removeFile 209-219, encode 236-244, decode 254-267).
    - A failed safetensors write throws `MLXError.caught(String)` from mlx-swift `save(arrays:metadata:url:)`. The message is `[save_safetensors] Failed to open <path>` (Cmlx `safetensors.cpp:174`).
    - `rename(2)` of a file onto a folder gives `EISDIR`, thus a folder at the destination makes line 197 throw `POSIXError(.EISDIR)`.
    - A removal fails when the folder of the file is read-only (mode 0o555). The file stays and the error goes to `ExecutorPromptCacheLog.info`. A test cannot see that log line. Thus `removeFile(at:)` gets a `report` parameter with the default `ExecutorPromptCacheLog.info`. This is the seam style that `ExecutorPromptCacheSlot` already uses (`report: @escaping (String) -> Void = ExecutorPromptCacheLog.info`). All four callers keep the default.
    - A missing file throws from `FileHandle(forReadingFrom:)`: `CocoaError(.fileReadNoSuchFile)`.
  timestamp: 2026-09-26T00:23:12.388169+00:00
- actor: claude-code
  id: 01m3dhr9d73yaxg05hgjs35p7n
  text: |-
    Implementation:
    - `removeFile(at:report:)` got the `report` seam (default `ExecutorPromptCacheLog.info`). No caller changed. This is the only production change, and it is necessary because the log line of a failed removal (old lines 216-217) cannot be seen from a test otherwise.
    - New tests in `ExecutorPromptCacheFileTests.swift`: `.tokenOutOfRange` from `prepare`; `.ledgerNotDecodable(key)` for 2 keys x 3 damages (missing, not base64, 3 bytes); write into a missing folder (`MLXError.caught` with the `[save_safetensors] Failed to open file <partial path> at` text, no partial file); rename onto a folder (`POSIXError(.EISDIR)`, partial file removed); `removeFile` of a missing URL (no report, no file); a removal in a read-only folder (file stays, one report with `NSCocoaErrorDomain Code=513`).
    - The truncated-file test now expects exactly `.truncated(byteCount: <cut size>, expectedByteCount: <file size or header end>)`. The missing-file test now expects `CocoaError` with code `.fileNoSuchFile` (4). Note: the task text guessed `.fileReadNoSuchFile` (260) would be natural, but `FileHandle(forReadingFrom:)` gives code 4.
    - The MLX message ends with ` at <source path>:<line>` of mlx-c, thus the test checks the prefix up to ` at `, not the whole text.
    - Break checks (each break made its test fail, then the break was removed): rethrow of another error at write line 117; no `removeFile` at line 116; `.EIO` in place of `errno` at line 197; no `fileExists` guard at line 211; no `report` at 216-217; another token at 239; another key at 258; `byteCount + 1` in `require`.
    - No bug found in the library.

    ### implement — changed
    - evidence: 2 files — Libraries/MLXFoundationModels/ExecutorPromptCacheFile.swift, Tests/MLXFoundationModelsTests/ExecutorPromptCacheFileTests.swift; MLXFoundationModelsTests 329 tests pass
    - next: /test
  timestamp: 2026-09-26T00:27:30.087308+00:00
position_column: doing
position_ordinal: '80'
title: Test the error paths of ExecutorPromptCacheFile write, rename, remove and TokenLedger
---
## What

File: `Libraries/MLXFoundationModels/ExecutorPromptCacheFile.swift`. File coverage: 94.07% (127/135 lines).

Uncovered functions and ranges:
- `ExecutorPromptCacheFile.write(_:to:)`, lines 110-119: 8/10. Uncovered 116-117. A failed write must remove the partial file `<name>.partial.safetensors` and throw.
- `ExecutorPromptCacheFile.rename(_:to:)`, lines 193-199: 6/7. Uncovered 197. A failed rename throws a `POSIXError`.
- `ExecutorPromptCacheFile.removeFile(at:)`, lines 209-219: 8/11. Uncovered 211 (no file at the URL) and 216-217 (a removal that fails goes to the log and does not throw).
- `TokenLedger.encode(_:)`, lines 236-244: 8/9. Uncovered 239. A token that does not fit in Int32 throws `ExecutorPromptCacheFileError.tokenOutOfRange`.
- `TokenLedger.decode(_:key:)`, lines 254-267: 13/14. Uncovered 258. A missing key, text that is not base64, or a byte count that is not a multiple of 4 throws `ExecutorPromptCacheFileError.ledgerNotDecodable(key)`.

Weak assertion in the same file of tests: `Tests/MLXFoundationModelsTests/ExecutorPromptCacheFileTests.swift:374-388` (`aTruncatedFileThrows`) and 390-400 (`aMissingFileThrows`) expect only `(any Error).self`. Thus they pass when the read throws for another reason (for example a lazy-load failure), and they do not prove that `SafetensorsLength.requireWholeFile` gave `.truncated`.

What to test:
- `prepare` of an entry whose ledger holds `Int(Int32.max) + 1` throws `.tokenOutOfRange`.
- `read` of a file whose `tokens` or `renderTokens` metadata is missing, is not base64, or has 3 bytes throws `.ledgerNotDecodable` with the correct key. Make the file with `savePromptCache` and the correct `format`, `modelID` and `sessionID` metadata.
- `write` to a URL whose folder does not exist throws, and leaves no partial file.
- `write` to a destination that is a folder (so the rename fails) throws, and removes the partial file.
- `removeFile(at:)` of a missing URL does nothing and does not throw.
- Add a new test: a file cut at its last byte and a file cut inside the header each throw exactly `.truncated(byteCount:expectedByteCount:)`, with the cut size as `byteCount`.

## Acceptance Criteria

- [ ] Each uncovered line above is covered by a test.
- [ ] Each error test checks the exact error case, not only `(any Error).self`.
- [ ] A test proves the exact `.truncated` case for both cuts.
- [ ] No test leaves a file in the temporary folder.
- [ ] All five unit bundles pass.

## Tests

Test file: `Tests/MLXFoundationModelsTests/ExecutorPromptCacheFileTests.swift`.

Run:

```sh
swift build --build-tests
xcrun xctest .build/out/Products/Debug/MLXFoundationModelsTests.xctest
```

## Workflow

- Use `/tdd` #coverage-gap #prompt-cache