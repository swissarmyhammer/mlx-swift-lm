{"id":"01KWW4Z8JHN3A7DDBS3V5VEXD8","timestamp":"2026-07-06T16:42:50.833064Z","op":"create","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,21 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '8480'\n+title: Expand MLXLanguageModel's error taxonomy to cover the real FoundationModels GenerationError cases\n+---\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `unsupportedCapability` and `unsupportedGenerationGuide`. Real FoundationModels callers pattern-match on the full `LanguageModelSession.GenerationError` case set, but this backend has no equivalents for:\n+\n+- `assetsUnavailable` (model failed to load / weights missing / OOM during load)\n+- `guardrailViolation` (if/when any content-safety gating is added)\n+- `unsupportedLanguageOrLocale`\n+- `decodingFailure` (tokenizer/detokenizer errors, malformed model output)\n+- `concurrentRequests`\n+- `rateLimited`\n+\n+Right now failures in these categories (model load failures, OOM, tokenizer errors) leak out as opaque/untyped errors that calling code can't distinguish from one another.\n+\n+Define the missing error cases (extending whatever error type/enum backs `unsupportedCapability` today) and map the actual internal failure sites in this file — model/weights loading, tokenizer encode/decode, and any other currently-untyped throw sites — to the appropriate typed case. Scope this task to load-failure and decoding-failure mapping (the cases that have concrete failure sites today); `guardrailViolation`, `concurrentRequests`, and `rateLimited` may just need the case defined if there's no corresponding behavior yet to wire them to.\n+\n+Add tests asserting that a simulated model-load failure and a simulated tokenizer/decoding failure each surface as their corresponding typed error case rather than a generic error.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,21 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '8480'\n-title: Expand MLXLanguageModel's error taxonomy to cover the real FoundationModels GenerationError cases\n----\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `unsupportedCapability` and `unsupportedGenerationGuide`. Real FoundationModels callers pattern-match on the full `LanguageModelSession.GenerationError` case set, but this backend has no equivalents for:\n-\n-- `assetsUnavailable` (model failed to load / weights missing / OOM during load)\n-- `guardrailViolation` (if/when any content-safety gating is added)\n-- `unsupportedLanguageOrLocale`\n-- `decodingFailure` (tokenizer/detokenizer errors, malformed model output)\n-- `concurrentRequests`\n-- `rateLimited`\n-\n-Right now failures in these categories (model load failures, OOM, tokenizer errors) leak out as opaque/untyped errors that calling code can't distinguish from one another.\n-\n-Define the missing error cases (extending whatever error type/enum backs `unsupportedCapability` today) and map the actual internal failure sites in this file — model/weights loading, tokenizer encode/decode, and any other currently-untyped throw sites — to the appropriate typed case. Scope this task to load-failure and decoding-failure mapping (the cases that have concrete failure sites today); `guardrailViolation`, `concurrentRequests`, and `rateLimited` may just need the case defined if there's no corresponding behavior yet to wire them to.\n-\n-Add tests asserting that a simulated model-load failure and a simulated tokenizer/decoding failure each surface as their corresponding typed error case rather than a generic error.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KWWDYQGYJDSG0N4S3DEKS8FC","timestamp":"2026-07-06T19:19:50.558506Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -3,19 +3,17 @@\n - claude-code\n position_column: todo\n position_ordinal: '8480'\n-title: Expand MLXLanguageModel's error taxonomy to cover the real FoundationModels GenerationError cases\n+title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `unsupportedCapability` and `unsupportedGenerationGuide`. Real FoundationModels callers pattern-match on the full `LanguageModelSession.GenerationError` case set, but this backend has no equivalents for:\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\n\n-- `assetsUnavailable` (model failed to load / weights missing / OOM during load)\n-- `guardrailViolation` (if/when any content-safety gating is added)\n-- `unsupportedLanguageOrLocale`\n-- `decodingFailure` (tokenizer/detokenizer errors, malformed model output)\n-- `concurrentRequests`\n-- `rateLimited`\n+**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\n\n-Right now failures in these categories (model load failures, OOM, tokenizer errors) leak out as opaque/untyped errors that calling code can't distinguish from one another.\n+- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\n+- `LanguageModelError.timeout(debugDescription: String)`\n+- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\n+- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\n\n-Define the missing error cases (extending whatever error type/enum backs `unsupportedCapability` today) and map the actual internal failure sites in this file — model/weights loading, tokenizer encode/decode, and any other currently-untyped throw sites — to the appropriate typed case. Scope this task to load-failure and decoding-failure mapping (the cases that have concrete failure sites today); `guardrailViolation`, `concurrentRequests`, and `rateLimited` may just need the case defined if there's no corresponding behavior yet to wire them to.\n+I do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\n\n-Add tests asserting that a simulated model-load failure and a simulated tokenizer/decoding failure each surface as their corresponding typed error case rather than a generic error.\n\\ No newline at end of file\n+Scope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,17 +3,19 @@\n - claude-code\n position_column: todo\n position_ordinal: '8480'\n-title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n+title: Expand MLXLanguageModel's error taxonomy to cover the real FoundationModels GenerationError cases\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `unsupportedCapability` and `unsupportedGenerationGuide`. Real FoundationModels callers pattern-match on the full `LanguageModelSession.GenerationError` case set, but this backend has no equivalents for:\n\n-**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\n+- `assetsUnavailable` (model failed to load / weights missing / OOM during load)\n+- `guardrailViolation` (if/when any content-safety gating is added)\n+- `unsupportedLanguageOrLocale`\n+- `decodingFailure` (tokenizer/detokenizer errors, malformed model output)\n+- `concurrentRequests`\n+- `rateLimited`\n\n-- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\n-- `LanguageModelError.timeout(debugDescription: String)`\n-- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\n-- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\n+Right now failures in these categories (model load failures, OOM, tokenizer errors) leak out as opaque/untyped errors that calling code can't distinguish from one another.\n\n-I do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\n+Define the missing error cases (extending whatever error type/enum backs `unsupportedCapability` today) and map the actual internal failure sites in this file — model/weights loading, tokenizer encode/decode, and any other currently-untyped throw sites — to the appropriate typed case. Scope this task to load-failure and decoding-failure mapping (the cases that have concrete failure sites today); `guardrailViolation`, `concurrentRequests`, and `rateLimited` may just need the case defined if there's no corresponding behavior yet to wire them to.\n\n-Scope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\n\\ No newline at end of file\n+Add tests asserting that a simulated model-load failure and a simulated tokenizer/decoding failure each surface as their corresponding typed error case rather than a generic error.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KX3A239D6FE14B98BCP9JCGD","timestamp":"2026-07-09T11:26:27.629703Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8480'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\n","transaction_id":null}
{"id":"01KX3C9B1ZAV296YCPJVDS5XRS","timestamp":"2026-07-09T12:05:22.111776Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,46 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01kx3c9b1zvhavy0jg1wxmdsst\n+  text: |-\n+    Investigated all 3 candidate cases. Implemented 1 of 3 (`.unsupportedTranscriptContent`); documented the other 2 as genuinely not applicable.\n+\n+    **Important correction to the task's premise:** this machine DOES now have a real os27 SDK/Xcode available (`xcodebuild -version` reports Xcode 27.0, `sw_vers` reports macOS 27.0, and `xcode-select -p` points at the installed Xcode). I used it to get ground truth instead of guessing:\n+    - `swiftc -typecheck` with a deliberately-incomplete `switch` over `LanguageModelError` prints the compiler's own \"add missing case\" diagnostics, giving the full, real case list: `.contextSizeExceeded`, `.rateLimited`, `.guardrailViolation`, `.refusal`, `.unsupportedCapability`, `.unsupportedTranscriptContent`, `.unsupportedGenerationGuide`, `.unsupportedLanguageOrLocale`, `.timeout` (+ `@unknown default`). No `assetsUnavailable`/`decodingFailure`/`concurrentRequests` exist — confirms the task's caution against guessing those was correct.\n+    - `swift-demangle` over `FoundationModels.tbd`'s exported symbols gave the real associated-value shapes. Confirmed via compile probes (not just reading symbol names) that only the *smaller*-arity public inits are actually callable (the `underlyingErrors:`-inclusive inits are exported at the ABI level but not part of the public Swift API surface — attempting to call them gives \"extra argument\" errors):\n+      - `LanguageModelError.RateLimited(resetDate: Date?, debugDescription: String)`\n+      - `LanguageModelError.Timeout(debugDescription: String)`\n+      - `LanguageModelError.UnsupportedTranscriptContent(unsupportedContent: [Transcript.Entry], debugDescription: String)` — note `unsupportedContent` is `[Transcript.Entry]`, **not** segments or `[UserInput.Image]`.\n+\n+    **1. `.rateLimited` — NOT implemented, genuinely not applicable.** Investigated concurrency handling: `PromptCache` (actor) and container loading both *queue/wait* for concurrent requests against the same model rather than *reject* them (see `PromptCache.swift` doc comments on slot pooling/actor serialization, and `ModelCache`'s per-model load de-dup). There is no code path anywhere in `MLXLanguageModel.swift`/`PromptCache.swift` that rejects a request because another is in flight. A fully local, in-process backend has no rate-limiter analog (no quota, no external service). Left unmapped per the task's own allowance.\n+\n+    **2. `.timeout` — NOT implemented, genuinely not applicable.** Grepped for any timeout/deadline concept: `GenerationOptions` (confirmed via the same SDK-probing technique) exposes only `maximumResponseTokens`, `sampling`/`samplingMode`, `temperature`, `toolCallingMode` — no deadline field. The only cancellation mechanism anywhere in the generation loop is cooperative `Task.checkCancellation()`/`catch is CancellationError` (`MLXLanguageModel.swift` respond(), `GuidedGenerationLoop.swift`), which fires for *any* reason the enclosing `Task` is cancelled (caller-initiated cancel, test harness time limit, etc.) — not specifically because a time budget was exceeded. Remapping raw `CancellationError` to `.timeout` would misrepresent arbitrary cancellations as timeouts, which is exactly the \"fake scenario\" the task warns against. No existing deadline parameter to hook a real mechanism to, so per the task's guidance I did not invent a new timeout feature. Left unmapped.\n+\n+    **3. `.unsupportedTranscriptContent` — IMPLEMENTED.** Traced the image path: `Transcript.ImageAttachment.ciImage` is a non-optional, non-throwing `CIImage` getter (confirmed via the SDK), and `UserInput.Image.asCIImage()` for our `.ciImage(...)` case just returns it directly — so the literal \"CIImage decode failure\" scenario the task description speculated about does **not** exist in this SDK; that door is already closed by the framework before we ever see the segment.\n+\n+    The *real* generic-error leak is one level downstream: `Executor.prepareRespondSetup`/`Executor.preparedInput` call `context.processor.prepare(input:)` (the concrete per-model `UserInputProcessor`, e.g. a VLM's own image tensor prep). When a model's own image-specific processing fails (mismatched image count, unsupported resolution, resize/decode failure — `MLXVLM.VLMError` and its per-architecture siblings), that error previously propagated fully opaque/untyped to the FoundationModels caller.\n+\n+    Deliberately did **not** add `import MLXVLM` to catch `VLMError` by type: `Package.swift` explicitly documents \"the MLXFoundationModels product target deliberately does NOT depend on MLXVLM; runtime trampoline discovery is by design and unchanged\" — adding that import would violate a documented architecture boundary. Instead, the mapping is by content-shape: added `TranscriptConverter.entriesWithImages(for:)` (new static func) and `MLXLanguageModel.Executor.preparedInputMappingImageFailures(processor:input:messages:transcriptEntries:)` (new static func), which wraps both production call sites of `processor.prepare(input:)`. When `prepare` throws while this round's messages carry image content, it remaps to `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: <the transcript entries that carried images>, debugDescription: ...)`. Cancellation and already-typed `LanguageModelError`s pass through unchanged (never re-wrapped); a failure with NO image content in the round also passes through completely unchanged, so an unrelated tokenizer/config bug is never misrepresented as a vision problem.\n+\n+    **Tests added** (`Tests/MLXFoundationModelsTests/UnsupportedTranscriptContentMappingTests.swift`, following the fake-model/fake-processor idiom from `EagerFallbackPrepareOrderingTests.swift`/`ContextSizeValidationTests.swift`):\n+    - `imageProcessingFailureMapsToTypedError` — a `UserInputProcessor` that throws a generic error only when handed image input; asserts the failure surfaces as `LanguageModelError.unsupportedTranscriptContent` with exactly 1 entry in `unsupportedContent`.\n+    - `textOnlyProcessingFailurePassesThroughUnchanged` — a processor that always throws; with a text-only (no image) transcript, asserts the ORIGINAL generic error type surfaces unchanged, proving the remap is scoped precisely to image-bearing rounds.\n+\n+    **Verification:**\n+    - `swift build`: clean, exit 0. 14 pre-existing warnings unrelated to this change (verified via targeted grep — none touch `TranscriptConverter.swift`, the new `MLXLanguageModel.swift` code, or the new test file).\n+    - Mandated safe invocation: `xcodebuild build-for-testing -scheme mlx-swift-lm-Package -destination 'platform=macOS' -clonedSourcePackagesDirPath .build -disableAutomaticPackageResolution -skipPackagePluginValidation` → `** TEST BUILD SUCCEEDED **`.\n+    - Unfiltered `xcrun xctest <bundle>` per bundle, each wrapped in `timeout`:\n+      - `CXGrammarTests.xctest`: 7/7 passed.\n+      - `MLXGuidedGenerationTests.xctest`: 62/62 passed.\n+      - `MLXFoundationModelsTests.xctest`: 139/139 passed (includes the 2 new tests, plus all pre-existing suites incl. `VisionCapabilityGateTests`, `TranscriptConverterTests`, `ContextSizeValidationTests`, `EagerFallbackPrepareOrderingTests`, `GrammarError typed mapping`).\n+      - `MLXLMTests.xctest`: 245/245 passed.\n+\n+    Did not touch `.contextSizeExceeded` (task `4kxr1kt`'s territory) or the tool-calling path's `applyChatTemplate`-based tokenization (`runToolCalling` never threads images through `LMInput` at all today — a separate, pre-existing \"no vision+tool-calling\" limitation noted but out of scope here, not something this task asked to fix).\n+\n+    Left in `doing` for review per process.\n+  timestamp: 2026-07-09T12:05:22.111524+00:00\n position_column: doing\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,46 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01kx3c9b1zvhavy0jg1wxmdsst\n-  text: |-\n-    Investigated all 3 candidate cases. Implemented 1 of 3 (`.unsupportedTranscriptContent`); documented the other 2 as genuinely not applicable.\n-\n-    **Important correction to the task's premise:** this machine DOES now have a real os27 SDK/Xcode available (`xcodebuild -version` reports Xcode 27.0, `sw_vers` reports macOS 27.0, and `xcode-select -p` points at the installed Xcode). I used it to get ground truth instead of guessing:\n-    - `swiftc -typecheck` with a deliberately-incomplete `switch` over `LanguageModelError` prints the compiler's own \"add missing case\" diagnostics, giving the full, real case list: `.contextSizeExceeded`, `.rateLimited`, `.guardrailViolation`, `.refusal`, `.unsupportedCapability`, `.unsupportedTranscriptContent`, `.unsupportedGenerationGuide`, `.unsupportedLanguageOrLocale`, `.timeout` (+ `@unknown default`). No `assetsUnavailable`/`decodingFailure`/`concurrentRequests` exist — confirms the task's caution against guessing those was correct.\n-    - `swift-demangle` over `FoundationModels.tbd`'s exported symbols gave the real associated-value shapes. Confirmed via compile probes (not just reading symbol names) that only the *smaller*-arity public inits are actually callable (the `underlyingErrors:`-inclusive inits are exported at the ABI level but not part of the public Swift API surface — attempting to call them gives \"extra argument\" errors):\n-      - `LanguageModelError.RateLimited(resetDate: Date?, debugDescription: String)`\n-      - `LanguageModelError.Timeout(debugDescription: String)`\n-      - `LanguageModelError.UnsupportedTranscriptContent(unsupportedContent: [Transcript.Entry], debugDescription: String)` — note `unsupportedContent` is `[Transcript.Entry]`, **not** segments or `[UserInput.Image]`.\n-\n-    **1. `.rateLimited` — NOT implemented, genuinely not applicable.** Investigated concurrency handling: `PromptCache` (actor) and container loading both *queue/wait* for concurrent requests against the same model rather than *reject* them (see `PromptCache.swift` doc comments on slot pooling/actor serialization, and `ModelCache`'s per-model load de-dup). There is no code path anywhere in `MLXLanguageModel.swift`/`PromptCache.swift` that rejects a request because another is in flight. A fully local, in-process backend has no rate-limiter analog (no quota, no external service). Left unmapped per the task's own allowance.\n-\n-    **2. `.timeout` — NOT implemented, genuinely not applicable.** Grepped for any timeout/deadline concept: `GenerationOptions` (confirmed via the same SDK-probing technique) exposes only `maximumResponseTokens`, `sampling`/`samplingMode`, `temperature`, `toolCallingMode` — no deadline field. The only cancellation mechanism anywhere in the generation loop is cooperative `Task.checkCancellation()`/`catch is CancellationError` (`MLXLanguageModel.swift` respond(), `GuidedGenerationLoop.swift`), which fires for *any* reason the enclosing `Task` is cancelled (caller-initiated cancel, test harness time limit, etc.) — not specifically because a time budget was exceeded. Remapping raw `CancellationError` to `.timeout` would misrepresent arbitrary cancellations as timeouts, which is exactly the \"fake scenario\" the task warns against. No existing deadline parameter to hook a real mechanism to, so per the task's guidance I did not invent a new timeout feature. Left unmapped.\n-\n-    **3. `.unsupportedTranscriptContent` — IMPLEMENTED.** Traced the image path: `Transcript.ImageAttachment.ciImage` is a non-optional, non-throwing `CIImage` getter (confirmed via the SDK), and `UserInput.Image.asCIImage()` for our `.ciImage(...)` case just returns it directly — so the literal \"CIImage decode failure\" scenario the task description speculated about does **not** exist in this SDK; that door is already closed by the framework before we ever see the segment.\n-\n-    The *real* generic-error leak is one level downstream: `Executor.prepareRespondSetup`/`Executor.preparedInput` call `context.processor.prepare(input:)` (the concrete per-model `UserInputProcessor`, e.g. a VLM's own image tensor prep). When a model's own image-specific processing fails (mismatched image count, unsupported resolution, resize/decode failure — `MLXVLM.VLMError` and its per-architecture siblings), that error previously propagated fully opaque/untyped to the FoundationModels caller.\n-\n-    Deliberately did **not** add `import MLXVLM` to catch `VLMError` by type: `Package.swift` explicitly documents \"the MLXFoundationModels product target deliberately does NOT depend on MLXVLM; runtime trampoline discovery is by design and unchanged\" — adding that import would violate a documented architecture boundary. Instead, the mapping is by content-shape: added `TranscriptConverter.entriesWithImages(for:)` (new static func) and `MLXLanguageModel.Executor.preparedInputMappingImageFailures(processor:input:messages:transcriptEntries:)` (new static func), which wraps both production call sites of `processor.prepare(input:)`. When `prepare` throws while this round's messages carry image content, it remaps to `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: <the transcript entries that carried images>, debugDescription: ...)`. Cancellation and already-typed `LanguageModelError`s pass through unchanged (never re-wrapped); a failure with NO image content in the round also passes through completely unchanged, so an unrelated tokenizer/config bug is never misrepresented as a vision problem.\n-\n-    **Tests added** (`Tests/MLXFoundationModelsTests/UnsupportedTranscriptContentMappingTests.swift`, following the fake-model/fake-processor idiom from `EagerFallbackPrepareOrderingTests.swift`/`ContextSizeValidationTests.swift`):\n-    - `imageProcessingFailureMapsToTypedError` — a `UserInputProcessor` that throws a generic error only when handed image input; asserts the failure surfaces as `LanguageModelError.unsupportedTranscriptContent` with exactly 1 entry in `unsupportedContent`.\n-    - `textOnlyProcessingFailurePassesThroughUnchanged` — a processor that always throws; with a text-only (no image) transcript, asserts the ORIGINAL generic error type surfaces unchanged, proving the remap is scoped precisely to image-bearing rounds.\n-\n-    **Verification:**\n-    - `swift build`: clean, exit 0. 14 pre-existing warnings unrelated to this change (verified via targeted grep — none touch `TranscriptConverter.swift`, the new `MLXLanguageModel.swift` code, or the new test file).\n-    - Mandated safe invocation: `xcodebuild build-for-testing -scheme mlx-swift-lm-Package -destination 'platform=macOS' -clonedSourcePackagesDirPath .build -disableAutomaticPackageResolution -skipPackagePluginValidation` → `** TEST BUILD SUCCEEDED **`.\n-    - Unfiltered `xcrun xctest <bundle>` per bundle, each wrapped in `timeout`:\n-      - `CXGrammarTests.xctest`: 7/7 passed.\n-      - `MLXGuidedGenerationTests.xctest`: 62/62 passed.\n-      - `MLXFoundationModelsTests.xctest`: 139/139 passed (includes the 2 new tests, plus all pre-existing suites incl. `VisionCapabilityGateTests`, `TranscriptConverterTests`, `ContextSizeValidationTests`, `EagerFallbackPrepareOrderingTests`, `GrammarError typed mapping`).\n-      - `MLXLMTests.xctest`: 245/245 passed.\n-\n-    Did not touch `.contextSizeExceeded` (task `4kxr1kt`'s territory) or the tool-calling path's `applyChatTemplate`-based tokenization (`runToolCalling` never threads images through `LMInput` at all today — a separate, pre-existing \"no vision+tool-calling\" limitation noted but out of scope here, not something this task asked to fix).\n-\n-    Left in `doing` for review per process.\n-  timestamp: 2026-07-09T12:05:22.111524+00:00\n position_column: doing\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n","transaction_id":null}
{"id":"01KX3CXTDCSB27D0HVKJJ4BTWZ","timestamp":"2026-07-09T12:16:33.196996Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -41,6 +41,23 @@\n\n     Left in `doing` for review per process.\n   timestamp: 2026-07-09T12:05:22.111524+00:00\n+- actor: claude-code\n+  id: 01kx3cxtdcck3nyv888c8d812k\n+  text: |-\n+    Adversarial double-check ran (per really-done) and returned REVISE with 2 findings. Addressed both:\n+\n+    **Finding 1 (moderate): doc comment said \"this round\" but `messages` is actually the FULL transcript re-rendered every call** (the `LanguageModelExecutor` protocol has no session identity — every `respond()` receives the complete history again). So an unrelated failure on a later, text-only turn could still get remapped to `.unsupportedTranscriptContent` merely because an *earlier* turn carried an image.\n+\n+    Investigated whether to narrow the check (e.g. only look at the newest transcript entry) and concluded that would make things *worse*, not better: a real VLM failure mode (`VLMError.singleImageAllowed`-shaped rejection when a second/earlier image is still in scope) would fire precisely when the newest turn has NO new image, so checking only the newest entry would cause us to miss genuine image-caused failures. Since the processor re-processes every image referenced across the whole rendered prompt on every call, \"an image is present anywhere in what's being prepared\" is the accurate correlate of \"this failure could be image-related\" — not a bug, but the necessary consequence of mapping by content-shape instead of catching `MLXVLM.VLMError` directly (which the architecture forbids, see task description/Package.swift).\n+\n+    Fixed the doc comment on `preparedInputMappingImageFailures` to state this precisely (dropped the misleading \"this round\" framing, added an explicit paragraph naming the tradeoff and reasoning through the `singleImageAllowed` counterexample), and added a third test, `earlierTurnImageStillMapsLaterTextOnlyFailure`, that constructs a 3-entry transcript (image-bearing first turn, assistant response, text-only second turn) and asserts the second turn's unrelated failure still surfaces as `.unsupportedTranscriptContent` — turning the previously-undocumented behavior into an explicit, tested, intentional contract.\n+\n+    **Finding 2 (minor/process): `Tests/MLXFoundationModelsTests/PromptCacheConcurrencyTests.swift` appeared in `git diff --stat` but wasn't disclosed.** Confirmed this is pre-existing uncommitted state that was already in the working tree before this session started (a `ConcurrencyProbeModel` → shared `PromptCacheProbeModel` test dedupe) — I never opened or edited that file. Disclosing explicitly here so the orchestrator doesn't attribute it to this task when committing; it's unrelated to `unsupportedTranscriptContent` and should be reviewed/committed separately (or was left mid-flight by a prior agent).\n+\n+    **Re-verified after the fix:** `swift build` clean; mandated safe invocation (`xcodebuild build-for-testing ...` then unfiltered `xcrun xctest <bundle>` per bundle, each under `timeout`) — all 4 bundles green: CXGrammarTests 7/7, MLXGuidedGenerationTests 62/62, MLXFoundationModelsTests 141/141 (now 3 tests in the new suite), MLXLMTests 245/245.\n+\n+    Task remains implemented as described in the prior comment (`.unsupportedTranscriptContent` mapped; `.rateLimited`/`.timeout` investigated and left unmapped as genuinely not applicable). Left in `doing` for review.\n+  timestamp: 2026-07-09T12:16:33.196494+00:00\n position_column: doing\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n","reverse_patch":"--- original\n+++ modified\n@@ -41,23 +41,6 @@\n\n     Left in `doing` for review per process.\n   timestamp: 2026-07-09T12:05:22.111524+00:00\n-- actor: claude-code\n-  id: 01kx3cxtdcck3nyv888c8d812k\n-  text: |-\n-    Adversarial double-check ran (per really-done) and returned REVISE with 2 findings. Addressed both:\n-\n-    **Finding 1 (moderate): doc comment said \"this round\" but `messages` is actually the FULL transcript re-rendered every call** (the `LanguageModelExecutor` protocol has no session identity — every `respond()` receives the complete history again). So an unrelated failure on a later, text-only turn could still get remapped to `.unsupportedTranscriptContent` merely because an *earlier* turn carried an image.\n-\n-    Investigated whether to narrow the check (e.g. only look at the newest transcript entry) and concluded that would make things *worse*, not better: a real VLM failure mode (`VLMError.singleImageAllowed`-shaped rejection when a second/earlier image is still in scope) would fire precisely when the newest turn has NO new image, so checking only the newest entry would cause us to miss genuine image-caused failures. Since the processor re-processes every image referenced across the whole rendered prompt on every call, \"an image is present anywhere in what's being prepared\" is the accurate correlate of \"this failure could be image-related\" — not a bug, but the necessary consequence of mapping by content-shape instead of catching `MLXVLM.VLMError` directly (which the architecture forbids, see task description/Package.swift).\n-\n-    Fixed the doc comment on `preparedInputMappingImageFailures` to state this precisely (dropped the misleading \"this round\" framing, added an explicit paragraph naming the tradeoff and reasoning through the `singleImageAllowed` counterexample), and added a third test, `earlierTurnImageStillMapsLaterTextOnlyFailure`, that constructs a 3-entry transcript (image-bearing first turn, assistant response, text-only second turn) and asserts the second turn's unrelated failure still surfaces as `.unsupportedTranscriptContent` — turning the previously-undocumented behavior into an explicit, tested, intentional contract.\n-\n-    **Finding 2 (minor/process): `Tests/MLXFoundationModelsTests/PromptCacheConcurrencyTests.swift` appeared in `git diff --stat` but wasn't disclosed.** Confirmed this is pre-existing uncommitted state that was already in the working tree before this session started (a `ConcurrencyProbeModel` → shared `PromptCacheProbeModel` test dedupe) — I never opened or edited that file. Disclosing explicitly here so the orchestrator doesn't attribute it to this task when committing; it's unrelated to `unsupportedTranscriptContent` and should be reviewed/committed separately (or was left mid-flight by a prior agent).\n-\n-    **Re-verified after the fix:** `swift build` clean; mandated safe invocation (`xcodebuild build-for-testing ...` then unfiltered `xcrun xctest <bundle>` per bundle, each under `timeout`) — all 4 bundles green: CXGrammarTests 7/7, MLXGuidedGenerationTests 62/62, MLXFoundationModelsTests 141/141 (now 3 tests in the new suite), MLXLMTests 245/245.\n-\n-    Task remains implemented as described in the prior comment (`.unsupportedTranscriptContent` mapped; `.rateLimited`/`.timeout` investigated and left unmapped as genuinely not applicable). Left in `doing` for review.\n-  timestamp: 2026-07-09T12:16:33.196494+00:00\n position_column: doing\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n","transaction_id":null}
{"id":"01KX3HAYG5F7GG99BYB4EVZVCF","timestamp":"2026-07-09T13:33:37.669995Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -62,15 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\n-\n-**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\n-\n-- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\n-- `LanguageModelError.timeout(debugDescription: String)`\n-- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\n-- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\n-\n-I do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\n-\n-Scope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\\n\\n- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\\n- `LanguageModelError.timeout(debugDescription: String)`\\n- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\\n- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\\n\\nI do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\\n\\nScope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated all 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply — no rejection-based concurrency limiting or deadline mechanism exists in this fully-local backend (correct, intended outcome per the task's own conservative design). `.unsupportedTranscriptContent` implemented: `TranscriptConverter.entriesWithImages(for:)` + `Executor.preparedInputMappingImageFailures(...)`, wired into both `UserInputProcessor.prepare` call sites, mapping a local image-processing failure to the typed case while passing through unrelated errors (cancellation, already-typed errors, text-only failures) unchanged. 3 new tests. Commit `cdce44a`: added missing `private` to the new helper per review.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInputMappingImageFailures`/`preparedInput` should use imperative verb form (`prepareInput...`) not past-participle. **REJECTED** — Swift's own idiom uses past-participle naming for non-mutating functions that compute and return a transformed value (`sorted()`, `reversed()`, `shuffled()`), exactly matching this file's own already-established `clampedTemperature` (also past-participle, also non-mutating/value-returning). Renaming these two while leaving `clampedTemperature` alone would be internally inconsistent; renaming all three would fight the same established Swift convention already used throughout this codebase.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` (single-call-site \\\"needless indirection\\\") — **RESOLVED AT THE RULE LEVEL**: this was the `reuse` validator flagging single-call-site helpers after its \\\"rule of three\\\" carve-out was removed earlier (to fix an unrelated contradiction with the `complexity` validator on `processReasoningToken`). Removing that carve-out had the side effect of making `reuse` flag every single-call-site helper repo-wide, including several extracted in earlier rounds specifically to satisfy `complexity`/`function-length`. User directed a permanent rule-level fix: `/Users/wballard/.validators/reuse/VALIDATOR.md` and `rules/reuse.md` updated to drop \\\"needless single-call-site helper\\\" from the validator's scope entirely, with an explicit new carve-out stating single-call-site helpers are never a reuse violation. No code changes needed — `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` stay exactly as they are.\\n- [x] Mark `clampedTemperature`/`mapGrammarError` `private` — **REJECTED**: both are called directly by name from `Tests/MLXFoundationModelsTests/MLXLanguageModelTests.swift` via `@testable import`. `private` is file-scoped and is NOT bypassed by `@testable import` (unlike `internal`), so marking either private would break that test file's compile. Confirmed via grep before rejecting.\\n- [x] Mark `makeParameters` `private` — **FIXING**: confirmed via grep no test file calls it directly; safe, consistent with the access-control tightening already applied to `preparedInputMappingImageFailures`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,4 +62,15 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\\n\\n- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\\n- `LanguageModelError.timeout(debugDescription: String)`\\n- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\\n- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\\n\\nI do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\\n\\nScope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated all 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply — no rejection-based concurrency limiting or deadline mechanism exists in this fully-local backend (correct, intended outcome per the task's own conservative design). `.unsupportedTranscriptContent` implemented: `TranscriptConverter.entriesWithImages(for:)` + `Executor.preparedInputMappingImageFailures(...)`, wired into both `UserInputProcessor.prepare` call sites, mapping a local image-processing failure to the typed case while passing through unrelated errors (cancellation, already-typed errors, text-only failures) unchanged. 3 new tests. Commit `cdce44a`: added missing `private` to the new helper per review.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInputMappingImageFailures`/`preparedInput` should use imperative verb form (`prepareInput...`) not past-participle. **REJECTED** — Swift's own idiom uses past-participle naming for non-mutating functions that compute and return a transformed value (`sorted()`, `reversed()`, `shuffled()`), exactly matching this file's own already-established `clampedTemperature` (also past-participle, also non-mutating/value-returning). Renaming these two while leaving `clampedTemperature` alone would be internally inconsistent; renaming all three would fight the same established Swift convention already used throughout this codebase.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` (single-call-site \\\"needless indirection\\\") — **RESOLVED AT THE RULE LEVEL**: this was the `reuse` validator flagging single-call-site helpers after its \\\"rule of three\\\" carve-out was removed earlier (to fix an unrelated contradiction with the `complexity` validator on `processReasoningToken`). Removing that carve-out had the side effect of making `reuse` flag every single-call-site helper repo-wide, including several extracted in earlier rounds specifically to satisfy `complexity`/`function-length`. User directed a permanent rule-level fix: `/Users/wballard/.validators/reuse/VALIDATOR.md` and `rules/reuse.md` updated to drop \\\"needless single-call-site helper\\\" from the validator's scope entirely, with an explicit new carve-out stating single-call-site helpers are never a reuse violation. No code changes needed — `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` stay exactly as they are.\\n- [x] Mark `clampedTemperature`/`mapGrammarError` `private` — **REJECTED**: both are called directly by name from `Tests/MLXFoundationModelsTests/MLXLanguageModelTests.swift` via `@testable import`. `private` is file-scoped and is NOT bypassed by `@testable import` (unlike `internal`), so marking either private would break that test file's compile. Confirmed via grep before rejecting.\\n- [x] Mark `makeParameters` `private` — **FIXING**: confirmed via grep no test file calls it directly; safe, consistent with the access-control tightening already applied to `preparedInputMappingImageFailures`.\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\n+\n+**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\n+\n+- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\n+- `LanguageModelError.timeout(debugDescription: String)`\n+- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\n+- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\n+\n+I do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\n+\n+Scope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KX3KT6TFJ5K5VC2QVEVWMM45","timestamp":"2026-07-09T14:16:54.863859Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -62,4 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\\n\\n- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\\n- `LanguageModelError.timeout(debugDescription: String)`\\n- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\\n- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\\n\\nI do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\\n\\nScope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated all 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply — no rejection-based concurrency limiting or deadline mechanism exists in this fully-local backend (correct, intended outcome per the task's own conservative design). `.unsupportedTranscriptContent` implemented: `TranscriptConverter.entriesWithImages(for:)` + `Executor.preparedInputMappingImageFailures(...)`, wired into both `UserInputProcessor.prepare` call sites, mapping a local image-processing failure to the typed case while passing through unrelated errors (cancellation, already-typed errors, text-only failures) unchanged. 3 new tests. Commit `cdce44a`: added missing `private` to the new helper per review.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInputMappingImageFailures`/`preparedInput` should use imperative verb form (`prepareInput...`) not past-participle. **REJECTED** — Swift's own idiom uses past-participle naming for non-mutating functions that compute and return a transformed value (`sorted()`, `reversed()`, `shuffled()`), exactly matching this file's own already-established `clampedTemperature` (also past-participle, also non-mutating/value-returning). Renaming these two while leaving `clampedTemperature` alone would be internally inconsistent; renaming all three would fight the same established Swift convention already used throughout this codebase.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` (single-call-site \\\"needless indirection\\\") — **RESOLVED AT THE RULE LEVEL**: this was the `reuse` validator flagging single-call-site helpers after its \\\"rule of three\\\" carve-out was removed earlier (to fix an unrelated contradiction with the `complexity` validator on `processReasoningToken`). Removing that carve-out had the side effect of making `reuse` flag every single-call-site helper repo-wide, including several extracted in earlier rounds specifically to satisfy `complexity`/`function-length`. User directed a permanent rule-level fix: `/Users/wballard/.validators/reuse/VALIDATOR.md` and `rules/reuse.md` updated to drop \\\"needless single-call-site helper\\\" from the validator's scope entirely, with an explicit new carve-out stating single-call-site helpers are never a reuse violation. No code changes needed — `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` stay exactly as they are.\\n- [x] Mark `clampedTemperature`/`mapGrammarError` `private` — **REJECTED**: both are called directly by name from `Tests/MLXFoundationModelsTests/MLXLanguageModelTests.swift` via `@testable import`. `private` is file-scoped and is NOT bypassed by `@testable import` (unlike `internal`), so marking either private would break that test file's compile. Confirmed via grep before rejecting.\\n- [x] Mark `makeParameters` `private` — **FIXING**: confirmed via grep no test file calls it directly; safe, consistent with the access-control tightening already applied to `preparedInputMappingImageFailures`.\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commit `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInput*` naming — REJECTED, matches Swift's own past-participle convention for value-returning functions, consistent with this file's established `clampedTemperature`.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` — RESOLVED AT THE RULE LEVEL (user updated the `reuse` validator directly).\\n- [x] Mark `clampedTemperature`/`mapGrammarError` private — REJECTED, both called directly from `MLXLanguageModelTests.swift` via `@testable import`; `private` is not bypassed by `@testable`, would break the test build.\\n- [x] Mark `makeParameters` private — FIXED, `28286ce`.\\n\\n## Review Findings (2026-07-09 08:38)\\n\\n- [x] `prewarm()` uses unstructured `Task { }` despite its doc comment saying \\\"spawns a detached warmup Task\\\" — should be `Task.detached { }`. **DEFERRED to `^9jtbtkd`** — confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,4 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commit `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInput*` naming — REJECTED, matches Swift's own past-participle convention for value-returning functions, consistent with this file's established `clampedTemperature`.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` — RESOLVED AT THE RULE LEVEL (user updated the `reuse` validator directly).\\n- [x] Mark `clampedTemperature`/`mapGrammarError` private — REJECTED, both called directly from `MLXLanguageModelTests.swift` via `@testable import`; `private` is not bypassed by `@testable`, would break the test build.\\n- [x] Mark `makeParameters` private — FIXED, `28286ce`.\\n\\n## Review Findings (2026-07-09 08:38)\\n\\n- [x] `prewarm()` uses unstructured `Task { }` despite its doc comment saying \\\"spawns a detached warmup Task\\\" — should be `Task.detached { }`. **DEFERRED to `^9jtbtkd`** — confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n**Confirmed real cases** (verified against Anthropic's own `ClaudeForFoundationModels` provider, which ships against the real os27 SDK — see `Sources/ClaudeForFoundationModels/ErrorMapper.swift` and `ClaudeError.swift` at https://github.com/anthropics/ClaudeForFoundationModels):\\n\\n- `LanguageModelError.rateLimited(resetDate: Date?, debugDescription: String)` — pass `resetDate: nil` when there's no real reset time to report (mirrors Anthropic's own usage, which doesn't fabricate one either).\\n- `LanguageModelError.timeout(debugDescription: String)`\\n- `LanguageModelError.unsupportedTranscriptContent(unsupportedContent: [...], debugDescription: String)` — Anthropic throws this when an image attachment can't be prepared for upload; for MLX the analogous case is an image the local vision pipeline can't decode/process.\\n- `LanguageModelError.contextSizeExceeded(...)` is tracked separately in task `01KWW4Z2JTSYTSSFVSM4KXR1KT` — don't duplicate it here.\\n\\nI do not have confirmed evidence for the other case names previously listed here (`assetsUnavailable`, `guardrailViolation`, `unsupportedLanguageOrLocale`, `decodingFailure`, `concurrentRequests`) — Anthropic's reference implementation doesn't exercise them, and no local FoundationModels SDK/swiftinterface is available on this machine to verify the full enum. Do not guess at those names or invent cases; if a real failure mode needs one of them, check `LanguageModelError`'s actual case list on a machine with the os27 SDK/Xcode 27 installed before adding it. For a genuine local failure that doesn't map onto any real `LanguageModelError` case (e.g. model weights failing to load), follow Anthropic's own pattern: define a small MLX-specific error enum (their `ClaudeError.missingCredential` is the analog) rather than forcing it into a framework case that doesn't fit.\\n\\nScope this task to: map rate-limit-shaped failures (if any exist in the local generation path — may not apply to a fully local backend) to `.rateLimited`, generation timeouts to `.timeout`, and unprocessable tool-calling/vision image input to `.unsupportedTranscriptContent`. Add tests asserting each mapped failure surfaces as its corresponding typed case rather than a generic error.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated all 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply — no rejection-based concurrency limiting or deadline mechanism exists in this fully-local backend (correct, intended outcome per the task's own conservative design). `.unsupportedTranscriptContent` implemented: `TranscriptConverter.entriesWithImages(for:)` + `Executor.preparedInputMappingImageFailures(...)`, wired into both `UserInputProcessor.prepare` call sites, mapping a local image-processing failure to the typed case while passing through unrelated errors (cancellation, already-typed errors, text-only failures) unchanged. 3 new tests. Commit `cdce44a`: added missing `private` to the new helper per review.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInputMappingImageFailures`/`preparedInput` should use imperative verb form (`prepareInput...`) not past-participle. **REJECTED** — Swift's own idiom uses past-participle naming for non-mutating functions that compute and return a transformed value (`sorted()`, `reversed()`, `shuffled()`), exactly matching this file's own already-established `clampedTemperature` (also past-participle, also non-mutating/value-returning). Renaming these two while leaving `clampedTemperature` alone would be internally inconsistent; renaming all three would fight the same established Swift convention already used throughout this codebase.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` (single-call-site \\\"needless indirection\\\") — **RESOLVED AT THE RULE LEVEL**: this was the `reuse` validator flagging single-call-site helpers after its \\\"rule of three\\\" carve-out was removed earlier (to fix an unrelated contradiction with the `complexity` validator on `processReasoningToken`). Removing that carve-out had the side effect of making `reuse` flag every single-call-site helper repo-wide, including several extracted in earlier rounds specifically to satisfy `complexity`/`function-length`. User directed a permanent rule-level fix: `/Users/wballard/.validators/reuse/VALIDATOR.md` and `rules/reuse.md` updated to drop \\\"needless single-call-site helper\\\" from the validator's scope entirely, with an explicit new carve-out stating single-call-site helpers are never a reuse violation. No code changes needed — `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` stay exactly as they are.\\n- [x] Mark `clampedTemperature`/`mapGrammarError` `private` — **REJECTED**: both are called directly by name from `Tests/MLXFoundationModelsTests/MLXLanguageModelTests.swift` via `@testable import`. `private` is file-scoped and is NOT bypassed by `@testable import` (unlike `internal`), so marking either private would break that test file's compile. Confirmed via grep before rejecting.\\n- [x] Mark `makeParameters` `private` — **FIXING**: confirmed via grep no test file calls it directly; safe, consistent with the access-control tightening already applied to `preparedInputMappingImageFailures`.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KX3M9VSWPK9Q1BRS4P54TWP5","timestamp":"2026-07-09T14:25:27.868339Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -62,4 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commit `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInput*` naming — REJECTED, matches Swift's own past-participle convention for value-returning functions, consistent with this file's established `clampedTemperature`.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` — RESOLVED AT THE RULE LEVEL (user updated the `reuse` validator directly).\\n- [x] Mark `clampedTemperature`/`mapGrammarError` private — REJECTED, both called directly from `MLXLanguageModelTests.swift` via `@testable import`; `private` is not bypassed by `@testable`, would break the test build.\\n- [x] Mark `makeParameters` private — FIXED, `28286ce`.\\n\\n## Review Findings (2026-07-09 08:38)\\n\\n- [x] `prewarm()` uses unstructured `Task { }` despite its doc comment saying \\\"spawns a detached warmup Task\\\" — should be `Task.detached { }`. **DEFERRED to `^9jtbtkd`** — confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44) / (2026-07-09 08:38) / (2026-07-09 09:16)\\n\\nAll FIXED (naming rejected per Swift convention, private access-control applied where safe), or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt: `makeConstraint` json/structuralTag duplication, `validateReasoningCapability`/`preparedInput` do-catch duplication, `storePromptCache` call duplication, 4-level generation-loop nesting, `prewarm()`'s `Task` vs `Task.detached`), or resolved at the rule level (single-call-site inlining findings — you updated the `reuse` validator directly).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,4 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44) / (2026-07-09 08:38) / (2026-07-09 09:16)\\n\\nAll FIXED (naming rejected per Swift convention, private access-control applied where safe), or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt: `makeConstraint` json/structuralTag duplication, `validateReasoningCapability`/`preparedInput` do-catch duplication, `storePromptCache` call duplication, 4-level generation-loop nesting, `prewarm()`'s `Task` vs `Task.detached`), or resolved at the rule level (single-call-site inlining findings — you updated the `reuse` validator directly).\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commit `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44)\\n\\n- [x] `preparedInput*` naming — REJECTED, matches Swift's own past-participle convention for value-returning functions, consistent with this file's established `clampedTemperature`.\\n- [x] Inline `mapGrammarError`/`resolvePhase2MaxTokens`/`isTextOnly` — RESOLVED AT THE RULE LEVEL (user updated the `reuse` validator directly).\\n- [x] Mark `clampedTemperature`/`mapGrammarError` private — REJECTED, both called directly from `MLXLanguageModelTests.swift` via `@testable import`; `private` is not bypassed by `@testable`, would break the test build.\\n- [x] Mark `makeParameters` private — FIXED, `28286ce`.\\n\\n## Review Findings (2026-07-09 08:38)\\n\\n- [x] `prewarm()` uses unstructured `Task { }` despite its doc comment saying \\\"spawns a detached warmup Task\\\" — should be `Task.detached { }`. **DEFERRED to `^9jtbtkd`** — confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KX3MNA22FXKJZQARV7ENFGYZ","timestamp":"2026-07-09T14:31:42.914185Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -62,4 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44) / (2026-07-09 08:38) / (2026-07-09 09:16)\\n\\nAll FIXED (naming rejected per Swift convention, private access-control applied where safe), or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt: `makeConstraint` json/structuralTag duplication, `validateReasoningCapability`/`preparedInput` do-catch duplication, `storePromptCache` call duplication, 4-level generation-loop nesting, `prewarm()`'s `Task` vs `Task.detached`), or resolved at the rule level (single-call-site inlining findings — you updated the `reuse` validator directly).\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings, rounds 1-4 (2026-07-09 07:44 through 09:16)\\n\\nAll FIXED, or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt), or resolved at the rule level (`reuse` validator updated directly).\\n\\n## Review Findings (2026-07-09 09:25) — round 5\\n\\n- [x] Duplicated reasoning error message (recurring) — **DEFERRED**, already tracked in `^9jtbtkd`, confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,4 +62,4 @@\n position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n-Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings, rounds 1-4 (2026-07-09 07:44 through 09:16)\\n\\nAll FIXED, or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt), or resolved at the rule level (`reuse` validator updated directly).\\n\\n## Review Findings (2026-07-09 09:25) — round 5\\n\\n- [x] Duplicated reasoning error message (recurring) — **DEFERRED**, already tracked in `^9jtbtkd`, confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n+Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings (2026-07-09 07:44) / (2026-07-09 08:38) / (2026-07-09 09:16)\\n\\nAll FIXED (naming rejected per Swift convention, private access-control applied where safe), or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt: `makeConstraint` json/structuralTag duplication, `validateReasoningCapability`/`preparedInput` do-catch duplication, `storePromptCache` call duplication, 4-level generation-loop nesting, `prewarm()`'s `Task` vs `Task.detached`), or resolved at the rule level (single-call-site inlining findings — you updated the `reuse` validator directly).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KX3N0RGTA9BBB858SCYDSX14","timestamp":"2026-07-09T14:37:58.170048Z","op":"update","item_id":"01KWW4Z8JGWMFAV4V6ENZBWJGD","forward_patch":"--- original\n+++ modified\n@@ -58,8 +58,8 @@\n\n     Task remains implemented as described in the prior comment (`.unsupportedTranscriptContent` mapped; `.rateLimited`/`.timeout` investigated and left unmapped as genuinely not applicable). Left in `doing` for review.\n   timestamp: 2026-07-09T12:16:33.196494+00:00\n-position_column: doing\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: '8680'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings, rounds 1-4 (2026-07-09 07:44 through 09:16)\\n\\nAll FIXED, or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt), or resolved at the rule level (`reuse` validator updated directly).\\n\\n## Review Findings (2026-07-09 09:25) — round 5\\n\\n- [x] Duplicated reasoning error message (recurring) — **DEFERRED**, already tracked in `^9jtbtkd`, confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -58,8 +58,8 @@\n\n     Task remains implemented as described in the prior comment (`.unsupportedTranscriptContent` mapped; `.rateLimited`/`.timeout` investigated and left unmapped as genuinely not applicable). Left in `doing` for review.\n   timestamp: 2026-07-09T12:16:33.196494+00:00\n-position_column: done\n-position_ordinal: '8680'\n+position_column: doing\n+position_ordinal: '80'\n title: Expand MLXLanguageModel's error taxonomy to cover confirmed LanguageModelError cases (rateLimited, timeout, unsupportedTranscriptContent)\n ---\n Libraries/MLXFoundationModels/MLXLanguageModel.swift currently only ever throws `LanguageModelError.unsupportedCapability` and `.unsupportedGenerationGuide` (confirmed via grep — no other case is used anywhere in the file). Real FoundationModels callers pattern-match on the framework's typed `LanguageModelError` cases; failures outside these two categories leak out as opaque/untyped errors.\\n\\n## Resolution (2026-07-09)\\n\\nCommit `f402e64`: investigated 3 candidate cases. `.rateLimited`/`.timeout` genuinely don't apply to this fully-local backend (correct, intended outcome). `.unsupportedTranscriptContent` implemented for local image-processing failures. Commits `cdce44a`/`28286ce`: access-control fixes.\\n\\n## Review Findings, rounds 1-4 (2026-07-09 07:44 through 09:16)\\n\\nAll FIXED, or DEFERRED to `^9jtbtkd` (confirmed pre-existing/untouched debt), or resolved at the rule level (`reuse` validator updated directly).\\n\\n## Review Findings (2026-07-09 09:25) — round 5\\n\\n- [x] Duplicated reasoning error message (recurring) — **DEFERRED**, already tracked in `^9jtbtkd`, confirmed pre-existing/untouched by any of this task's commits.\n\\ No newline at end of file\n","transaction_id":null}
