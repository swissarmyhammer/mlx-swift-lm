---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kx1eqyhtyv4a5xxfgqymp0jt
  text: Reconciled with the duplicate task qeqw5r3 ("Wire cross-turn KV cache in MLXLanguageModel.Executor", session-keyed design) — user chose this task's token-prefix cache design over that one. qeqw5r3 deleted as superseded. Proceeding to implement this task's design via /finish.
  timestamp: 2026-07-08T18:09:49.114439+00:00
- actor: claude-code
  id: 01kx1j1tkbntfktxeny6pjagx9
  text: |-
    Implemented the token-prefix `PromptCache` design end-to-end. Summary:

    **New file `Libraries/MLXFoundationModels/PromptCache.swift`**: `actor PromptCache` mirroring `ModelCache`'s pattern — `private var entries: [String: (tokens: [Int], cache: [KVCache])]`, keyed by `modelID`. Two pure, unit-tested static functions live on it:
    - `decide(cachedTokens:newTokens:isTrimmable:) -> Decision` (`.reuseSuffix(count:)` / `.trimTo(commonPrefixLength:thenSuffix:)` / `.rebuild`) — the prefix-match/trim/rebuild decision.
    - `reconcileGeneratedTokens(reencoded:actualGeneratedCount:) -> [Int]?` — added after a double-check review caught a real bug (see below).

    `resolve(modelID:newTokens:model:parameters:)` **removes** (not just reads) the model's entry before handing out its `[KVCache]`: `KVCache` classes have no internal synchronization, so two concurrent `respond()` calls for the same model must never alias the same instances — removing means a second concurrent caller finds nothing and safely rebuilds. Swift 6 strict concurrency also required boxing the non-Sendable `any LanguageModel`/`[KVCache]` across the actor boundary via `MLXLMCommon`'s existing `package`-visibility `SendableBox` (same idiom already used elsewhere in this file) — plain `sending` parameters weren't enough because the actor's own stored dictionary retained a reference alongside what was being "sent" out.

    **Wiring** (`Executor` in `MLXLanguageModel.swift`): added `isTextOnly(_:)`, `PromptCacheSlot`, `makePromptCacheSlot`, and two `commitPromptCache` overloads (exact token IDs vs. re-encoded text). Wired into all three explicitly-named paths (`runUnconstrained` → `generate`, `runReasoning` → `generateTokens`, `runToolCallReasoningPhase` → `generateTokensTask`) **and** the grammar-constrained path, after investigating `GuidedGenerationLoop.run`: it turned out feasible with a minimal change — added `cache: [KVCache]? = nil` (default preserves every existing call site) and changed line 102 from `model.newCache(parameters: nil)` to `cache ?? model.newCache(parameters: nil)`. `runGuidedGenerationLoop` (shared by both `runGuidedGeneration` and `executeToolCallingPhase2`) now resolves/feeds/commits through the same `PromptCache`, so tool-call continuation rounds do get cache reuse, addressing that acceptance criterion. Multimodal (image/video/audio) rounds are deliberately excluded from the cache path entirely (`isTextOnly`) — reducing `LMInput` to a raw token suffix would silently drop attached media with no way to re-attach it; those rounds fall through to the original, unreduced full-input/no-cache behavior unchanged.

    **Correctness discipline**: since `KVCache.offset` is the only ground truth for what a cache actually holds, `commitPromptCache` always reconciles the round's generated tokens against `cache.first?.offset` before storing, and drops (`removePromptCache`) rather than trusts a mismatch. `runReasoning`/`runToolCallReasoningPhase` get exact token IDs from their raw `.token(Int)` streams (verified via `TokenIterator.next()`'s feed-then-return ordering — the returned token has already been fed into the cache, so no off-by-one there). `runUnconstrained`/`runGuidedGenerationLoop` only have decoded text, so they re-encode it via the tokenizer and gate on a count match.

    **Bug caught by adversarial double-check, fixed**: the first version's fidelity check required an *exact* count match, but `GuidedGenerationLoop.run`'s natural (grammar-accepted) termination path commits/detokenizes/counts its terminal token *before* checking `commitResult.isTerminated` and breaking — that last token is never fed back through the model, so `cache.offset` legitimately lands one token short on every normal successful completion. The exact-match check was silently dropping the cache entry on essentially every successful guided-generation/tool-calling round, defeating that acceptance criterion. Fixed by extracting `PromptCache.reconcileGeneratedTokens`, which also accepts a count of `actualGeneratedCount + 1` and drops the reconstruction's trailing token in that case (verified this is the *only* mismatch scenario by reading every break site in `GuidedGenerationLoop.run`: `mask.isTerminated`, the EOS check, and the `emit`-returns-false break are all self-consistent already). Added 4 new unit tests for this function (`PromptCacheReconciliationTests`).

    **Tests**: `Tests/MLXFoundationModelsTests/PromptCacheTests.swift` — 8 tests for `decide` (first-call rebuild, exact-prefix reuse with/without trimmability, identical-sequence zero-suffix reuse, divergence trim/rebuild, divergence at token 0, shorter-new-tokens trim) + 4 for `reconcileGeneratedTokens`. All written RED-first, watched fail, then implemented.

    **Integration test**: `IntegrationTesting/.../TextGeneration/PromptCacheReuseTests.swift` — two-round `respond()` test asserting the second round's `.updateUsage` prompt-token-count is smaller than the first's. Written and code-reviewed as correct, but **could not be executed to a passing result in this sandbox**: the entire `IntegrationTesting` xcodeproj target fails to compile on this environment's Xcode-beta/FoundationModels SDK, independent of this change — verified via `git stash` (fully reverting this diff still fails the same way) in multiple pre-existing, unrelated files (`PrewarmGrammarTests.swift`, `PlainChatGenerationTests.swift`, `StreamingDeltaTests.swift`) all using the identical `if case .appendText(let x) = response.action` pattern, which no longer resolves against this SDK's `Response.Action` (an opaque struct with static factory methods, not a plain enum in this build). This is a pre-existing SDK-compatibility break unrelated to prompt caching.

    **Verification**: `swift build` clean. Full suite via the mandated safe pattern (`xcodebuild build-for-testing` + `xcrun xctest` per bundle, no `-XCTest` filtering, each wrapped in `timeout`): CXGrammarTests 7/7, MLXFoundationModelsTests 96/96, MLXGuidedGenerationTests 62/62, MLXLMTests 244/244 — 409 tests, 0 failures, no regressions. Adversarial double-check run via the `double-check` agent; found the offset-reconciliation bug above (fixed and re-verified), plus one informational note (a thrown error/cancellation mid-round forfeits that round's cache store, which is safe — the entry was already removed on `resolve()` — but means the *next* round also rebuilds; no code change needed, this matches the actor's documented remove-on-read design).

    Not committing per instructions — leaving in `doing` for review.
  timestamp: 2026-07-08T19:07:38.475186+00:00
- actor: claude-code
  id: 01kx1kyf3yxwe9xy0dw8xgme0t
  text: 'User decision: take all of the Fable architectural review''s recommendations, including the multi-slot LCP-matching capability (not deferred). Full review preserved below verbatim for implementation reference.'
- actor: claude-code
  id: 01kx1vssvabzrbddxkmknpt731
  text: |-
    Implemented the full Fable architectural review (all 6 numbered fixes, including the multi-slot LCP redesign, per the user's decision in the prior comment). Summary:

    **1. Multi-slot LCP redesign (`Libraries/MLXFoundationModels/PromptCache.swift`, full rewrite)**

    Data structure: `actor PromptCache { private var entries: [String: [Slot]] }` where `Slot { tokens: [Int]; cache: [KVCache]; lastUsed: Int }`. Capped at `maxSlotsPerModel = 4`.

    Deviation from the review's exact suggestion: `lastUsed` is a plain monotonically-increasing `Int` counter (`nextRecency()`), not `ContinuousClock.Instant`. Checked the codebase for an existing injectable-clock/testable-timestamp pattern first (per the task's own instruction) — found none. Since recency here only needs *relative* ordering, and actor isolation already guarantees strict monotonic increase, a counter sidesteps all wall-clock testability concerns (frozen/fake clocks, `ContinuousClock` resolution) entirely, without inventing a new clock-injection pattern this codebase doesn't otherwise have. Documented as a deliberate decision in the type's doc comment.

    Selection algorithm: `nonisolated static func selectSlot(candidates: [(tokens: [Int], lastUsed: Int)], newTokens: [Int]) -> Int?` — pure, unit-tested. Longest-common-prefix (LCP) against every slot's tokens; ties go to highest `lastUsed`; returns `nil` (not a slot with LCP 0) when no candidate has any overlap, so the caller rebuilds instead of checking out a useless slot (exactly as the review specified).

    `resolve()`: computes candidates, calls `selectSlot`, removes (checkout semantics, unchanged from before) the selected slot from the model's slot array, applies `decide()`'s existing pure decision function against just that slot's tokens (unchanged, still independently unit-tested), then a new `applyDecision` control-flow layer maps the decision to an actual `(cache, tokensToFeed)` result — this is where fixes #2 and #5 hook in (see below). Other slots stay available for a *different* incoming prompt from a concurrent call.

    `store()`: appends a new slot with a fresh recency stamp; if count exceeds 4, sorts by `lastUsed` ascending and evicts the oldest down to the cap.

    Deferred (explicitly, per task instructions): a total-token-budget-per-model memory guard. The 4-slot cap already bounds unbounded growth (closing item 6b from the review), and a byte/token budget on top felt like meaningful additional design surface (what happens when slot 1 alone exceeds the budget? evict it anyway and leave <4 slots? partial-trim it?) that didn't have a forcing use case in this pass. Noting it here as a clean follow-up if multi-session memory pressure becomes a real concern.

    **2. `trim()` return-value verification** — new `PromptCache.trimAndVerify(_:from:to:) -> Bool`: calls `trimPromptCache`, then checks `cache.allSatisfy { $0.offset == targetOffset }` (checks *every* layer, not just `cache.first`, for extra safety against a hypothetical mixed-cache-type array, though in practice all layers always advance in lockstep). Used everywhere a trim is attempted (`applyDecision`'s `.trimTo` branch, `regenerateLastToken`, and the exact-token-ID commit path's `.trimCacheByOne` case in `MLXLanguageModel.swift`). A failed verification always falls back to rebuild/remove rather than trusting a mismatched cache.

    **3. Real generated token IDs instead of re-encoding text** — investigated thoroughly rather than assuming:
    - `runReasoning`/`runToolCallReasoningPhase` already used real IDs from `generateTokens`/`generateTokensTask`, but I found (by tracing `TokenIterator`'s next()-ahead prefetch design in `MLXLMCommon/Evaluate.swift`) that they had a **latent, previously-undiscovered bug**: `generateLoopTask`'s `iterator.discardGeneratedToken()` branch discards the natural-EOS/stop token *without ever exposing it to the handler*, even though that token's forward pass already advanced `cache.offset` (the terminal token is fed as `previous:` input during the very call that returns it, before the next sample happens). This means the old EXACT-match-only `commitPromptCache(...generatedTokenIDs:)` was silently dropping the cache entry on essentially every normal, successful reasoning-path completion — the single most common case — defeating caching for those two paths almost entirely. Fixed via a new `PromptCache.reconcileCacheAdvance(observedTokenCount:cacheAdvance:) -> .matches/.trimCacheByOne/.untrustworthy`: when the cache's real advance is exactly one more than the observed IDs, TRIM the cache back by one (via `trimAndVerify`) to restore consistency, rather than fabricating a token value for the unknown missing one. This is more honest than the old text-path's "drop the trailing token and trust it" approach (which works there because the trailing token's *value* is redundant, not because a value is unknown).
    - `GuidedGenerationLoop.run` (`Libraries/MLXGuidedGeneration/GuidedGenerationLoop.swift`) now takes a new `onTokenCommitted: ((Int) -> Void)? = nil` parameter, called at the exact two points a token is fed through the model (the fast-forward loop's per-token `model()` call, and the plain single-token branch's `model()` call) — never at the `commitResult.isTerminated` grammar-accepted-stop branch, matching the doc's claim that that terminal token is sampled/detokenized/emitted but never fed back. This gives an exact (not reconstructed) token-ID list matching `cache.offset`'s advance by construction, letting `runGuidedGenerationLoop` (in `MLXLanguageModel.swift`) switch entirely to the exact-ID `commitPromptCache` overload, deleting its `emittedText`-accumulation/re-encoding entirely.
    - **Deliberately NOT changed**: `runUnconstrained`'s `generate()` call. Investigated whether real IDs are obtainable there: `generate()`'s `AsyncStream<Generation>` (`.chunk`/`.info`/`.toolCall`) never exposes token IDs — they're only visible inside `TextToolTokenLoopHandler` internal to `MLXLMCommon/Evaluate.swift`. Getting them out would require either (a) switching to the raw-token `generateTokens()` stream, which drops `effectiveStopStrings` support (a real, unrelated behavior regression), or (b) adding a new callback threaded through `generate()`/`generateTask()`/`generateLoopTask()` in shared, heavily-used `MLXLMCommon` code — a materially larger, riskier change than warranted here. `PromptCache.reconcileGeneratedTokens` (the text-reencoding heuristic) is kept, but now used *only* by this one remaining path; its doc comment was rewritten to reflect that scope and explain the same root-cause (`TokenIterator`'s prefetch design) rather than attributing the off-by-one only to the grammar loop.

    **4. KV-quantization array-aliasing bug** — `GuidedGenerationLoop.run` now returns a new `RunResult { tokenCount: Int; cache: [KVCache] }` instead of a bare `Int` (still `@discardableResult`). The doc comment no longer claims the caller's `cache:` argument reflects post-call state; it explicitly says to use `RunResult.cache` instead. **Documented, deliberate gap**: on the `incompleteOutput` throw path, no `RunResult` is produced (Swift can't return a value and throw), so a caller can't get the updated array back on that exit path specifically. Since `kvBits` is never set by any FoundationModels caller today (confirmed straightforwardly), this is currently unreachable/dormant, exactly as the task noted — fixing it fully would need either `cache` as an `inout` parameter (breaks the ergonomics of all ~10 call sites that omit `cache:` entirely, most in `IntegrationTesting`) or associated data on `GuidedGenerationError.incompleteOutput` (breaks `#expect(throws: GuidedGenerationError.incompleteOutput)` value-matching in existing tests). Chose the return-value approach as the least invasive correct fix for the reachable (success) path and documented the residual gap explicitly in both the parameter doc and the `- Throws:` doc.

    **IMPORTANT bug caught by adversarial double-check and fixed**: my first pass of wiring `RunResult.cache` into `runGuidedGenerationLoop` unconditionally did `finalCache = result.cache` regardless of whether `slot.cache` started as `nil` (the multimodal-round signal from `isTextOnly`/`makePromptCacheSlot`). Since `run()` always returns a concrete non-nil array (building one internally via `model.newCache` when passed `nil`), this silently turned a multimodal round's intentional-`nil` into a real, image/video/audio-conditioned cache that would then get committed and stored keyed only by `slot.promptTokens` (the TEXT tokens) — exactly the "silently wrong attention" class of bug this whole task exists to prevent, since a later text-only round could get this cache handed out via LCP match with no record that its KV state also depended on non-text input. Fixed: `finalCache` is now only overwritten from `result.cache` when `slot.cache != nil` to begin with, preserving the multimodal exclusion. Re-verified: full mandated suite still green after the fix.

    **5. llama.cpp's "n_past--" trick** — new `PromptCache.regenerateLastToken`: on `.reuseSuffix(count: 0)` (full match) or `.trimTo(_, thenSuffix: 0)` (shrunk/edited-earlier prompt), trims the cache back to `commonPrefixLength - 1` (verified via `trimAndVerify`) and feeds exactly `newTokens[commonPrefixLength - 1]` — a single-token forward pass instead of a full rebuild. Falls back to rebuild if the cache isn't trimmable or the trim doesn't verify.

    **6. Minor fixes**:
    - (a) `cachedTokenCount` in `.updateUsage` events: `PromptCacheSlot` gained a computed `cachedTokenCount: Int { promptTokens.count - feedInput.text.tokens.size }`. `sendUsageUpdate` now takes and reports a real `cachedTokenCount`, and `promptTokenCount` is now always the FULL transcript length (not the cache-shrunk suffix) at all 4 call sites (`runUnconstrained`, `runReasoning`, `runGuidedGeneration`, `runToolCalling`). Threaded `cachedTokenCount` up through `runGuidedGenerationLoop`'s and `executeToolCallingPhase2`'s return tuples. **Found and fixed via the local `review` tool** (not the double-check pass): the think-then-call tool-calling path computes `cachedTokenCount` against Phase 2's prompt (base input + Phase 1 reasoning tokens) but reports `promptTokenCount` against the shorter base input alone (kept that way deliberately, to avoid double-counting reasoning tokens as both prompt *and* output) — meaning `cachedTokenCount` could exceed `promptTokenCount` when a long prior conversation's cached prefix overlaps into what's nominally "new" reasoning-extended content. Fixed with a defensive `Swift.min(cachedTokenCount, promptTokenCount)` clamp inside `sendUsageUpdate` itself (mirroring the existing `reasoningTokenCount` clamp pattern), protecting all 4 call sites uniformly.
    - (b) Unbounded per-model memory: addressed by the 4-slot cap (see item 1); no additional byte/token budget added (see the deferral note above).

    **Deferred, per explicit task instructions**: the full expanded test suite from the review (baseline-equivalence against a real model, divergence-equivalence, actor-level concurrency test asserting no `KVCache` instance is ever handed out twice concurrently, property-based tests, multimodal-boundary integration test, etc.). Added focused unit tests for every new *pure* function instead (`selectSlot`, `trimAndVerify`, `reconcileCacheAdvance`), using real `KVCacheSimple` instances (not mocks) with directly-set `.offset` to exercise `trimAndVerify`'s shortfall-detection path. The double-check pass flagged the missing actor-level concurrency/eviction test as a gap — correct, but it's exactly the item explicitly deferred to the follow-up pass per this task's own scope, so left as-is and noted here rather than silently dropped.

    **Verification**: `swift build` clean throughout (checked incrementally after each file). Full mandated suite via the safe `xcodebuild build-for-testing` + per-bundle `xcrun xctest` pattern, no `-XCTest` filtering: CXGrammarTests 7/7, MLXFoundationModelsTests 108/108 (96 previous + 12 new), MLXGuidedGenerationTests 62/62, MLXLMTests 244/244 — **421 tests, 0 failures**, re-run twice (once before, once after the multimodal-guard fix found by adversarial double-check). `diagnostics check working` clean (0 errors, 0 warnings) both times. Adversarial double-check run via the `double-check` agent: first pass returned REVISE with the multimodal-cache-leak finding above (fixed and re-verified) plus the deferred-actor-test note (acknowledged, not fixed, per this task's own scope); a second `review` tool pass (local multi-agent review) caught the promptTokenCount/cachedTokenCount clamp issue independently.

    **Known pre-existing, out-of-scope observation** (not fixed, not introduced by this change): while tracing `GuidedGenerationLoop.run`'s fast-forward branch to place `onTokenCommitted` correctly, I noticed the originally-sampled token that triggers a non-empty `ffTokens` result is never itself fed through the model in that branch (only the FF tokens are) — this predates this change entirely and is a question about the FF algorithm's own correctness, not prompt-cache bookkeeping (my `onTokenCommitted` hook faithfully mirrors whatever the real feed pattern is, so it doesn't introduce any NEW mismatch). Flagging for a separate investigation, not touched here.

    Not committing per instructions — leaving in `doing` for review. `IntegrationTesting/.../EmitStopSignalTests.swift` has one call site (`let tokensGenerated = try GuidedGenerationLoop.run(...)`) that will need `.tokenCount` added given the `RunResult` return-type change; left untouched since the entire `IntegrationTesting` target already fails to compile in this sandbox for unrelated, pre-existing SDK-compat reasons (confirmed in the prior implementation pass via `git stash`), so I have no way to verify a fix there.
  timestamp: 2026-07-08T21:58:01.322304+00:00
- actor: claude-code
  id: 01kx1z4sjenqvxrfgrfdnvck62
  text: |-
    Addressed all 4 round-2 (2026-07-08 17:13) review findings on commit 1c9751b. Summary of changes:

    **Fix 1 — `generatedTokenCount` asymmetry on incomplete output** (`MLXLanguageModel.swift`, `runGuidedGenerationLoop`): the `catch GuidedGenerationError.incompleteOutput` block now sets `generatedTokenCount = generatedTokenIDs.count` before `incomplete = true`, so usage reporting on a cut-off guided-generation round reflects exactly what `onTokenCommitted` already reported as fed into the KV cache — matching `runReasoning`'s existing partial-output usage reporting.

    **Fix 2 — `tokenCount` on the maxTokens-budget throw path** (`GuidedGenerationLoop.swift`): went with the documentation + verification approach rather than adding an associated value to `GuidedGenerationError.incompleteOutput`, since that case is pattern-matched via `#expect(throws: GuidedGenerationError.incompleteOutput)` in two IntegrationTesting test files (`MaxTokenTruncationTests.swift`) that would break if the case gained an associated value. Rewrote `run()`'s `- Throws:` doc to explicitly state that *neither* field of `RunResult` (not just `cache`) is available on a throwing exit, and that a caller needing the count must tally `onTokenCommitted` itself — which is exactly what Fix 1's catch site now does correctly. Traced every catch site: the two IntegrationTesting `#expect(throws:)` sites don't rely on partial counts (they just assert the throw happens), and the `catch is GuidedGenerationError` in `GuidedGenerationTests.swift` also doesn't use a count. `runGuidedGenerationLoop` was the only call site with a real bug, now fixed by Fix 1.

    **Fix 3 — `quantizedKVStart` casing** (`GuidedGenerationLoop.swift`): renamed the parameter to `quantizedKvStart` (declaration, doc comment, both call sites) to match `kvBits`/`kvGroupSize`'s lowercase-`kv` convention. Scoped strictly to this one function (and the new helper below) — verified via repo-wide grep that no external caller passes `quantizedKVStart:` by label to `GuidedGenerationLoop.run`, and left the identically-named but unrelated `quantizedKVStart` parameter on `maybeQuantizeKVCache` (KVCache.swift) and `GenerateParameters` untouched, since those are established, widely-used names outside this function's scope. (Tracked follow-up task `tba2jnb` already anticipated this exact rename.)

    **Fix 4 — `tokenId`→`tokenID` + dedupe GPU-sync/mask block** (`GuidedGenerationLoop.swift`): renamed the local variable throughout `run()` (including the diagnostic log string) to match the established uppercase-`ID` convention next to the `onTokenCommitted` callback's own `tokenID` parameter. Extracted the duplicated 8-line quantize/asyncEval/computeMask/eval sequence (identical in both the fast-forward and normal single-token branches) into a new private static helper `advanceMaskOnCPUWhileGPURuns(logits:cache:kvBits:kvGroupSize:quantizedKvStart:constraint:vocabSize:logitDim:) -> (MaskResult, MLXArray?)`; both branches now call `(mask, maskArray) = try Self.advanceMaskOnCPUWhileGPURuns(...)`. Reduced `run()`'s line count modestly (per `tba2jnb`'s note, the larger 4-way decomposition remains out of scope here).

    **Verification**: `swift build` clean (0 errors). Full mandated suite via the safe pattern (`xcodebuild build-for-testing -scheme mlx-swift-lm-Package -destination 'platform=macOS' -clonedSourcePackagesDirPath .build -disableAutomaticPackageResolution -skipPackagePluginValidation`, then unfiltered `xcrun xctest <bundle>` per bundle, each under `timeout`): CXGrammarTests 7/7, MLXGuidedGenerationTests 62/62, MLXFoundationModelsTests 108/108, MLXLMTests 244/244 — **421/421 tests, 0 failures**, matching the pre-existing baseline (no regressions). `diagnostics check working` clean (0 errors, 0 warnings). Adversarial double-check agent run: **PASS**, no findings — confirmed the extracted helper is byte-identical in behavior to both original blocks, the casing rename is correctly scoped, the `tokenID` rename has no stragglers, and Fix 1's token count is provably correct by tracing `onTokenCommitted`'s call sites against `tokenCount`'s increments.

    Not committed and column left in `doing` per instructions — ready for `/review`.
  timestamp: 2026-07-08T22:56:27.214343+00:00
- actor: claude-code
  id: 01kx2d2aqrdyvg87ypgkzp1ycf
  text: |-
    Implemented the expanded test suite from Fable's review (the item explicitly deferred in the prior comment). Summary per item:

    **1. Baseline-equivalence integration test** — `IntegrationTesting/.../TextGeneration/PromptCacheEquivalenceTests.swift`, `cachedRunMatchesFreshPrefillEveryRound`. Real `gemma-3-270m-it-4bit` model, greedy sampling, 4 turns. Phase A runs the whole conversation once (real cross-turn cache reuse); Phase B replays each turn's identical growing transcript with `MLXLanguageModel.removePromptCache(modelID:)` (internal, reachable via `@testable import`) forcing a fresh rebuild each time, and asserts output text is identical per turn. UNVERIFIED BY EXECUTION (see sandbox note below).

    **2. Divergence-equivalence integration test** — same file, `editedEarlierTurnForcesTrimAndMatchesFreshRebuild`. Edits round 1's own reply to a guaranteed-different string before appending a new user turn, forcing `PromptCache.decide`'s `.trimTo` path against a real `KVCache.trim()` call. After an adversarial double-check flagged that the test only checked output-equality without confirming *which* decision branch actually fired (a latent gap that could pass vacuously if e.g. a tokenizer quirk hit `.reuseSuffix` instead), I extended the shared usage helper with `outputTokenCount` and added an explicit assertion bounding `cachedTokenCount` strictly between 0 and round 1's full stored slot length (`promptTokenCount + outputTokenCount`) — a range only reachable via `.trimTo` (0 would mean `.rebuild`; equal to the full length would mean `.reuseSuffix`). UNVERIFIED BY EXECUTION.

    **3. `trim()` semantics unit tests** — two parts:
    - `Tests/MLXLMTests/KVCacheTests.swift`: `testTrimThenContinueMatchesFreshPrefillOfTruncatedSequence` — prefills a real `KVCacheSimple` with 7 synthetic single-token key/value pairs, trims back 3, continues with 2 new tokens, and asserts the resulting state tensor equals an independently-built cache fed only the truncated prefix + the same continuation. Ran green (245/245 in MLXLMTests, up from 244).
    - `Tests/MLXFoundationModelsTests/PromptCacheTests.swift`: `chunkedCacheTrimShortfallIsRejected` — constructs a `ChunkedKVCache` with `startPosition > 0` directly via `.metaState` (no real model needed), proving `trimAndVerify`'s rejection path catches a legitimate trim shortfall. Ran green.

    **4. Guided/tool-call round-trip test** — `IntegrationTesting/.../TextGeneration/PromptCacheGuidedRoundTripTests.swift`, new file. Schema-constrained round 1 followed by a schema-constrained round 2 replaying round 1's answer; asserts `cachedTokenCount > 0` on round 2 (proving the guided-generation cache entry was actually stored/reused, not silently dropped by the original off-by-one this redesign fixed) and that round 2's output matches a cache-evicted baseline. UNVERIFIED BY EXECUTION.

    **5. Property-based tests on `decide`/`selectSlot`** — `Tests/MLXFoundationModelsTests/PromptCacheTests.swift`, two new suites (`PromptCacheDecidePropertyTests`, `PromptCacheSelectSlotPropertyTests`) running a seeded, dependency-free `SplitMix64` PRNG over 2000 deterministic cases each, checking: never `.trimTo` when `!isTrimmable`; `commonPrefixLength + thenSuffix == newTokens.count`; `.reuseSuffix` iff cached tokens are a genuine prefix; the resident-prefix + fed-suffix reconstruction reproduces `newTokens` for every decision kind; `selectSlot`'s LCP-with-recency-tiebreak selection is never beaten by an unselected candidate. No property-testing library exists in this repo's dependencies (checked Package.swift) — table-driven over a large deterministic case set instead, per the task's own fallback guidance. Ran green.

    **6. Actor concurrency test** — `Tests/MLXFoundationModelsTests/PromptCacheConcurrencyTests.swift`, new file, two tests against a real `PromptCache` actor + a fake `LanguageModel`. First test: 12 concurrent `resolve()` callers racing for one pre-stored slot, asserting (via `ObjectIdentifier`) every caller gets a distinct `KVCache` instance and exactly one reuses the pre-stored slot. Second test: sustained concurrent resolve/store churn (8 workers × 6 iterations) with a `Task.yield()` between checkout and checkin, using a separate `CheckoutTracker` actor to detect any double-checkout. **Found and fixed a bug in my own test code during development** (not production code): the first version dropped each result's `[KVCache]` array immediately after extracting its `ObjectIdentifier`, which let the allocator legitimately reuse a just-freed address for a later concurrent allocation — a classic ObjectIdentifier ABA collision that manifested as flaky false-positive "double-checkout" failures (`Set(identities).count == 2` instead of 12) in ~1/3 of runs. Fixed by collecting all result *boxes* (unconsumed, keeping every cache instance alive) from the task group before extracting any identities. Verified fix with 8 consecutive clean runs after the fix, 0/8 before. Ran green, matches item 6's ask precisely.

    **7. Multimodal boundary test** — `IntegrationTesting/.../TextGeneration/PromptCacheMultimodalBoundaryTests.swift`, new file. `isTextOnly`/`makePromptCacheSlot` are `private` inside `Executor` (not reachable even via `@testable import` — Swift's `private` is declaration-scoped, not module-scoped), so this can't be a true unit test; written as an opt-in (`MLX_RUN_VLM_INTEGRATION=1`) real-model integration test mirroring `VisionIntegrationTests`' existing pattern (`Qwen3-VL-4B-Instruct-4bit`, skipped by default). Text round 1 → image round 2 → text round 3 (continuing from round 1, not round 2), asserting round 2's `cachedTokenCount == 0` (multimodal rounds never participate in cache reuse) and round 3's `cachedTokenCount > 0` (the pre-image entry survives an intervening multimodal round). UNVERIFIED BY EXECUTION *and* not run here even hypothetically (opt-in, multi-GB VLM download, never attempted in this sandbox).

    **8. Adversarial re-encode** — verified via `grep` that no live re-encode-by-count risk remains: the one remaining path that reconstructs token IDs from decoded text (`commitPromptCache(...emittedText:...)`, used only by `runUnconstrained`) anchors its fidelity check to `cache.first?.offset` (the ground truth), not to a caller-assumed count, and any mismatch beyond the documented ±1 pattern is rejected via `PromptCache.reconcileGeneratedTokens` (already covered by the existing `PromptCacheReconciliationTests`). Confirmed moot, no new test needed.

    **9. Fixed `PromptCacheReuseTests.swift`'s silent-pass bug** — replaced `guard #available(...) else { return }` with `@available(iOS 27.0, macOS 27.0, visionOS 27.0, *)` directly on the `@Test` func plus `.enabled(if: ProcessInfo.processInfo.isOperatingSystemAtLeast(...))` on the `@Suite`, mirroring the working `.enabled(if:)` precedent already in this repo's `VisionIntegrationTests.swift`. An unsupported OS now reports as an explicit SKIPPED test, not a silent zero-assertion pass. Applied the identical pattern to all 3 new IntegrationTesting files from the start.

    **Refactor (from local `review` tool feedback, applied before finalizing)**: consolidated 4 near-identical private stream-draining helpers (1 pre-existing + 3 I'd written) into one shared `respondCollectingTextAndUsage` in `Support/FMTestHelpers.swift`, returning `(text, promptTokenCount, cachedTokenCount, outputTokenCount)`; all call sites use labeled tuple access so this was behavior-preserving everywhere (confirmed by the double-check agent).

    **Reviewed but not applied**: the local `review` tool repeatedly flagged unlabeled first arguments (`_ executor:`, `_ id:`) on several new AND pre-existing helper functions as a style issue. Left as-is: this matches this codebase's own established, consistent convention across dozens of pre-existing call sites in this exact test suite (`executeResponse(_ executor:...)`, `makeTestModel(_ id:...)` etc. used unlabeled everywhere already) — applying the suggestion would create inconsistency rather than fix one, the same class of finding this task's history has rejected before for the same reason.

    **Adversarial double-check**: ran via the `double-check` agent. First pass: REVISE — flagged that item 2's divergence test asserted output-equality only, without confirming `.trimTo` (vs `.reuseSuffix`/`.rebuild`) actually fired. Fixed as described above. All other findings (concurrency ABA fix soundness, property-test invariant correctness, `chunkedCacheTrimShortfallIsRejected`'s arithmetic, the FMTestHelpers.swift consolidation's behavior-preservation, the `.enabled(if:)`/`@available` fix) held up under scrutiny with no further changes needed.

    **IntegrationTesting compilability — reconfirmed still broken, pre-existing, unrelated to this work**: ran a fresh `xcodebuild build-for-testing -project IntegrationTesting.xcodeproj -scheme IntegrationTesting -destination 'platform=macOS' -skipPackagePluginValidation` (note: needed `-skipPackagePluginValidation` this time, or it fails even earlier on a "Validate plug-in CudaBuild" step) multiple times, both before and after all changes in this pass. Consistently fails with the same errors as previously documented: `Response.Action`/`.appendText`/`.updateUsage` are an opaque struct with static factory methods in this environment's Xcode-beta FoundationModels SDK, not a plain enum — breaking `if case .appendText(...) = action` pattern matching in ~15 files (`UpdateUsageEmissionTests`, `StreamingDeltaTests`, `PlainChatGenerationTests`, `GuidedGenerationIntegrationTests`, `ToolCallingReasoningTests`, `ToolCallingReasoningCharacterizationTests`, `FoundationModelsToolCallingTests`, `PrewarmGrammarTests`, `ConfigurationResolverRoutingTests`, and now also the shared `FMTestHelpers.swift` helper, confirming the new/consolidated code fails for the identical pre-existing reason, not a new one). All 3 new integration test files (`PromptCacheEquivalenceTests.swift`, `PromptCacheGuidedRoundTripTests.swift`, `PromptCacheMultimodalBoundaryTests.swift`) were confirmed submitted to the compiler in these runs; none produced any error distinct from this pre-existing category.

    **Full mandated verification (SPM package)**: `swift build` clean (0 errors) both before and after all changes. `xcodebuild build-for-testing -scheme mlx-swift-lm-Package -destination 'platform=macOS' -clonedSourcePackagesDirPath .build -disableAutomaticPackageResolution -skipPackagePluginValidation` succeeded. Per-bundle `xcrun xctest`, unfiltered, each under `timeout`: CXGrammarTests 7/7, MLXGuidedGenerationTests 62/62, MLXFoundationModelsTests 113/113 (108 previous + 5 new: `chunkedCacheTrimShortfallIsRejected`, `decideInvariantsHoldAcrossRandomCases`, `selectSlotInvariantsHoldAcrossRandomCases`, `concurrentResolveNeverDoubleChecksOutASlot`, `sustainedConcurrentChurnNeverDoubleChecksOut`), MLXLMTests 245/245 (244 previous + 1 new: `testTrimThenContinueMatchesFreshPrefillOfTruncatedSequence`) — 421 previous + 6 new = 427 tests total in the SPM package, 0 failures, no regressions. `diagnostics check working` clean (0 errors, 0 warnings).

    No production code (`PromptCache.swift`, `MLXLanguageModel.swift`, `GuidedGenerationLoop.swift`) was modified — `git diff --stat -- Tests IntegrationTesting Libraries` confirms zero changes outside `Tests/` and `IntegrationTesting/`. Not committing per instructions — leaving in `doing` for review.
  timestamp: 2026-07-09T02:59:46.552905+00:00
- actor: claude-code
  id: 01kx2gjs7ghq44erzhstr1eb4e
  text: |-
    Fixed round-9 review findings (test-only cleanup, no production code touched):

    1. Added a one-line `///` doc comment above each of the 8 test-suite structs this task created: `PromptCacheEquivalenceTests`, `PromptCacheGuidedRoundTripTests`, `PromptCacheMultimodalBoundaryTests`, `PromptCacheReuseTests` (IntegrationTesting), and `PromptCacheConcurrencyTests`, `PromptCacheDecisionTests`, `PromptCacheSlotSelectionTests`, `PromptCacheTrimAndVerifyTests` (unit tests). Placed directly above `@Suite(...)`, matching this repo's existing convention (doc comment immediately precedes the `@Suite`+`struct` pair). Left `FMTestHelpers.swift`'s `gemmaModelID` untouched per the DROP decision.

    2. `PromptCacheReuseTests.swift`'s `secondRoundPrefillsOnlyAppendedSuffix()`: extracted the duplicated `GenerationOptions(maximumResponseTokens: 8)` into a local `let generationOptions` at the top of the function, reused by both `makeExecutorRequest` calls.

    3. `PromptCacheTests.swift`: added file-level `private let propertyTestIterationCount = 2000` and `private let testVocabularySize = 4` (each with a one-line doc comment), and replaced every `2000`/`vocabulary: 4` occurrence in `decideInvariantsHoldAcrossRandomCases()` and `selectSlotInvariantsHoldAcrossRandomCases()` with the named constants.

    Verification: `swift build` clean (no warnings from touched files). Full mandated suite via the SAFE invocation (`xcodebuild build-for-testing` + unfiltered `xcrun xctest` per bundle, each under `timeout`): CXGrammarTests 7/7, MLXGuidedGenerationTests 62/62, MLXFoundationModelsTests 113/113 (including all 8 PromptCache suites, both property tests using the new constants), MLXLMTests 245/245 — 427/427 green, matching the task's previously-recorded total. IntegrationTesting files inspected by hand only (compile check not possible in this sandbox, per prior notes) — edits are syntactically/semantically correct Swift.

    Left in `doing` per instructions; not committed, column untouched.
  timestamp: 2026-07-09T04:01:11.408113+00:00
- actor: claude-code
  id: 01kx3dag5rtf3pn18d5s91j3kx
  text: 'Post-completion note: discovered 4 untracked test files (`PromptCacheTestSupport.swift`, `PromptCacheResolveTests.swift`, `PromptCacheSlotPoolTests.swift`, `PromptCacheMultiSessionTests.swift`) plus an uncommitted refactor of `PromptCacheConcurrencyTests.swift` sitting in the working tree while working a later task — almost certainly leftover, never-committed output from this task''s own massive test-expansion pass that got lost before the final commit. Verified all 5 files independently (build clean, full suite green including these suites confirmed running by name, no duplication with already-committed `PromptCacheTests.swift`/`PromptCacheConcurrencyTests.swift`, genuinely valuable additional coverage: `resolve()` end-to-end decision composition, LRU eviction, multi-session interleaving/concurrency). Committed separately as `c4e37a4`.'
  timestamp: 2026-07-09T12:23:28.696362+00:00
position_column: done
position_ordinal: '8380'
title: Untitled
---
## What\nSee git history / prior comments for full original design writeup and the Fable architectural review (multi-slot redesign, correctness fixes, expanded test suite).\n\n## Resolution (2026-07-08)\n\nCommit `6cec843`: initial single-slot implementation. Commit `1c9751b`: full multi-slot redesign per Fable review. Commit `a299a1f`, `bfd341e`: round-2/5 review fixes. Commit `1a383dc`: expanded test suite per Fable's testing-gaps list. All independently re-verified at each step: 427/427 tests green.\n\n**Known environment limitation (not a defect in this work)**: `IntegrationTesting` fails to compile in this sandbox against an unrelated, pre-existing SDK-compat break.\n\n## Review Findings, rounds 1-8 (2026-07-08 14:18 through 22:19)\n\nAll findings FIXED, DEFERRED to `^tba2jnb`/`^9jtbtkd`, REJECTED as factually incorrect/convention contradictions, or DROPPED as pre-existing test-support code — see prior dated sections for full detail.\n\n## Review Findings (2026-07-08 22:28) — round 9, on commit 1a383dc\n\n- [ ] `FMTestHelpers.swift` — `gemmaModelID` missing doc comment (3rd recurrence). **DROP** — confirmed pre-existing (predates qawe2hb entirely), blanket exception.\n- [ ] Missing doc comments on 8 test-suite structs this task itself created: `PromptCacheEquivalenceTests`, `PromptCacheGuidedRoundTripTests`, `PromptCacheMultimodalBoundaryTests`, `PromptCacheReuseTests`, `PromptCacheConcurrencyTests`, `PromptCacheDecisionTests`, `PromptCacheSlotSelectionTests`, `PromptCacheTrimAndVerifyTests`. **FIX** — these are qawe2hb's own new test code (not pre-existing tests this task merely touched), so the \"don't refactor existing tests\" exception does not apply; add a one-line doc comment to each.\n- [ ] `PromptCacheReuseTests.swift` — `GenerationOptions(maximumResponseTokens: 8)` duplicated within one test function. **FIX** — this task's own new test file; extract to a local constant.\n- [ ] `PromptCacheTests.swift` — `2000` iteration count and `vocabulary: 4` literals repeated across the two new property-based test functions. **FIX** — this task's own new test code; extract to file-level constants.